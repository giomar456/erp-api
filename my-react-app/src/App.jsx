import { forwardRef, useEffect, useMemo, useRef, useState } from 'react';
import { Capacitor, registerPlugin } from '@capacitor/core';

const TEST_API_URL = new URLSearchParams(window.location.search).get('api');
const BASE_URL = TEST_API_URL || 'https://erp-api-7x3d.onrender.com';
const EMPRESA = 'computer_army';
const APP_VERSION = '1.76';
const IS_NATIVE_APP = !!(Capacitor?.isNativePlatform && Capacitor.isNativePlatform());
const IS_PC_TESTER = typeof window !== 'undefined' && new URLSearchParams(window.location.search).get('tester') === 'pc';
const IS_PC_DESKTOP = typeof window !== 'undefined' && new URLSearchParams(window.location.search).get('desktop') === 'pc';
const CASH_SOUND_URL = `${import.meta.env.BASE_URL || '/'}sounds/cash-register.mp3`;
const AndroidDocumentShare = registerPlugin('AndroidDocumentShare');
const AndroidRadio = registerPlugin('AndroidRadio');
const APP_LOGO_DATA_URL = 'data:image/png;base64,';
const BRANCHES = [{ label: 'Computer Army', value: 'computer_army' }];
const DOC_TYPES = ['BOLETA', 'FACTURA', 'PASE', 'NOTA DE VENTA', 'PROFORMA'];
const DOCS_CAJA = new Set(['BOLETA', 'FACTURA', 'PASE', 'NOTA DE VENTA']);
const PAY_METHODS = ['EFECTIVO', 'TRANSFERENCIA', 'YAPE', 'PLIN', 'TARJETA'];
const PAY_STATES = ['PAGADO', 'CREDITO', 'DEUDA'];
const SUNAT_STATES = ['PENDIENTE', 'PROCESO', 'ACEPTADO', 'RECHAZADO'];
const WARRANTY_STATES = ['RECIBIDO', 'REVISION', 'APROBADO', 'RECHAZADO', 'ENTREGADO'];
const SERIES_STATES = ['DISPONIBLE', 'VENDIDO', 'GARANTIA', 'GARANTIA_INGRESO', 'GARANTIA_CAMBIO', 'RESERVADO', 'AGOTADO'];
const GARANTIA_RESOLUCIONES = [
  { id: 'PROVEEDOR_RESPONDIO', label: 'Proveedor respondio' },
  { id: 'CAMBIO_PRODUCTO', label: 'Cambio de producto' },
  { id: 'DEVOLUCION_DINERO', label: 'Devolucion de dinero' },
  { id: 'NOTA_CREDITO', label: 'Nota de credito' },
  { id: 'REPARADO', label: 'Reparado y entregado' },
  { id: 'RECHAZADO', label: 'Rechazado' },
  { id: 'EN_PROCESO', label: 'En proceso con proveedor' },
  { id: 'OTRO', label: 'Otro' },
];
const DEFAULT_WAREHOUSES = ['TIENDA', 'ALMACEN', 'VITRINA', 'TALLER', 'GARANTIA'];
const WAREHOUSES = DEFAULT_WAREHOUSES;
const THEME_COLORS = ['#304fb8', '#0f766e', '#ea580c', '#7c3aed', '#be123c', '#0891b2', '#166534', '#111827'];

function normalizeThemeColor(value) {
  const color = String(value || '').trim();
  return /^#[0-9a-fA-F]{6}$/.test(color) ? color : THEME_COLORS[0];
}

function currentEmpresa() {
  return localStorage.getItem('gg_erp_empresa') || EMPRESA;
}

const modules = [
  { id: 'dashboard', label: 'Panel', icon: 'dashboard', tone: 'bg-cyan-700' },
  { id: 'ventas', label: 'Ventas', icon: 'sales', tone: 'bg-emerald-700' },
  { id: 'reservas', label: 'Reservas', icon: 'reserve', tone: 'bg-pink-700' },
  { id: 'caja', label: 'Caja', icon: 'cash', tone: 'bg-amber-600' },
  { id: 'clientes', label: 'Clientes', icon: 'clients', tone: 'bg-blue-700' },
  { id: 'productos', label: 'Productos', icon: 'products', tone: 'bg-orange-600' },
  { id: 'inventario', label: 'Inventario', icon: 'inventory', tone: 'bg-violet-700' },
  { id: 'compras', label: 'Compras', icon: 'cart', tone: 'bg-sky-700' },
  { id: 'documentos', label: 'Documentos', icon: 'documents', tone: 'bg-red-700' },
  { id: 'sunat', label: 'SUNAT', icon: 'sunat', tone: 'bg-yellow-600' },
  { id: 'usuarios', label: 'Usuarios', icon: 'users', tone: 'bg-slate-700' },
  { id: 'radio', label: 'Radio', icon: 'radio', tone: 'bg-lime-700' },
  { id: 'servicios', label: 'Servicios', icon: 'service', tone: 'bg-fuchsia-700' },
  { id: 'garantias', label: 'Garantías', icon: 'shield', tone: 'bg-teal-700' },
  { id: 'auditoria', label: 'Registro', icon: 'history', tone: 'bg-purple-700' },
  { id: 'web', label: 'Web', icon: 'globe', tone: 'bg-indigo-700' },
  { id: 'ajustes', label: 'Ajustes', icon: 'settings', tone: 'bg-zinc-800' },
];

const emptyProduct = {
  id: '',
  nombre: '',
  categoria: '',
  marca: '',
  modelo: '',
  precio_compra: '',
  precio_venta: '',
  stock: '',
  almacen: 'TIENDA',
  sku_woo: '',
  observacion: '',
  imagen_url: '',
};

function NavIcon({ name, className = 'h-5 w-5' }) {
  const common = {
    fill: 'none',
    stroke: 'currentColor',
    strokeWidth: 2,
    strokeLinecap: 'round',
    strokeLinejoin: 'round',
  };
  const paths = {
    dashboard: <><rect x="3" y="3" width="7" height="7" rx="1.5" /><rect x="14" y="3" width="7" height="7" rx="1.5" /><rect x="3" y="14" width="7" height="7" rx="1.5" /><path d="M14 17h7M14 21h7M14 13h7" /></>,
    sales: <><path d="M4 19V5a2 2 0 0 1 2-2h11l3 3v13a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2Z" /><path d="M8 9h8M8 13h8M8 17h5" /></>,
    cash: <><rect x="3" y="7" width="18" height="12" rx="2" /><path d="M7 7V5h10v2M7 12h.01M11 12h.01M15 12h2M7 16h10" /></>,
    clients: <><circle cx="9" cy="8" r="3" /><path d="M3 20a6 6 0 0 1 12 0" /><circle cx="17" cy="9" r="2" /><path d="M15 17a5 5 0 0 1 6 3" /></>,
    products: <><path d="M12 3 3 7.5l9 4.5 9-4.5L12 3Z" /><path d="M3 7.5v9L12 21l9-4.5v-9" /><path d="M12 12v9" /></>,
    inventory: <><path d="M4 5h16v4H4zM6 9v10h12V9" /><path d="M9 13h6" /></>,
    cart: <><path d="M3 4h2l2.2 10.5a2 2 0 0 0 2 1.5h7.8a2 2 0 0 0 1.9-1.4L21 8H6" /><circle cx="10" cy="20" r="1" /><circle cx="18" cy="20" r="1" /></>,
    documents: <><path d="M7 3h7l4 4v14H7a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2Z" /><path d="M14 3v5h5M8 13h8M8 17h6" /></>,
    sunat: <><path d="M5 3h14v18H5z" /><path d="M8 7h8M8 11h8M8 15h5" /><path d="m14 17 2 2 4-5" /></>,
    reserve: <><path d="M6 4h12a2 2 0 0 1 2 2v14H4V6a2 2 0 0 1 2-2Z" /><path d="M8 2v4M16 2v4M4 10h16" /><path d="m8 16 2 2 5-5" /></>,
    users: <><circle cx="8" cy="8" r="3" /><circle cx="17" cy="8" r="3" /><path d="M2 21a6 6 0 0 1 12 0M12 21a6 6 0 0 1 10 0" /></>,
    radio: <><path d="M5 5h14v14H5z" /><path d="M9 5V3h6v2M9 9h6M9 13h6" /><circle cx="10" cy="17" r="1" /><circle cx="14" cy="17" r="1" /></>,
    service: <><path d="M14.7 6.3a4 4 0 0 0-5.4 5.4L4 17v3h3l5.3-5.3a4 4 0 0 0 5.4-5.4l-2.8 2.8-2-2 2.8-2.8Z" /><path d="M5 19l3-3" /></>,
    shield: <><path d="M12 3 5 6v5c0 5 3 8 7 10 4-2 7-5 7-10V6l-7-3Z" /><path d="m9 12 2 2 4-5" /></>,
    history: <><path d="M3 12a9 9 0 1 0 3-6.7" /><path d="M3 4v5h5M12 7v6l4 2" /></>,
    globe: <><circle cx="12" cy="12" r="9" /><path d="M3 12h18M12 3a14 14 0 0 1 0 18M12 3a14 14 0 0 0 0 18" /></>,
    settings: <><path d="M12 15.5A3.5 3.5 0 1 0 12 8a3.5 3.5 0 0 0 0 7.5Z" /><path d="M19.4 15a1.8 1.8 0 0 0 .3 2l.1.1-2.1 2.1-.1-.1a1.8 1.8 0 0 0-2-.3 1.8 1.8 0 0 0-1 1.6V21h-3v-.2a1.8 1.8 0 0 0-1-1.6 1.8 1.8 0 0 0-2 .3l-.1.1-2.1-2.1.1-.1a1.8 1.8 0 0 0 .3-2 1.8 1.8 0 0 0-1.6-1H5v-3h.2a1.8 1.8 0 0 0 1.6-1 1.8 1.8 0 0 0-.3-2l-.1-.1 2.1-2.1.1.1a1.8 1.8 0 0 0 2 .3 1.8 1.8 0 0 0 1-1.6V5h3v.2a1.8 1.8 0 0 0 1 1.6 1.8 1.8 0 0 0 2-.3l.1-.1 2.1 2.1-.1.1a1.8 1.8 0 0 0-.3 2 1.8 1.8 0 0 0 1.6 1h.2v3h-.2a1.8 1.8 0 0 0-1.6 1Z" /></>,
  };
  return <svg viewBox="0 0 24 24" className={className} aria-hidden="true" {...common}>{paths[name] || paths.dashboard}</svg>;
}

function money(value, currency = 'S/') {
  const num = Number(value || 0);
  return `${currency} ${num.toLocaleString('es-PE', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`;
}

function cleanPriceInput(value) {
  const raw = String(value ?? '').replace(',', '.');
  const cleaned = raw.replace(/[^\d.]/g, '');
  const firstDot = cleaned.indexOf('.');
  if (firstDot === -1) return cleaned;
  return `${cleaned.slice(0, firstDot + 1)}${cleaned.slice(firstDot + 1).replace(/\./g, '')}`;
}

function parsePrice(value) {
  const num = Number(cleanPriceInput(value));
  return Number.isFinite(num) ? num : 0;
}

function price99(value) {
  const num = parsePrice(value);
  const base = Number.isFinite(num) && num > 0 ? Math.floor(num) : 0;
  return (base + 0.99).toFixed(2);
}

async function copyText(value) {
  const text = String(value ?? '');
  if (!text) return;
  try {
    await navigator.clipboard?.writeText(text);
  } catch {
    const input = document.createElement('input');
    input.value = text;
    document.body.appendChild(input);
    input.select();
    document.execCommand('copy');
    input.remove();
  }
}

function splitSeriesInput(value) {
  const seen = new Set();
  return String(value || '')
    .split(/[,;\n\r|]+/)
    .map((serie) => serie.trim())
    .filter(Boolean)
    .filter((serie) => {
      const key = serie.toUpperCase();
      if (seen.has(key)) return false;
      seen.add(key);
      return true;
    });
}

function normalizeSeriesKey(value) {
  return String(value || '').toUpperCase().replace(/[^A-Z0-9]/g, '');
}

function normalizeSeriesInput(value) {
  return splitSeriesInput(value).join('\n');
}

function pad2(value) {
  return String(value).padStart(2, '0');
}

function documentDigits(value) {
  return String(value || '').replace(/\D/g, '');
}

async function lookupDocumentoApi(value) {
  const numero = documentDigits(value);
  if (!numero) {
    return { ok: false, found: false, msg: 'Ingresa un DNI (8 digitos) o RUC (11 digitos).' };
  }
  if (numero.length !== 8 && numero.length !== 11) {
    return { ok: false, found: false, msg: 'El documento debe tener 8 digitos (DNI) o 11 (RUC).' };
  }
  return apiFetch(`/consulta/documento/${encodeURIComponent(numero)}`);
}

function localDateForApi(date = new Date()) {
  return `${date.getFullYear()}-${pad2(date.getMonth() + 1)}-${pad2(date.getDate())}`;
}

function localTimestampForApi(date = new Date()) {
  return `${localDateForApi(date)} ${pad2(date.getHours())}:${pad2(date.getMinutes())}:${pad2(date.getSeconds())}`;
}

function asArray(value) {
  if (Array.isArray(value)) return value;
  if (Array.isArray(value?.data)) return value.data;
  if (Array.isArray(value?.items)) return value.items;
  if (Array.isArray(value?.productos)) return value.productos;
  if (Array.isArray(value?.documentos)) return value.documentos;
  if (Array.isArray(value?.garantias)) return value.garantias;
  if (Array.isArray(value?.usuarios)) return value.usuarios;
  return [];
}

const PRODUCT_SEARCH_MIN_TOKEN = 0.8;
const PRODUCT_SEARCH_MIN_SCORE = 0.68;
const PRODUCT_BRAND_TOKENS = new Set([
  'msi', 'asus', 'gigabyte', 'asrock', 'biostar', 'evga', 'zotac', 'sapphire', 'xfx', 'powercolor',
  'intel', 'amd', 'nvidia', 'kingston', 'corsair', 'thermaltake', 'coolermaster', 'cougar', 'redragon',
  'lenovo', 'hp', 'dell', 'acer', 'samsung', 'lg', 'logitech', 'razer', 'hyperx', 'crucial', 'seagate',
  'western', 'digital', 'wd', 'adata', 'teamgroup', 'hikvision', 'tplink', 'dlink', 'genius', 'trust',
]);

function normalizeProductSearchText(value) {
  return String(value || '')
    .toLowerCase()
    .normalize('NFD')
    .replace(/[\u0300-\u036f]/g, '')
    .replace(/[^a-z0-9]+/g, ' ')
    .trim();
}

function tokenizeProductSearch(value) {
  return normalizeProductSearchText(value).split(/\s+/).filter(Boolean);
}

function levenshteinDistance(a, b) {
  if (a === b) return 0;
  if (!a.length) return b.length;
  if (!b.length) return a.length;
  const rows = a.length + 1;
  const cols = b.length + 1;
  const dist = Array.from({ length: rows }, () => Array(cols).fill(0));
  for (let i = 0; i < rows; i += 1) dist[i][0] = i;
  for (let j = 0; j < cols; j += 1) dist[0][j] = j;
  for (let i = 1; i < rows; i += 1) {
    for (let j = 1; j < cols; j += 1) {
      const cost = a[i - 1] === b[j - 1] ? 0 : 1;
      dist[i][j] = Math.min(dist[i - 1][j] + 1, dist[i][j - 1] + 1, dist[i - 1][j - 1] + cost);
    }
  }
  return dist[rows - 1][cols - 1];
}

function geometricMean(values) {
  if (!values.length) return 0;
  if (values.some((value) => value <= 0)) return 0;
  return values.reduce((acc, value) => acc * value, 1) ** (1 / values.length);
}

function productTokenSimilarity(a, b) {
  if (!a || !b) return 0;
  if (a === b) return 1;
  const short = a.length <= b.length ? a : b;
  const long = a.length <= b.length ? b : a;
  if (short.length <= 2) {
    if (short === long) return 1;
    if (long.startsWith(short)) return 0.9;
    return 0;
  }
  if (long.includes(short)) {
    const ratio = short.length / long.length;
    if (ratio < 0.55) return 0;
    return 0.84 + ratio * 0.14;
  }
  const dist = levenshteinDistance(short, long);
  const maxAllowed = Math.max(1, Math.floor(long.length * 0.22));
  if (dist > maxAllowed) return 0;
  return 1 - dist / long.length;
}

function isModelLikeToken(token) {
  return /\d/.test(token) && token.length >= 3;
}

function extractModelCore(token) {
  const match = String(token || '').toLowerCase().match(/^([a-z]{1,5})(\d{2,5})([a-z]*)$/);
  if (!match) return null;
  return { prefix: match[1], digits: match[2], suffix: match[3] || '' };
}

function isChipsetModelToken(token) {
  return extractModelCore(token) !== null;
}

function modelSuffixCompatible(querySuffix, candidateSuffix) {
  if (!querySuffix || !candidateSuffix) return true;
  if (querySuffix === candidateSuffix) return true;
  if (candidateSuffix.startsWith(querySuffix) || querySuffix.startsWith(candidateSuffix)) return true;
  // Variantes pegadas al chipset: b550m-a -> b550ma, b550m-vc -> b550mvc, b550mpro -> b550mpro.
  if (querySuffix.length === 1 && candidateSuffix.startsWith(querySuffix)) return true;
  return false;
}

function modelDigitsEquivalent(queryDigits, candidateDigits) {
  if (queryDigits === candidateDigits) return 1;
  if (queryDigits.length !== candidateDigits.length) return 0;
  if (queryDigits[0] !== candidateDigits[0]) return 0;
  let diffs = 0;
  for (let i = 0; i < queryDigits.length; i += 1) {
    if (queryDigits[i] !== candidateDigits[i]) diffs += 1;
  }
  if (diffs > 1) return 0;
  return diffs === 0 ? 1 : 0.92;
}

function modelTokensEquivalent(queryToken, candidateToken) {
  const q = extractModelCore(queryToken);
  const c = extractModelCore(candidateToken);
  if (!q || !c) return false;
  if (q.prefix !== c.prefix) return false;
  const digitScore = modelDigitsEquivalent(q.digits, c.digits);
  if (!digitScore) return false;
  if (q.suffix && !c.suffix) return false;
  if (!modelSuffixCompatible(q.suffix, c.suffix)) return false;
  return digitScore;
}

const CHIPSET_MODEL_PATTERN = /[a-z]{1,5}\d{2,5}[a-z]*/g;

function collectChipsetModelCandidates(...texts) {
  const candidates = new Set();
  const parts = texts.map((text) => normalizeProductSearchText(text)).filter(Boolean);
  const combined = parts.join(' ');
  for (const norm of (combined ? [...parts, combined] : parts)) {
    for (const token of norm.split(/\s+/).filter(Boolean)) {
      candidates.add(token);
      for (let i = 0; i < token.length; i += 1) {
        if (!/[a-z]/.test(token[i])) continue;
        const embedded = token.slice(i).match(/^([a-z]{1,5}\d{2,5}[a-z]*)/);
        if (embedded?.[1] && embedded[1].length >= 4) candidates.add(embedded[1]);
      }
    }
    const pattern = new RegExp(CHIPSET_MODEL_PATTERN.source, 'g');
    let match;
    while ((match = pattern.exec(norm)) !== null) {
      if (match[0].length >= 4) candidates.add(match[0]);
    }
  }
  return [...candidates];
}

function bestChipsetModelTokenMatch(queryToken, productTokens) {
  let best = 0;
  for (const token of productTokens) {
    const score = modelTokensEquivalent(queryToken, token);
    if (score > best) best = score;
  }
  return best;
}

function chipsetModelMatchInProduct(queryToken, product) {
  const candidates = collectChipsetModelCandidates(
    product?.nombre,
    product?.marca,
    product?.modelo,
    product?.sku_woo,
    product?.categoria,
  );
  return bestChipsetModelTokenMatch(queryToken, candidates);
}

function productBrandTokens(product) {
  const found = new Set();
  const marca = normalizeProductSearchText(product?.marca || '');
  if (marca && PRODUCT_BRAND_TOKENS.has(marca)) found.add(marca);
  for (const token of tokenizeProductSearch(product?.nombre || '')) {
    if (PRODUCT_BRAND_TOKENS.has(token)) found.add(token);
  }
  return found;
}

function productHasBrand(product, brandToken) {
  const brands = productBrandTokens(product);
  if (brands.has(brandToken)) return true;
  const marca = normalizeProductSearchText(product?.marca || '');
  if (marca && productTokenSimilarity(brandToken, marca) >= 0.94) return true;
  return tokenizeProductSearch(product?.nombre || '').some((token) => token === brandToken);
}

function productConflictsRequiredBrands(product, requiredBrands) {
  if (!requiredBrands.length) return false;
  const productBrands = productBrandTokens(product);
  if (!productBrands.size) return false;
  return Array.from(productBrands).some((brand) => !requiredBrands.includes(brand));
}

function bestProductTokenMatch(queryToken, productTokens, fieldText = '', product = null) {
  if (PRODUCT_BRAND_TOKENS.has(queryToken)) {
    if (product && productHasBrand(product, queryToken)) return 1;
    let brandBest = 0;
    for (const token of productTokens) {
      if (token === queryToken) brandBest = 1;
      else brandBest = Math.max(brandBest, productTokenSimilarity(queryToken, token));
    }
    return brandBest;
  }
  if (isChipsetModelToken(queryToken)) {
    const candidates = fieldText
      ? collectChipsetModelCandidates(fieldText)
      : productTokens;
    return bestChipsetModelTokenMatch(queryToken, candidates.length ? candidates : productTokens);
  }
  let best = 0;
  for (const token of productTokens) {
    best = Math.max(best, productTokenSimilarity(queryToken, token));
  }
  return best;
}

function strictQueryTokenMatch(queryToken, product) {
  const nombre = String(product?.nombre || '');
  const marca = String(product?.marca || '');
  const modelo = String(product?.modelo || '');
  const nombreTokens = tokenizeProductSearch(nombre);
  const modeloTokens = tokenizeProductSearch(modelo);

  if (PRODUCT_BRAND_TOKENS.has(queryToken)) {
    return productHasBrand(product, queryToken) ? 1 : 0;
  }

  if (isModelLikeToken(queryToken)) {
    const modelTokens = [...nombreTokens, ...modeloTokens];
    if (isChipsetModelToken(queryToken)) {
      const chipsetScore = chipsetModelMatchInProduct(queryToken, product);
      if (chipsetScore > 0) return chipsetScore;
      return bestChipsetModelTokenMatch(queryToken, modelTokens);
    }
    let best = 0;
    for (const token of modelTokens) {
      best = Math.max(best, productTokenSimilarity(queryToken, token));
    }
    return best >= 0.86 ? best : 0;
  }

  const nombreBest = bestProductTokenMatch(queryToken, nombreTokens);
  if (nombreBest >= PRODUCT_SEARCH_MIN_TOKEN) return nombreBest;
  const modeloBest = bestProductTokenMatch(queryToken, modeloTokens);
  if (modeloBest >= PRODUCT_SEARCH_MIN_TOKEN) return modeloBest;
  const marcaBest = productTokenSimilarity(queryToken, normalizeProductSearchText(marca));
  if (marcaBest >= 0.94) return marcaBest;
  return 0;
}

function productSearchText(product) {
  return [product?.nombre, product?.marca, product?.modelo, product?.categoria, product?.sku_woo]
    .map((part) => String(part || '').trim())
    .filter(Boolean)
    .join(' ');
}

function fieldSearchSimilarity(queryTokens, fieldText, product = null) {
  const fieldTokens = tokenizeProductSearch(fieldText);
  if (!queryTokens.length || !fieldTokens.length) return 0;
  const matches = queryTokens.map((token) => bestProductTokenMatch(token, fieldTokens, fieldText, product));
  if (matches.some((score) => score < PRODUCT_SEARCH_MIN_TOKEN)) return 0;
  return geometricMean(matches);
}

function orderedTokenPhraseScore(queryTokens, fieldText) {
  const fieldTokens = tokenizeProductSearch(fieldText);
  if (!queryTokens.length || !fieldTokens.length) return 0;
  let cursor = 0;
  let used = 0;
  for (const qToken of queryTokens) {
    let bestIndex = -1;
    let bestScore = 0;
    for (let idx = cursor; idx < fieldTokens.length; idx += 1) {
      const score = productTokenSimilarity(qToken, fieldTokens[idx]);
      if (score > bestScore) {
        bestScore = score;
        bestIndex = idx;
      }
    }
    if (bestScore < PRODUCT_SEARCH_MIN_TOKEN) return 0;
    used += bestScore;
    cursor = bestIndex + 1;
  }
  return used / queryTokens.length;
}

function productSearchScore(query, product) {
  const q = String(query || '').trim();
  if (!q) return 1;
  const qTokens = tokenizeProductSearch(q);
  if (!qTokens.length) return 0;

  const nombre = String(product?.nombre || '');
  const marca = String(product?.marca || '');
  const modelo = String(product?.modelo || '');
  const requiredBrands = qTokens.filter((token) => PRODUCT_BRAND_TOKENS.has(token));

  if (productConflictsRequiredBrands(product, requiredBrands)) return 0;
  for (const brandToken of requiredBrands) {
    if (!productHasBrand(product, brandToken)) return 0;
  }

  const tokenMatches = qTokens.map((token) => strictQueryTokenMatch(token, product));
  if (tokenMatches.some((score) => score < PRODUCT_SEARCH_MIN_TOKEN)) return 0;

  const fullGeo = geometricMean(tokenMatches);
  const nombreField = fieldSearchSimilarity(qTokens, nombre, product);
  const modeloField = fieldSearchSimilarity(qTokens, modelo, product);
  const orderedNombre = orderedTokenPhraseScore(qTokens, nombre);

  const nombreNorm = normalizeProductSearchText(nombre);
  const queryNorm = normalizeProductSearchText(q);
  let exactBonus = 0;
  if (nombreNorm === queryNorm) exactBonus = 0.35;
  else if (nombreNorm.includes(queryNorm)) exactBonus = 0.24;
  else if (qTokens.every((token) => nombreNorm.includes(token))) exactBonus = 0.14;

  const score = (
    0.5 * fullGeo
    + 0.28 * (nombreField || orderedNombre || 0)
    + 0.12 * (modeloField || 0)
    + 0.1 * orderedNombre
    + exactBonus
  );

  if (requiredBrands.length && qTokens.some((token) => isModelLikeToken(token))) {
    const brandOk = requiredBrands.every((brand) => productHasBrand(product, brand));
    const modelOk = qTokens.filter((token) => isModelLikeToken(token))
      .every((token) => strictQueryTokenMatch(token, product) >= PRODUCT_SEARCH_MIN_TOKEN);
    if (!brandOk || !modelOk) return 0;
    return Math.min(1, Math.max(score + 0.08, fullGeo * 0.92));
  }

  let finalScore = Math.min(1, score);
  if (tokenMatches.every((matchScore) => matchScore >= PRODUCT_SEARCH_MIN_TOKEN)) {
    finalScore = Math.max(finalScore, fullGeo * 0.92);
  }
  return finalScore;
}

function filterProductsBySearch(products, query, { limit, minScore = PRODUCT_SEARCH_MIN_SCORE } = {}) {
  const q = String(query || '').trim();
  if (!q) return products;
  const ranked = products
    .map((product) => ({ product, score: productSearchScore(q, product) }))
    .filter((row) => row.score >= minScore)
    .sort((a, b) => b.score - a.score || String(a.product?.nombre || '').localeCompare(String(b.product?.nombre || '')));
  const max = Number.isFinite(limit) ? limit : ranked.length;
  return ranked.slice(0, max).map((row) => row.product);
}

function okResponse(value) {
  if (!value || typeof value !== 'object') return false;
  return value.ok === true || value.success === true || value.data !== undefined || value.id !== undefined;
}

function versionParts(value) {
  return String(value || '0').split(/[^\d]+/).filter(Boolean).map((part) => Number(part || 0));
}

function isNewerVersion(remote, current) {
  const a = versionParts(remote);
  const b = versionParts(current);
  const length = Math.max(a.length, b.length, 1);
  for (let i = 0; i < length; i += 1) {
    const left = a[i] || 0;
    const right = b[i] || 0;
    if (left > right) return true;
    if (left < right) return false;
  }
  return false;
}

function docNumber(doc) {
  if (doc?.numero && String(doc.numero).includes('-')) return String(doc.numero);
  if (doc?.serie || doc?.numero) return `${doc.serie || ''}${doc.serie && doc.numero ? '-' : ''}${doc.numero || ''}`;
  return String(doc?.id || '');
}

const DOCUMENT_EDIT_USERS = new Set(['giomar']);
const SERIES_EDIT_USERS = new Set(['giomar', 'mily']);
const SUNAT_EMIT_USERS = new Set(['giomar', 'jean', 'mily']);

function normalizeUserForPermission(value) {
  return String(value || '').trim().toLowerCase().replace(/[^a-z0-9]+/g, '');
}

function canEditProcessedDocuments(user) {
  return DOCUMENT_EDIT_USERS.has(normalizeUserForPermission(user));
}

function canEmitLegalSunat(user) {
  return SUNAT_EMIT_USERS.has(normalizeUserForPermission(user));
}

function canEditSeries(user) {
  return SERIES_EDIT_USERS.has(normalizeUserForPermission(user));
}

function safeImageSrc(value) {
  const text = String(value || '').trim();
  if (!text) return '';
  if (text.startsWith('data:image/')) return text;
  if (/^https?:\/\//i.test(text)) return text;
  if (text.startsWith('/')) return text;
  return '';
}

function cssImageUrl(value) {
  const src = safeImageSrc(value);
  return src ? `url("${src.replace(/"/g, '\\"')}")` : '';
}

function fileToDataUrl(file, maxMb = 4) {
  return new Promise((resolve, reject) => {
    if (!file) {
      resolve('');
      return;
    }
    if (!file.type.startsWith('image/')) {
      reject(new Error('Selecciona una imagen PNG, JPG o WebP.'));
      return;
    }
    if (file.size > maxMb * 1024 * 1024) {
      reject(new Error(`La imagen no debe pasar de ${maxMb} MB.`));
      return;
    }
    const reader = new FileReader();
    reader.onload = () => resolve(String(reader.result || ''));
    reader.onerror = () => reject(new Error('No se pudo cargar la imagen.'));
    reader.readAsDataURL(file);
  });
}

function fileToBase64(file, maxMb = 8) {
  return new Promise((resolve, reject) => {
    if (!file) {
      resolve('');
      return;
    }
    if (file.size > maxMb * 1024 * 1024) {
      reject(new Error(`El archivo no debe pasar de ${maxMb} MB.`));
      return;
    }
    const reader = new FileReader();
    reader.onload = () => {
      const text = String(reader.result || '');
      resolve(text.includes(',') ? text.split(',').pop() : text);
    };
    reader.onerror = () => reject(new Error('No se pudo leer el archivo.'));
    reader.readAsDataURL(file);
  });
}

function useDeviceMode() {
  const getMode = () => (typeof window !== 'undefined' && window.innerWidth >= 900 ? 'dex' : 'phone');
  const [mode, setMode] = useState(getMode);
  useEffect(() => {
    const update = () => setMode(getMode());
    update();
    window.addEventListener('resize', update);
    window.addEventListener('orientationchange', update);
    return () => {
      window.removeEventListener('resize', update);
      window.removeEventListener('orientationchange', update);
    };
  }, []);
  return mode;
}

async function apiFetch(endpoint, options = {}) {
  const url = new URL(`${BASE_URL}${endpoint}`);
  const empresa = currentEmpresa();
  url.searchParams.append('sucursal', empresa);
  url.searchParams.append('empresa', empresa);
  Object.entries(options.params || {}).forEach(([key, value]) => {
    if (value !== undefined && value !== null && value !== '') url.searchParams.append(key, value);
  });

  try {
    const controller = new AbortController();
    const timeoutMs = Number(options.timeoutMs || 18000);
    const timeout = window.setTimeout(() => controller.abort(), timeoutMs);
    const response = await fetch(url, {
      ...options,
      signal: options.signal || controller.signal,
      headers: {
        'Content-Type': 'application/json',
        ...(options.headers || {}),
      },
      body: options.json ? JSON.stringify({ ...options.json, sucursal: empresa, empresa }) : options.body,
    });
    window.clearTimeout(timeout);
    const text = await response.text();
    let data = {};
    try {
      data = text ? JSON.parse(text) : {};
    } catch {
      data = { ok: false, msg: text };
    }
    if (!response.ok && !data.msg) data.msg = `HTTP ${response.status}`;
    return data;
  } catch (error) {
    if (error?.name === 'AbortError') {
      return { ok: false, msg: 'Servidor lento o sin respuesta. Intenta nuevamente.' };
    }
    return { ok: false, msg: error?.message || 'No hay conexion con el servidor.' };
  }
}

function fileToReceiptPayload(file) {
  return new Promise((resolve, reject) => {
    if (!file) {
      resolve({});
      return;
    }
    if (file.size > 15 * 1024 * 1024) {
      reject(new Error('El comprobante no debe pasar de 15 MB.'));
      return;
    }
    const reader = new FileReader();
    reader.onload = () => {
      const dataUrl = String(reader.result || '');
      const base64 = dataUrl.includes(',') ? dataUrl.split(',').pop() : '';
      resolve({
        comprobante_pago: file.name,
        comprobante_pago_nombre: file.name,
        comprobante_pago_mime: file.type || 'application/octet-stream',
        comprobante_pago_tamano: file.size,
        comprobante_pago_base64: base64,
        comprobante_pago_data_url: dataUrl,
      });
    };
    reader.onerror = () => reject(new Error('No se pudo leer el comprobante.'));
    reader.readAsDataURL(file);
  });
}

function fileToProductImage(file) {
  return new Promise((resolve, reject) => {
    if (!file) {
      resolve('');
      return;
    }
    if (!String(file.type || '').startsWith('image/')) {
      reject(new Error('Selecciona una imagen valida.'));
      return;
    }
    if (file.size > 5 * 1024 * 1024) {
      reject(new Error('La imagen no debe pasar de 5 MB.'));
      return;
    }
    const reader = new FileReader();
    reader.onload = () => resolve(String(reader.result || ''));
    reader.onerror = () => reject(new Error('No se pudo leer la imagen.'));
    reader.readAsDataURL(file);
  });
}

async function filesToReceiptsPayload(fileList) {
  const files = Array.from(fileList || []);
  const receipts = [];
  for (const file of files) {
    const payload = await fileToReceiptPayload(file);
    receipts.push({
      ...payload,
      nombre: payload.comprobante_pago_nombre,
      mime: payload.comprobante_pago_mime,
      tamano: payload.comprobante_pago_tamano,
      base64: payload.comprobante_pago_base64,
      data_url: payload.comprobante_pago_data_url,
    });
  }
  const first = receipts[0] || {};
  return { ...first, comprobantes_pago: receipts };
}

function blobToBase64(blob) {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    reader.onload = () => {
      const dataUrl = String(reader.result || '');
      resolve(dataUrl.includes(',') ? dataUrl.split(',').pop() : dataUrl);
    };
    reader.onerror = () => reject(new Error('No se pudo leer el audio.'));
    reader.readAsDataURL(blob);
  });
}

function bufferToBase64(buffer) {
  let binary = '';
  const bytes = new Uint8Array(buffer);
  const chunkSize = 0x8000;
  for (let i = 0; i < bytes.length; i += chunkSize) {
    binary += String.fromCharCode(...bytes.subarray(i, i + chunkSize));
  }
  return btoa(binary);
}

function encodeWav(samples, sampleRate) {
  const bytesPerSample = 2;
  const blockAlign = bytesPerSample;
  const buffer = new ArrayBuffer(44 + samples.length * bytesPerSample);
  const view = new DataView(buffer);
  const writeString = (offset, value) => {
    for (let i = 0; i < value.length; i += 1) view.setUint8(offset + i, value.charCodeAt(i));
  };
  writeString(0, 'RIFF');
  view.setUint32(4, 36 + samples.length * bytesPerSample, true);
  writeString(8, 'WAVE');
  writeString(12, 'fmt ');
  view.setUint32(16, 16, true);
  view.setUint16(20, 1, true);
  view.setUint16(22, 1, true);
  view.setUint32(24, sampleRate, true);
  view.setUint32(28, sampleRate * blockAlign, true);
  view.setUint16(32, blockAlign, true);
  view.setUint16(34, 16, true);
  writeString(36, 'data');
  view.setUint32(40, samples.length * bytesPerSample, true);
  let offset = 44;
  for (let i = 0; i < samples.length; i += 1, offset += 2) {
    const sample = Math.max(-1, Math.min(1, samples[i]));
    view.setInt16(offset, sample < 0 ? sample * 0x8000 : sample * 0x7fff, true);
  }
  return buffer;
}

function receiptListFromDoc(doc, detail = {}) {
  const raw = doc?.comprobantes_pago || detail?.comprobantes_pago || detail?.documento?.comprobantes_pago || [];
  const list = Array.isArray(raw) ? raw : [];
  if (list.length) {
    return list.map((item) => ({
      nombre: item.nombre || item.comprobante_pago_nombre || 'Comprobante',
      mime: item.mime || item.comprobante_pago_mime || 'application/octet-stream',
      data_url: item.data_url || item.comprobante_pago_data_url || '',
      base64: item.base64 || item.comprobante_pago_base64 || '',
    }));
  }
  const merged = { ...(detail?.documento || {}), ...(doc || {}), ...(detail || {}) };
  if (merged.comprobante_pago_data_url || merged.comprobante_pago_base64 || merged.comprobante_pago_nombre) {
    return [{
      nombre: merged.comprobante_pago_nombre || 'Comprobante',
      mime: merged.comprobante_pago_mime || 'application/octet-stream',
      data_url: merged.comprobante_pago_data_url || '',
      base64: merged.comprobante_pago_base64 || '',
    }];
  }
  return [];
}

function escapeHtml(value) {
  return String(value ?? '')
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;')
    .replace(/'/g, '&#39;');
}

function detailLinesFromPayload(payload) {
  const candidates = [
    payload?.detalle,
    payload?.items,
    payload?.lineas,
    payload?.data?.detalle,
    payload?.data?.items,
    payload?.data,
  ];
  for (const candidate of candidates) {
    const rows = asArray(candidate);
    if (rows.length) return rows;
  }
  return [];
}

function documentFileName(title) {
  const clean = String(title || 'documento')
    .normalize('NFD')
    .replace(/[\u0300-\u036f]/g, '')
    .replace(/[^A-Za-z0-9_-]+/g, '_')
    .replace(/^_+|_+$/g, '')
    .slice(0, 80);
  return `${clean || 'documento'}.pdf`;
}

function formatSeriesForPrint(seriesText) {
  const series = splitSeriesInput(seriesText).map((item) => String(item || '')
    .trim()
    .replace(/^(s\s*\/?\s*n|sn|serie)\s*[:\-]?\s*/i, '')
    .trim()
    .toUpperCase())
    .filter(Boolean);
  return series.length
    ? series.map((serie, idx) => (idx === 0 ? `S/N: ${serie}` : `     ${serie}`)).join('\n')
    : '';
}

function printRowHeightMm(baseHeight, seriesText) {
  const count = splitSeriesInput(seriesText).length;
  if (!count) return baseHeight;
  return Math.max(baseHeight, baseHeight + Math.max(0, count - 1) * 2.4 + 1.2);
}

function amountToWordsSoles(value) {
  const total = Number(value || 0);
  const entero = Math.floor(Math.abs(total));
  const centimos = Math.round((Math.abs(total) - entero) * 100);
  const unidades = ['', 'UNO', 'DOS', 'TRES', 'CUATRO', 'CINCO', 'SEIS', 'SIETE', 'OCHO', 'NUEVE'];
  const especiales = ['DIEZ', 'ONCE', 'DOCE', 'TRECE', 'CATORCE', 'QUINCE', 'DIECISEIS', 'DIECISIETE', 'DIECIOCHO', 'DIECINUEVE'];
  const decenas = ['', '', 'VEINTE', 'TREINTA', 'CUARENTA', 'CINCUENTA', 'SESENTA', 'SETENTA', 'OCHENTA', 'NOVENTA'];
  const centenas = ['', 'CIENTO', 'DOSCIENTOS', 'TRESCIENTOS', 'CUATROCIENTOS', 'QUINIENTOS', 'SEISCIENTOS', 'SETECIENTOS', 'OCHOCIENTOS', 'NOVECIENTOS'];
  const tens = (n) => {
    if (n < 10) return unidades[n];
    if (n < 20) return especiales[n - 10];
    if (n === 20) return 'VEINTE';
    if (n < 30) return `VEINTI${unidades[n - 20]}`;
    const d = Math.floor(n / 10);
    const u = n % 10;
    return u ? `${decenas[d]} Y ${unidades[u]}` : decenas[d];
  };
  const hundreds = (n) => {
    if (n === 0) return '';
    if (n === 100) return 'CIEN';
    if (n < 100) return tens(n);
    const c = Math.floor(n / 100);
    const r = n % 100;
    return `${centenas[c]}${r ? ` ${tens(r)}` : ''}`;
  };
  const words = (n) => {
    if (n === 0) return 'CERO';
    if (n < 1000) return hundreds(n);
    if (n < 1000000) {
      const miles = Math.floor(n / 1000);
      const rest = n % 1000;
      return `${miles === 1 ? 'MIL' : `${hundreds(miles)} MIL`}${rest ? ` ${hundreds(rest)}` : ''}`;
    }
    return String(n);
  };
  return `SON ${words(entero)} Y ${String(centimos).padStart(2, '0')}/100 SOLES`;
}

async function nativePdfAction(action, viewer) {
  if (!viewer?.html) return false;
  if (Capacitor.getPlatform?.() !== 'android') return false;
  const payload = {
    title: viewer.title || 'Documento',
    fileName: viewer.fileName || documentFileName(viewer.title),
    html: viewer.html,
  };
  if (action === 'share') {
    await AndroidDocumentShare.shareHtmlPdf(payload);
    return true;
  }
  if (action === 'download') {
    const res = await AndroidDocumentShare.saveHtmlPdf(payload);
    if (res?.displayName) alert(`PDF guardado en Descargas:\n${res.displayName}`);
    return true;
  }
  return false;
}

async function downloadBlobFile(blob, fileName) {
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = fileName || 'documento.pdf';
  document.body.appendChild(a);
  a.click();
  a.remove();
  window.setTimeout(() => URL.revokeObjectURL(url), 1500);
}

function downloadUrlFile(url, fileName) {
  const a = document.createElement('a');
  a.href = url;
  a.download = fileName || 'documento.pdf';
  a.target = '_blank';
  a.rel = 'noopener';
  document.body.appendChild(a);
  a.click();
  a.remove();
}

async function desktopDownloadPdf(url, fileName) {
  const bridge = window.pywebview?.api;
  if (!bridge?.download_pdf) return false;
  const res = await bridge.download_pdf(url, fileName || 'documento.pdf');
  if (!res?.ok) throw new Error(res?.msg || 'No se pudo guardar en Descargas.');
  alert(`PDF guardado en Descargas:\n${res.displayName || fileName || 'documento.pdf'}`);
  return true;
}

async function serverPdfAction(action, viewer) {
  if (!viewer?.pdfUrl) return false;
  const fileName = viewer.fileName || documentFileName(viewer.title);
  const platform = Capacitor.getPlatform?.();
  const url = action === 'download' ? (viewer.downloadUrl || viewer.pdfUrl) : viewer.pdfUrl;
  if (platform !== 'android' && action === 'download' && await desktopDownloadPdf(url, fileName)) {
    return true;
  }
  const res = await fetch(url, { cache: 'no-store' });
  if (!res.ok) throw new Error(`Servidor PDF ${res.status}`);
  const blob = await res.blob();
  if (platform === 'android') {
    const payload = {
      title: viewer.title || 'Documento',
      fileName,
      pdfBase64: await blobToBase64(blob),
    };
    if (action === 'download') {
      const saved = await AndroidDocumentShare.saveBase64Pdf(payload);
      if (saved?.displayName) alert(`PDF guardado en Descargas:\n${saved.displayName}`);
      return true;
    }
    if (action === 'share') {
      await AndroidDocumentShare.shareBase64Pdf(payload);
      return true;
    }
  }
  if (action === 'download') {
    try {
      await downloadBlobFile(blob, fileName);
    } catch {
      downloadUrlFile(viewer.downloadUrl || viewer.pdfUrl, fileName);
    }
    return true;
  }
  if (action === 'share' && navigator.share) {
    try {
      const file = new File([blob], fileName, { type: 'application/pdf' });
      if (navigator.canShare?.({ files: [file] })) {
        await navigator.share({ title: viewer.title || 'Documento', files: [file] });
      } else {
        await navigator.share({ title: viewer.title || 'Documento', url: viewer.pdfUrl });
      }
      return true;
    } catch (error) {
      if (String(error?.message || '').toLowerCase().includes('cancel')) return true;
      throw error;
    }
  }
  return false;
}

async function printServerPdf(viewer) {
  if (!viewer?.pdfUrl) return false;
  const printUrl = viewer.printUrl || viewer.pdfUrl;
  const iframe = document.createElement('iframe');
  iframe.style.position = 'fixed';
  iframe.style.right = '0';
  iframe.style.bottom = '0';
  iframe.style.width = '0';
  iframe.style.height = '0';
  iframe.style.border = '0';
  iframe.setAttribute('aria-hidden', 'true');
  document.body.appendChild(iframe);
  try {
    await new Promise((resolve, reject) => {
      const timer = window.setTimeout(resolve, 2500);
      iframe.onload = () => {
        window.clearTimeout(timer);
        resolve();
      };
      iframe.onerror = reject;
      iframe.src = printUrl;
    });
    iframe.contentWindow?.focus?.();
    iframe.contentWindow?.print?.();
    window.setTimeout(() => iframe.remove(), 6000);
    return true;
  } catch {
    iframe.remove();
    const win = window.open(printUrl, '_blank', 'noopener');
    return !!win;
  }
}

async function fallbackDownloadPdf(viewer) {
  if (!viewer?.html) return;
  const iframe = document.createElement('iframe');
  iframe.style.position = 'fixed';
  iframe.style.left = '-10000px';
  iframe.style.top = '0';
  iframe.style.width = '794px';
  iframe.style.height = '1123px';
  iframe.style.opacity = '0';
  iframe.setAttribute('aria-hidden', 'true');
  document.body.appendChild(iframe);
  try {
    await new Promise((resolve) => {
      iframe.onload = resolve;
      iframe.srcdoc = viewer.html;
      window.setTimeout(resolve, 900);
    });
    const page = iframe.contentDocument?.querySelector('.page') || iframe.contentDocument?.body;
    if (!page) throw new Error('No se pudo preparar el PDF.');
    const [{ default: html2canvas }, { jsPDF }] = await Promise.all([
      import('html2canvas'),
      import('jspdf'),
    ]);
    const canvas = await html2canvas(page, {
      backgroundColor: '#ffffff',
      scale: 2,
      useCORS: true,
      logging: false,
      windowWidth: page.scrollWidth,
      windowHeight: page.scrollHeight,
    });
    const pdf = new jsPDF({ orientation: 'portrait', unit: 'mm', format: 'a4', compress: true });
    pdf.addImage(canvas.toDataURL('image/jpeg', 0.98), 'JPEG', 0, 0, 210, 297);
    pdf.save(viewer.fileName || documentFileName(viewer.title));
  } finally {
    iframe.remove();
  }
}

function downloadTextFile(content, fileName, mime = 'text/html;charset=utf-8') {
  const blob = new Blob([content], { type: mime });
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = fileName;
  document.body.appendChild(a);
  a.click();
  a.remove();
  window.setTimeout(() => URL.revokeObjectURL(url), 1200);
}

function fallbackDownloadPrintableHtml(viewer) {
  if (!viewer?.html) return;
  const htmlName = String(viewer.fileName || documentFileName(viewer.title)).replace(/\.pdf$/i, '.html');
  downloadTextFile(viewer.html, htmlName);
}

let documentConfigCache = null;

async function fetchDocumentConfig() {
  if (documentConfigCache) return documentConfigCache;
  try {
    const res = await apiFetch('/config/documento');
    documentConfigCache = res?.data || res?.config || res || {};
  } catch {
    documentConfigCache = {};
  }
  return documentConfigCache;
}

function textWithBreaks(value) {
  return escapeHtml(value || '').replace(/\r?\n/g, '<br>');
}

function documentLayoutNumber(layout, key, fallback, min, max) {
  const raw = Number(layout?.[key]);
  const value = Number.isFinite(raw) ? raw : fallback;
  return Math.max(min, Math.min(max, value));
}

function buildDocumentPreviewHtml(doc, lines, config = {}) {
  const editor = config?.doc_editor || {};
  const labels = editor.labels || {};
  const texts = editor.texts || {};
  const layout = editor.layout || {};
  const maxRows = Math.round(documentLayoutNumber(layout, 'max_productos', 12, 1, 24));
  const rowHeight = documentLayoutNumber(layout, 'alto_fila_mm', 6.5, 5, 18);
  const tableHeight = documentLayoutNumber(layout, 'alto_tabla_mm', 86, 80, 150);
  const tableFont = documentLayoutNumber(layout, 'letra_tabla_px', 7.0, 5, 10);
  const descFont = documentLayoutNumber(layout, 'letra_descripcion_px', 7.0, 5, 10.5);
  const logoWidth = documentLayoutNumber(layout, 'logo_ancho_mm', 24, 8, 45);
  const logoHeight = documentLayoutNumber(layout, 'logo_alto_mm', 15, 8, 35);
  const logoTop = documentLayoutNumber(layout, 'logo_bajar_mm', 23, 0, 30);
  const pagePaddingTop = documentLayoutNumber(layout, 'margen_superior_mm', 8, 3, 18);
  const companyName = texts.empresa || config.company_name || config.empresa || 'CORPORACION COMPUTER ARMY EIRL';
  const address = texts.direccion || config.address || config.direccion || 'PRINCIPAL >> AV. INCA GARCILASO DE LA VEGA NRO. 1348 INT2B 130-131 - CERCADO DE LIMA - LIMA - PERU';
  const slogan = texts.slogan || '';
  const ruc = config.ruc || '20611068701';
  const configuredLogo = String(config.logo || config.document_logo || '').trim();
  const logo = configuredLogo || APP_LOGO_DATA_URL;
  const docType = String(doc.tipo || 'DOCUMENTO').toUpperCase();
  const docTitle = {
    BOLETA: 'BOLETA DE VENTA<br>ELECTRONICA',
    FACTURA: 'FACTURA<br>ELECTRONICA',
    PROFORMA: 'PROFORMA',
    PASE: 'PASE',
    'NOTA DE VENTA': 'NOTA DE VENTA',
  }[docType] || docType;
  const issueDate = String(doc.fecha_emision || doc.fecha || localDateForApi()).slice(0, 10);
  const dueDate = doc.fecha_vencimiento ? String(doc.fecha_vencimiento).slice(0, 10) : '-';
  const printableRows = (lines || []).slice(0, maxRows);
  const rows = printableRows.map((line, idx) => {
    const qty = Number(line.cantidad || 0);
    const price = Number(line.precio_unitario || line.precio || 0);
    const total = Number(line.total || qty * price || 0);
    const desc = line.descripcion || line.nombre || line.producto || '';
    const series = line.series_texto || line.serie || '';
    const seriesLabel = formatSeriesForPrint(series);
    const rowMinHeight = printRowHeightMm(rowHeight, series);
    return `<tr class="item-row" style="height:${rowMinHeight}mm;min-height:${rowMinHeight}mm">
      <td>${idx + 1}</td>
      <td>UNIDADES</td>
      <td><b>${textWithBreaks(String(desc).toUpperCase())}</b>${seriesLabel ? `<span class="series-print">${escapeHtml(seriesLabel)}</span>` : ''}</td>
      <td>${qty}</td>
      <td>${money(total)}</td>
      <td>${money(price)}</td>
    </tr>`;
  }).join('');
  const fillerRow = '<tr class="filler-row"><td>&nbsp;</td><td></td><td></td><td></td><td></td><td></td></tr>';
  const title = `${escapeHtml(doc.tipo || 'DOCUMENTO')} ${escapeHtml(docNumber(doc))}`;
  const subtotal = Number(doc.subtotal || doc.total || 0);
  const igv = Number(doc.igv || 0);
  const grandTotal = Number(doc.total || 0);
  const bankLines = [
    config.cuenta_bcp ? `Bcp soles : ${config.cuenta_bcp}` : 'Bcp soles :1941066028058',
    'Titular:Computer Army Eirl',
    config.cuenta_interbank ? `Interbank soles cuenta corriente : ${config.cuenta_interbank}` : 'Interbank soles cuenta corriente : 2003005323345',
    'Titular: Computer Army eirl',
  ].filter(Boolean);
  const configuredLegalLine2 = String(texts.legal_line2 || '').trim();
  const legalLine2 = configuredLegalLine2 && !(docType !== 'BOLETA' && configuredLegalLine2.toUpperCase().includes('BOLETA'))
    ? configuredLegalLine2
    : `Representacion impresa de la ${docTitle.replace(/<br>/g, ' ')}`;
  const legalLines = [
    texts.legal_line1 || 'Autorizado mediante resolucion Nro 034-005-0010431/SUNAT',
    legalLine2,
    texts.legal_line3 || 'Emitido mediante G&G ERP',
  ].filter(Boolean);
  return `<!doctype html>
<html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>${title}</title>
<style>
  @page{size:A4;margin:0}
  body{margin:0;background:#e8eef6;color:#111;font-family:Arial,Helvetica,sans-serif}
  .page{position:relative;width:210mm;height:297mm;margin:12px auto;background:#fff;padding:${pagePaddingTop}mm 7mm 0;box-sizing:border-box;border:1px solid #d8e2ee;overflow:hidden}
  .top{display:grid;grid-template-columns:116mm 72mm;gap:6mm;align-items:start}
  .brand{display:grid;grid-template-columns:34mm 1fr;gap:6mm;min-height:50mm;align-items:start}
  .logo{height:31mm;width:34mm;display:grid;place-items:center;overflow:hidden;background:#fff;padding-top:${logoTop}mm}
  .logo img{max-width:${logoWidth}mm;max-height:${logoHeight}mm;object-fit:contain}
  .brand h1{margin:4mm 0 8mm;font-size:18px;line-height:0.95;font-weight:900;text-transform:uppercase;max-width:86mm}
  .brand p{margin:0 0 5mm;font-size:8px;line-height:1.25;text-transform:uppercase;font-weight:700}
  .docbox{border:1.35px solid #111;text-align:center;min-height:39mm;padding:7mm 3mm 2mm;box-sizing:border-box}
  .docbox p{margin:0 0 8mm;font-size:12px}.docbox h2{font-size:18px;line-height:1.08;margin:0 0 8mm;font-weight:900}
  .client{display:grid;grid-template-columns:31mm 88mm 43mm 25mm;gap:0 2mm;margin:0 0 3.5mm;font-size:8px;line-height:1.55;text-transform:uppercase}
  .client b{font-size:8px}.client span{min-height:4.5mm;overflow-wrap:anywhere}
  table{width:100%;height:${tableHeight}mm;border-collapse:collapse;border:1px solid #111;font-size:${tableFont}px;table-layout:fixed}
  th{background:#111827;color:#fff;font-weight:900;text-align:center}
  th{border:1px solid #111;padding:1px 1.8px;vertical-align:top;word-wrap:break-word;overflow-wrap:anywhere;page-break-inside:avoid}
  td{border-left:1px solid #111;border-right:1px solid #111;border-top:0;border-bottom:0;padding:1px 1.8px;vertical-align:top;word-wrap:break-word;overflow-wrap:anywhere;page-break-inside:avoid}
  th:nth-child(1){width:8mm}th:nth-child(2){width:20mm}th:nth-child(4){width:16mm}th:nth-child(5){width:21mm}th:nth-child(6){width:21mm}
  td:nth-child(1),td:nth-child(2){text-align:center}
  td:nth-child(4),td:nth-child(5),td:nth-child(6){text-align:right;white-space:nowrap}
  td:nth-child(3){font-weight:700;line-height:1.08;white-space:normal;font-size:${descFont}px}
  td:nth-child(3) b{white-space:pre-line}
  tr.item-row{height:${rowHeight}mm;min-height:${rowHeight}mm}
  tr.filler-row{height:auto}
  .series-print{display:block;color:#111;margin-top:3px;padding-top:2px;font-size:6.5px;font-weight:500;white-space:pre-line;line-height:1.6;letter-spacing:0.02em}
  small{display:block;color:#111;margin-top:1px;font-size:7px;font-weight:400;white-space:pre-line;line-height:1.15}
  .amount{border-top:0;text-align:center;font-size:6px;padding:2mm 1mm;text-transform:uppercase}
  .totals{position:absolute;right:7mm;top:153mm;width:70mm;font-size:8.8px}
  .totals div{display:grid;grid-template-columns:1fr 14mm 28mm;border:1px solid #111;border-bottom:0;padding:2.1mm 3mm}
  .totals div:last-child{border-bottom:1px solid #111;font-weight:900;font-size:9.5px}
  .meta{position:absolute;left:7mm;top:176mm;font-size:7px;line-height:1.65;text-transform:uppercase}
  .meta b{display:inline-block;width:52mm}
  .banks{position:absolute;left:65mm;top:190mm;font-size:6.2px;line-height:1.6}
  .legal{position:absolute;left:5.5mm;right:36mm;top:215mm;font-size:5.8px;line-height:1.8;color:#111}
  .qr{position:absolute;right:5.5mm;top:190mm;width:20mm;height:20mm;border:1px solid #111;display:grid;place-items:center;font-size:6px;color:#64748b}
  .foot{position:absolute;left:40mm;right:40mm;top:244mm;text-align:center;font-size:6.2px;color:#111;line-height:1.45}
  .erp-footer{display:none}
  @media(max-width:760px){.page{width:100%;min-height:100vh;margin:0;border:0;padding:14px}.top{grid-template-columns:1fr}.brand{grid-template-columns:1fr}.client{grid-template-columns:1fr 1fr}.docbox{padding:12px}.legal,.qr,.foot{position:static;margin-top:12px}.qr{margin-left:auto}}
  @media print{body{background:#fff}.page{margin:0;border:0}}
</style></head><body>
<main class="page">
  <section class="top"><div class="brand"><div class="logo"><img src="${escapeHtml(logo)}" alt="Logo"></div><div><h1>${escapeHtml(companyName)}</h1><p>${textWithBreaks(address)}</p>${slogan ? `<p><b>${escapeHtml(slogan)}</b></p>` : ''}</div></div><div class="docbox"><p>RUC ${escapeHtml(ruc)}</p><h2>${docTitle}</h2><p>${escapeHtml(docNumber(doc))}</p></div></section>
  <section class="client"><b>${escapeHtml(labels.documento || 'DOCUMENTO')}</b><span>${escapeHtml(doc.numero_documento_cliente || doc.documento_cliente || '')}</span><b>${escapeHtml(labels.fecha_emision || 'FECHA EMISION')}</b><span>${escapeHtml(issueDate)}</span><b>${escapeHtml(labels.cliente || 'CLIENTE')}</b><span>${escapeHtml(doc.cliente_nombre || 'USUARIO X')}</span><b>${escapeHtml(labels.fecha_vencimiento || 'FECHA VENCIMIENTO')}</b><span>${escapeHtml(dueDate)}</span><b>${escapeHtml(labels.direccion || labels.direccion_cliente || 'DIRECCION')}</b><span>${escapeHtml(doc.direccion_cliente || 'SIN DIRECCION')}</span><b>${escapeHtml(labels.moneda || 'MONEDA')}</b><span>SOLES</span></section>
  <table><thead><tr><th>Nro</th><th>UNIDAD</th><th>DESCRIPCION</th><th>CANT.</th><th>TOTAL</th><th>P.UNIT.</th></tr></thead><tbody>${rows || '<tr class="item-row"><td colspan="6">Sin detalle disponible.</td></tr>'}${fillerRow}</tbody></table>
  <div class="amount">${escapeHtml(amountToWordsSoles(grandTotal))}</div>
  <section class="totals"><div><span>${escapeHtml(labels.gravado || 'GRAVADO')}</span><b>S/</b><b>${subtotal.toFixed(2)}</b></div><div><span>${escapeHtml(labels.igv || 'I.G.V. 18%')}</span><b>S/</b><b>${igv.toFixed(2)}</b></div><div><span>${escapeHtml(labels.total || 'TOTAL')}</span><b>S/</b><b>${grandTotal.toFixed(2)}</b></div></section>
  <div class="meta"><b>${escapeHtml(labels.usuario || 'USUARIO')}</b>: ${escapeHtml(doc.usuario_emisor || '')} - ${escapeHtml(issueDate)}<br><b>${escapeHtml(labels.condicion_pago || 'CONDICION DE PAGO')}</b>: ${escapeHtml(doc.estado_pago || texts.condicion_pago_valor || 'CONTADO')}</div>
  <div class="banks"><b>${escapeHtml(labels.cuentas_bancarias || 'CUENTAS BANCARIAS')}</b><br>${bankLines.map((line) => `<span>${escapeHtml(line)}</span><br>`).join('')}</div>
  <div class="legal">${legalLines.map((line) => `<div>${escapeHtml(line)}</div>`).join('')}<div>${escapeHtml(texts.resumen || 'Resumen')}</div></div>
  <div class="qr">QR</div>
  <div class="foot">${[1, 2, 3, 4, 5, 6].map((idx) => texts[`garantia_${idx}`]).filter(Boolean).map((line) => `<div>${escapeHtml(line)}</div>`).join('')}</div>
  <div class="erp-footer"><b>G&amp;G ERP</b><span>Comprobante emitido a traves de</span><span><b style="display:inline;font-size:8px">G&amp;G ERP</b></span></div>
</main></body></html>`;
}

async function buildDocumentViewer(doc) {
  if (!doc) return null;
  const [detail, config] = await Promise.all([
    apiFetch(`/documentos/${doc.id}`),
    fetchDocumentConfig(),
  ]);
  const fullDoc = { ...doc, ...(detail?.documento || detail?.data?.documento || {}) };
  const empresa = currentEmpresa();
  return {
    title: `${fullDoc.tipo || 'Documento'} ${docNumber(fullDoc)}`,
    fileName: documentFileName(`${fullDoc.tipo || 'Documento'} ${docNumber(fullDoc)}`),
    pdfUrl: `${BASE_URL}/documentos/${fullDoc.id || doc.id}/pdf?sucursal=${encodeURIComponent(empresa)}`,
    downloadUrl: `${BASE_URL}/documentos/${fullDoc.id || doc.id}/pdf?sucursal=${encodeURIComponent(empresa)}`,
    printUrl: `${BASE_URL}/documentos/${fullDoc.id || doc.id}/pdf?sucursal=${encodeURIComponent(empresa)}&inline=true`,
    html: buildDocumentPreviewHtml(fullDoc, detailLinesFromPayload(detail), config),
  };
}

async function buildDraftPdfViewer(doc, lines, config = null) {
  const url = new URL(`${BASE_URL}/documentos/preview/pdf`);
  const empresa = currentEmpresa();
  url.searchParams.append('sucursal', empresa);
  url.searchParams.append('empresa', empresa);
  url.searchParams.append('inline', 'true');
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      tipo: doc.tipo || 'PROFORMA',
      cliente_nombre: doc.cliente_nombre || 'CLIENTE GENERAL',
      tipo_documento_cliente: doc.tipo_documento_cliente || '',
      numero_documento_cliente: doc.numero_documento_cliente || doc.documento_cliente || '',
      direccion_cliente: doc.direccion_cliente || 'SIN DIRECCION',
      usuario_emisor: doc.usuario_emisor || '',
      fecha_emision: doc.fecha_emision || localTimestampForApi(),
      fecha_vencimiento: doc.fecha_vencimiento || '',
      estado_pago: doc.estado_pago || '',
      metodo_pago: doc.metodo_pago || '',
      items: (lines || []).map((line) => ({
        id: Number(line.id || line.producto_id || 0),
        producto_id: Number(line.producto_id || line.id || 0),
        nombre: line.descripcion || line.nombre || '',
        marca: line.marca || '',
        modelo: line.modelo || '',
        cantidad: Number(line.cantidad || 1),
        precio: Number(line.precio || line.precio_unitario || 0),
        total: Number(line.total || (Number(line.cantidad || 1) * Number(line.precio || line.precio_unitario || 0))),
        serie: line.serie || '',
        series_texto: line.series_texto || line.serie || '',
      })),
    }),
  });
  const contentType = String(response.headers.get('content-type') || '').toLowerCase();
  if (!response.ok || !contentType.includes('application/pdf')) {
    const text = await response.text().catch(() => '');
    throw new Error(text || `Vista previa PDF ${response.status}`);
  }
  const blob = await response.blob();
  const pdfUrl = URL.createObjectURL(blob);
  const title = `${doc.tipo || 'Documento'} ${doc.numero || 'VISTA-PREVIA'}`;
  const htmlConfig = config || await fetchDocumentConfig();
  return {
    title,
    fileName: documentFileName(title),
    pdfUrl,
    downloadUrl: pdfUrl,
    printUrl: pdfUrl,
    html: buildDocumentPreviewHtml(doc, lines, htmlConfig),
  };
}

function useCashRegisterSound() {
  const elementRef = useRef(null);
  const audioRef = useRef(null);
  const unlockedRef = useRef(false);

  const unlock = async () => {
    try {
      if (!elementRef.current) {
        elementRef.current = new Audio(CASH_SOUND_URL);
        elementRef.current.preload = 'auto';
        elementRef.current.volume = 1;
        elementRef.current.load();
      }
      const AudioContext = window.AudioContext || window.webkitAudioContext;
      if (AudioContext) {
        if (!audioRef.current) audioRef.current = new AudioContext();
        if (audioRef.current.state === 'suspended') await audioRef.current.resume();
      }
      unlockedRef.current = true;
      return true;
    } catch {
      return false;
    }
  };

  const playFallbackTone = async () => {
    if (!audioRef.current) return;
    const ctx = audioRef.current;
    if (ctx.state === 'suspended') await ctx.resume();
    const now = ctx.currentTime;
    const hit = (start, freq, duration, gainValue) => {
      const osc = ctx.createOscillator();
      const gain = ctx.createGain();
      osc.type = 'square';
      osc.frequency.setValueAtTime(freq, start);
      gain.gain.setValueAtTime(0.0001, start);
      gain.gain.exponentialRampToValueAtTime(gainValue, start + 0.015);
      gain.gain.exponentialRampToValueAtTime(0.0001, start + duration);
      osc.connect(gain).connect(ctx.destination);
      osc.start(start);
      osc.stop(start + duration + 0.02);
    };
    hit(now, 880, 0.08, 0.08);
    hit(now + 0.09, 1320, 0.08, 0.08);
    hit(now + 0.18, 1760, 0.14, 0.06);
  };

  const play = async () => {
    const ready = unlockedRef.current || (await unlock());
    if (!ready) return;
    try {
      if (elementRef.current) {
        elementRef.current.currentTime = 0;
        await elementRef.current.play();
      } else {
        await playFallbackTone();
      }
    } catch {
      await playFallbackTone();
    }
    try {
      if (navigator.vibrate) navigator.vibrate([80, 40, 80]);
    } catch {
      // Best effort in Android WebView.
    }
  };

  const click = async () => {
    const ready = unlockedRef.current || (await unlock());
    if (!ready || !audioRef.current) return;
    try {
      const ctx = audioRef.current;
      if (ctx.state === 'suspended') await ctx.resume();
      const start = ctx.currentTime;
      const osc = ctx.createOscillator();
      const gain = ctx.createGain();
      osc.type = 'triangle';
      osc.frequency.setValueAtTime(720, start);
      osc.frequency.exponentialRampToValueAtTime(960, start + 0.05);
      gain.gain.setValueAtTime(0.0001, start);
      gain.gain.exponentialRampToValueAtTime(0.045, start + 0.01);
      gain.gain.exponentialRampToValueAtTime(0.0001, start + 0.075);
      osc.connect(gain).connect(ctx.destination);
      osc.start(start);
      osc.stop(start + 0.09);
    } catch {
      // Navegacion sin sonido si Android bloquea audio en ese instante.
    }
  };

  const radioTone = async (type = 'start') => {
    const ready = unlockedRef.current || (await unlock());
    if (!ready || !audioRef.current) return;
    if (Capacitor.getPlatform?.() === 'android') {
      try {
        if (type === 'start') await AndroidRadio.beepStart();
        else await AndroidRadio.beepEnd();
        return;
      } catch {
        // Continue with WebAudio fallback.
      }
    }
    try {
      const ctx = audioRef.current;
      if (ctx.state === 'suspended') await ctx.resume();
      const start = ctx.currentTime;
      const osc = ctx.createOscillator();
      const gain = ctx.createGain();
      osc.type = 'square';
      osc.frequency.setValueAtTime(type === 'start' ? 520 : 760, start);
      osc.frequency.exponentialRampToValueAtTime(type === 'start' ? 760 : 420, start + 0.12);
      gain.gain.setValueAtTime(0.0001, start);
      gain.gain.exponentialRampToValueAtTime(0.08, start + 0.015);
      gain.gain.exponentialRampToValueAtTime(0.0001, start + 0.16);
      osc.connect(gain).connect(ctx.destination);
      osc.start(start);
      osc.stop(start + 0.18);
    } catch {
      // Best effort.
    }
  };

  return { unlock, play, click, radioStart: () => radioTone('start'), radioStop: () => radioTone('stop') };
}

let radioPlayChain = Promise.resolve();

async function playIncomingRadioAudio(msg) {
  const src = `data:${msg.audio_mime || 'audio/webm'};base64,${msg.audio_base64 || ''}`;
  if (!msg.audio_base64) return false;
  radioPlayChain = radioPlayChain.then(async () => {
    const audio = new Audio(src);
    audio.preload = 'auto';
    audio.volume = 1;
    await new Promise((resolve, reject) => {
      audio.onended = resolve;
      audio.onerror = reject;
      audio.play().catch(reject);
    });
  }).catch(() => {});
  await radioPlayChain;
  return true;
}

function useGlobalRadioAutoPlay({ userName, radioEnabled }) {
  const lastIdRef = useRef(0);
  const playedIdsRef = useRef(new Set());

  useEffect(() => {
    if (IS_NATIVE_APP) return undefined;
    if (!radioEnabled || !userName) return undefined;
    let active = true;
    const poll = async () => {
      try {
        const res = await apiFetch('/boquitoqui/live', {
          params: { usuario: userName, since_id: lastIdRef.current, limit: 20 },
        });
        if (!active) return;
        const rows = asArray(res);
        if (!rows.length) return;
        const newest = rows[rows.length - 1];
        lastIdRef.current = Math.max(lastIdRef.current, Number(newest.id || 0));
        const incoming = rows.filter((row) => {
          const id = String(row.id || '');
          const sender = String(row.usuario_emisor || '').toLowerCase();
          return id && sender !== userName.toLowerCase() && !playedIdsRef.current.has(id);
        });
        for (const row of incoming) {
          playedIdsRef.current.add(String(row.id || ''));
          try {
            await playIncomingRadioAudio(row);
          } catch {
            // If the browser blocks autoplay, Android background service still handles it.
          }
        }
      } catch {
        // Retry on next tick.
      }
    };
    poll();
    const timer = window.setInterval(poll, 330);
    return () => {
      active = false;
      window.clearInterval(timer);
    };
  }, [radioEnabled, userName]);
}

function useAndroidAutoUpdate() {
  const checkingRef = useRef(false);
  const [updateInfo, setUpdateInfo] = useState(null);

  const checkForUpdate = async ({ manual = false } = {}) => {
    if (checkingRef.current) return null;
    checkingRef.current = true;
    try {
      const info = await apiFetch('/app/version', { params: { platform: 'android', current_version: APP_VERSION } });
      const remoteVersion = info.android_version || info.version || '';
      const isDexUpdate = typeof window !== 'undefined' && window.innerWidth >= 900;
      const remoteUrl = isDexUpdate ? (info.android_dex_url || info.android_url || '') : (info.android_url || '');
      const hasUpdate = info.force_update || isNewerVersion(remoteVersion, APP_VERSION);
      if (!hasUpdate) {
        setUpdateInfo(null);
        if (manual) alert(`G&G ERP Android esta actualizado. Version ${APP_VERSION}.`);
        return null;
      }

      const next = {
        version: remoteVersion,
        url: remoteUrl,
        name: (isDexUpdate ? (info.android_dex_name || info.android_name) : info.android_name) || 'G&G ERP Android.apk',
        notes: info.android_notes || info.notes || '',
        force: !!info.force_update,
      };
      setUpdateInfo(next);
      const dismissed = localStorage.getItem('gg_erp_android_update_dismissed');
      if (manual || next.force || dismissed !== String(next.version)) {
        const message = `Hay una actualización Android ${next.version} disponible.\n\n${next.notes || next.name}`;
        if (next.url) {
          if (confirm(`${message}\n\n?Deseas abrir la descarga ahora?`)) {
            window.open(next.url, '_blank');
          } else if (!next.force) {
            localStorage.setItem('gg_erp_android_update_dismissed', String(next.version));
          }
        } else {
          alert(`${message}\n\nAun falta publicar el APK en el servidor.`);
        }
      }
      return next;
    } catch (error) {
      if (manual) alert(`No se pudo verificar actualización.\n${error?.message || ''}`.trim());
      return null;
    } finally {
      checkingRef.current = false;
    }
  };

  useEffect(() => {
    checkForUpdate();
    const timer = window.setInterval(() => checkForUpdate(), 60 * 60 * 1000);
    return () => window.clearInterval(timer);
  }, []);

  return { updateInfo, checkForUpdate };
}

function Field({ label, children }) {
  return (
    <label className="grid gap-1 text-xs font-bold text-slate-500">
      {label}
      {children}
    </label>
  );
}

const TextInput = forwardRef(function TextInput(props, ref) {
  return (
    <input
      {...props}
      ref={ref}
      className={`h-11 w-full rounded-md border border-slate-200 bg-white px-3 text-sm text-slate-900 outline-none focus:border-teal-600 ${props.className || ''}`}
    />
  );
});

const TextArea = forwardRef(function TextArea(props, ref) {
  return (
    <textarea
      {...props}
      ref={ref}
      className={`min-h-24 w-full rounded-md border border-slate-200 bg-white px-3 py-2 text-sm text-slate-900 outline-none focus:border-teal-600 ${props.className || ''}`}
    />
  );
});

function SelectInput(props) {
  return (
    <select
      {...props}
      className={`h-11 w-full rounded-md border border-slate-200 bg-white px-3 text-sm text-slate-900 outline-none focus:border-teal-600 ${props.className || ''}`}
    />
  );
}

function ActionButton({ tone = 'primary', className = '', ...props }) {
  const tones = {
    primary: 'bg-teal-700 text-white active:bg-teal-800',
    neutral: 'bg-slate-100 text-slate-800 active:bg-slate-200',
    danger: 'bg-red-600 text-white active:bg-red-700',
    blue: 'bg-blue-600 text-white active:bg-blue-700',
    amber: 'bg-amber-500 text-white active:bg-amber-600',
    violet: 'bg-violet-700 text-white active:bg-violet-800',
  };
  return (
    <button
      {...props}
      className={`min-h-11 rounded-md px-4 py-2 text-sm font-black shadow-sm disabled:opacity-50 ${tones[tone]} ${className}`}
    />
  );
}

function Card({ title, actions, children, className = '' }) {
  return (
    <section className={`rounded-lg border border-slate-200 bg-white shadow-sm ${className}`}>
      {(title || actions) && (
        <div className="flex flex-wrap items-center justify-between gap-2 border-b border-slate-200 p-3">
          {title && <h2 className="text-lg font-black text-slate-950">{title}</h2>}
          {actions && <div className="flex flex-wrap gap-2">{actions}</div>}
        </div>
      )}
      <div className="p-3">{children}</div>
    </section>
  );
}

function Modal({ title, children, onClose }) {
  useEffect(() => {
    const handleKeyDown = (event) => {
      if (event.key === 'Escape') onClose?.();
    };
    window.addEventListener('keydown', handleKeyDown);
    return () => window.removeEventListener('keydown', handleKeyDown);
  }, [onClose]);
  return (
    <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/55 p-3" onMouseDown={(event) => event.target === event.currentTarget && onClose?.()}>
      <section className="max-h-[92vh] w-full max-w-3xl overflow-hidden rounded-lg bg-white shadow-2xl" onMouseDown={(event) => event.stopPropagation()}>
        <div className="flex items-center justify-between gap-3 border-b border-slate-200 p-4">
          <h2 className="min-w-0 truncate text-xl font-black text-slate-950">{title}</h2>
          <button onClick={onClose} className="rounded-md bg-slate-100 px-4 py-2 text-sm font-black text-slate-700">Cerrar</button>
        </div>
        <div className="max-h-[78vh] overflow-auto p-4">{children}</div>
      </section>
    </div>
  );
}

function DocumentViewerModal({ viewer, onClose }) {
  const [busyAction, setBusyAction] = useState('');
  if (!viewer) return null;
  const download = async () => {
    let nativeError = null;
    let pdfError = null;
    try {
      setBusyAction('download');
      try {
        const serverHandled = await serverPdfAction('download', viewer);
        if (serverHandled) return;
      } catch (error) {
        pdfError = error;
      }
      try {
        const handled = await nativePdfAction('download', viewer);
        if (handled) return;
      } catch (error) {
        nativeError = error;
      }
      try {
        await fallbackDownloadPdf(viewer);
        return;
      } catch (error) {
        pdfError = error;
      }
      fallbackDownloadPrintableHtml(viewer);
      alert(`No se pudo crear el PDF en este equipo, pero se descargo el documento imprimible como respaldo.\n\nPDF: ${pdfError?.message || nativeError?.message || 'bloqueado'}`);
    } finally {
      setBusyAction('');
    }
  };
  const share = async () => {
    try {
      setBusyAction('share');
      const serverHandled = await serverPdfAction('share', viewer);
      if (serverHandled) return;
      const handled = await nativePdfAction('share', viewer);
      if (!handled && navigator.share) await navigator.share({ title: viewer.title, text: viewer.title });
      else if (!handled) alert('Compartir archivo PDF solo esta disponible dentro del APK Android.');
    } catch (error) {
      if (!String(error?.message || '').toLowerCase().includes('cancel')) alert(error?.message || 'No se pudo compartir.');
    } finally {
      setBusyAction('');
    }
  };
  const printDoc = async () => {
    try {
      setBusyAction('print');
      const currentFrame = document.querySelector('#gg-doc-viewer');
      if (viewer?.pdfUrl && currentFrame?.contentWindow?.print) {
        currentFrame.contentWindow.focus?.();
        currentFrame.contentWindow.print();
        return;
      }
      const opened = await printServerPdf(viewer);
      if (!opened) currentFrame?.contentWindow?.print?.();
    } catch {
      if (viewer?.pdfUrl) {
        alert('No se pudo abrir el PDF real para imprimir. Prueba Descargar y luego imprime el archivo.');
      } else {
        document.querySelector('#gg-doc-viewer')?.contentWindow?.print?.();
      }
    } finally {
      setBusyAction('');
    }
  };
  return (
    <div className="fixed inset-0 z-50 grid bg-slate-950/60 p-0 sm:p-2 md:p-5">
      <section className="grid h-full min-h-0 grid-rows-[auto_1fr_auto] overflow-hidden rounded-none bg-white shadow-2xl sm:rounded-lg">
        <header className="flex min-w-0 items-center justify-between gap-2 border-b border-slate-200 px-3 py-2" style={{ paddingTop: 'max(0.5rem, env(safe-area-inset-top))' }}>
          <div className="min-w-0">
            <h2 className="truncate text-base font-black text-slate-950">{viewer.title}</h2>
            <p className="text-xs font-bold text-slate-500">Vista interna del documento</p>
          </div>
          <button type="button" className="shrink-0 rounded-md bg-slate-100 px-3 py-2 text-xs font-black text-slate-700" onClick={onClose}>Cerrar</button>
        </header>
        {viewer.pdfUrl && !IS_NATIVE_APP ? (
          <iframe id="gg-doc-viewer" title={viewer.title} src={viewer.printUrl || viewer.pdfUrl} className="h-full min-h-0 w-full border-0 bg-slate-100" />
        ) : (
          <iframe id="gg-doc-viewer" title={viewer.title} srcDoc={viewer.html || '<p style="font-family:Arial;padding:20px">Cargando documento...</p>'} className="h-full min-h-0 w-full border-0 bg-slate-100" />
        )}
        <footer className="grid grid-cols-2 gap-2 border-t border-slate-200 bg-white p-2 sm:flex sm:justify-end" style={{ paddingBottom: 'max(0.5rem, env(safe-area-inset-bottom))' }}>
          <button type="button" disabled={!!busyAction} className="min-h-11 rounded-md bg-blue-600 px-3 py-2 text-sm font-black text-white disabled:opacity-60" onClick={printDoc}>{busyAction === 'print' ? 'Abriendo...' : 'Imprimir'}</button>
          <button type="button" disabled={!!busyAction} className="min-h-11 rounded-md bg-emerald-600 px-3 py-2 text-sm font-black text-white disabled:opacity-60" onClick={share}>{busyAction === 'share' ? 'Preparando...' : 'Compartir'}</button>
          <button type="button" disabled={!!busyAction} className="min-h-11 rounded-md bg-indigo-600 px-3 py-2 text-sm font-black text-white disabled:opacity-60" onClick={download}>{busyAction === 'download' ? 'Guardando...' : 'Descargar'}</button>
          <button type="button" className="min-h-11 rounded-md bg-slate-100 px-3 py-2 text-sm font-black text-slate-700" onClick={onClose}>Cerrar</button>
        </footer>
      </section>
    </div>
  );
}

function Stat({ label, value, tone = 'slate' }) {
  const colors = {
    slate: 'border-slate-200 bg-white text-slate-950',
    green: 'border-emerald-200 bg-emerald-50 text-emerald-800',
    amber: 'border-amber-200 bg-amber-50 text-amber-800',
    red: 'border-red-200 bg-red-50 text-red-800',
    blue: 'border-blue-200 bg-blue-50 text-blue-800',
    violet: 'border-violet-200 bg-violet-50 text-violet-800',
  };
  return (
    <div className={`rounded-lg border p-3 ${colors[tone]}`}>
      <div className="text-xs font-bold opacity-75">{label}</div>
      <div className="mt-1 text-xl font-black">{value}</div>
    </div>
  );
}

function Empty({ text = 'Sin datos.' }) {
  return <div className="rounded-md border border-dashed border-slate-200 p-5 text-center text-sm font-semibold text-slate-500">{text}</div>;
}

function SeriesQuickSearch({ compact = false }) {
  const [query, setQuery] = useState('');
  const [rows, setRows] = useState([]);
  const [loading, setLoading] = useState(false);
  const search = async () => {
    const q = query.trim();
    if (!q) {
      setRows([]);
      return;
    }
    setLoading(true);
    try {
      const all = asArray(await apiFetch('/series', { params: { q } }));
      setRows(all.slice(0, compact ? 5 : 15));
    } finally {
      setLoading(false);
    }
  };
  return (
    <div className="grid gap-3">
      <div className="grid grid-cols-[1fr_auto] gap-2">
        <TextInput placeholder="Pistolea o busca serie..." value={query} onChange={(e) => setQuery(e.target.value)} onKeyDown={(e) => e.key === 'Enter' && search()} />
        <ActionButton tone="blue" onClick={search} disabled={loading}>{loading ? 'Buscando...' : 'Buscar'}</ActionButton>
      </div>
      <div className={`${compact ? 'max-h-48' : 'max-h-64'} overflow-auto rounded-lg border border-slate-200 bg-white`}>
        {rows.map((s) => (
          <div key={s.id || s.serie} className="grid gap-0.5 border-b border-slate-100 p-2.5 text-xs hover:bg-slate-50">
            <div className="flex items-center justify-between">
              <b className="font-mono text-sm text-slate-950">{s.serie}</b>
              <span className={`rounded px-1.5 py-0.5 text-[10px] font-black ${String(s.estado || '').toUpperCase() === 'DISPONIBLE' ? 'bg-green-100 text-green-700' : String(s.estado || '').toUpperCase() === 'VENDIDO' ? 'bg-red-100 text-red-700' : 'bg-amber-100 text-amber-700'}`}>
                {s.estado || 'DISPONIBLE'}
              </span>
            </div>
            <div className="font-semibold text-slate-700 truncate">{s.producto_nombre || s.nombre || 'Sin nombre'}</div>
            <div className="text-[10px] text-slate-500 flex justify-between">
              <span>{s.almacen || 'TIENDA'}</span>
              <span>Ingreso: {String(s.fecha_ingreso || '').slice(0, 10) || '-'}</span>
            </div>
          </div>
        ))}
        {!rows.length && <div className="p-3 text-xs text-slate-500">Busca o pistolea una serie para verla aquí rápido (stock, estado, fechas). Tanto disponibles como vendidas se muestran.</div>}
      </div>
      <div className="text-[10px] text-slate-500">Usa este buscador para control rápido de series sin salir del Panel.</div>
    </div>
  );
}

function Login({ onLogin, sound, deviceMode }) {
  const [empresa, setEmpresa] = useState(() => localStorage.getItem('gg_erp_empresa') || EMPRESA);
  const [branches, setBranches] = useState(BRANCHES);
  const [usuario, setUsuario] = useState(() => localStorage.getItem('gg_erp_usuario') || '');
  const [clave, setClave] = useState(() => localStorage.getItem('gg_erp_clave') || '');
  const [remember, setRemember] = useState(() => localStorage.getItem('gg_erp_recordar') !== '0');
  const [showPass, setShowPass] = useState(false);
  const [profile, setProfile] = useState(null);
  const [loading, setLoading] = useState(false);
  const [loginMsg, setLoginMsg] = useState('');

  useEffect(() => {
    let active = true;
    const user = usuario.trim();
    if (!user) {
      setProfile(null);
      return undefined;
    }
    const timer = window.setTimeout(async () => {
      try {
        const data = await apiFetch('/usuarios/perfil', { params: { usuario: user } });
        if (active && (data.ok || data.found || data.usuario)) setProfile(data);
      } catch {
        if (active) setProfile(null);
      }
    }, 250);
    return () => {
      active = false;
      window.clearTimeout(timer);
    };
  }, [usuario]);

  const initials = useMemo(() => {
    const name = String(profile?.usuario || usuario || '').trim();
    if (!name) return '';
    return name.split(/\s+/).slice(0, 2).map((part) => part[0]?.toUpperCase()).join('') || name.slice(0, 2).toUpperCase();
  }, [profile, usuario]);
  const profilePhoto = safeImageSrc(profile?.foto_url);
  const profileBackground = cssImageUrl(profile?.fondo_url);

  useEffect(() => {
    apiFetch('/sucursales').then((rows) => {
      const next = asArray(rows).map((b) => ({ value: b.codigo || b.value, label: b.nombre || b.label || b.codigo })).filter((b) => b.value);
      if (next.length) {
        setBranches(next);
        if (!next.some((b) => b.value === empresa)) {
          setEmpresa(next[0].value);
          localStorage.setItem('gg_erp_empresa', next[0].value);
        }
      }
    }).catch(() => {});
  }, []);

  // Auto-login cancel support
  const autoAbortRef = useRef(null);

  const submit = async (silent = false) => {
    setLoginMsg('');
    if (!usuario || !clave) {
      if (silent) return;
      alert('Ingresa usuario y clave.');
      return;
    }
    setLoading(true);
    await sound.unlock();

    // Prepare abort for this attempt (used by cancel button)
    const controller = new AbortController();
    if (silent) autoAbortRef.current = controller;

    try {
      const res = await apiFetch('/login', {
        method: 'POST',
        json: { usuario, clave, sucursal: empresa, empresa },
        signal: controller.signal,
        timeoutMs: silent ? 12000 : 20000, // shorter for auto
      });

      if (res.ok) {
        if (remember) {
          localStorage.setItem('gg_erp_usuario', usuario);
          localStorage.setItem('gg_erp_empresa', empresa);
          localStorage.setItem('gg_erp_clave', clave);
          localStorage.setItem('gg_erp_recordar', '1');
        } else {
          localStorage.setItem('gg_erp_usuario', usuario);
          localStorage.setItem('gg_erp_empresa', empresa);
          localStorage.removeItem('gg_erp_clave');
          localStorage.setItem('gg_erp_recordar', '0');
        }
        onLogin({
          id: res.id,
          usuario: res.usuario || usuario,
          rol: res.rol || '',
          foto_url: res.foto_url || '',
          fondo_url: res.fondo_url || '',
          boquitoqui_enabled: !!res.boquitoqui_enabled,
          color_tema: res.color_tema || THEME_COLORS[0],
          sucursal: res.sucursal || empresa,
          permisos: res.permisos || {},
        });
        return;
      }

      const msg = res.msg || 'No se pudo iniciar sesion.';
      if (silent) {
        setLoginMsg(`${msg} Puedes ingresar manualmente.`);
      } else {
        alert(msg);
      }
    } catch (error) {
      if (error?.name === 'AbortError') {
        if (silent) setLoginMsg('Auto-login cancelado.');
        return;
      }
      const msg = `No hay conexion con el servidor.\n${error?.message || ''}`.trim();
      if (!silent) alert(msg);
      else setLoginMsg('No hay conexion con el servidor. Puedes ingresar manualmente.');
    } finally {
      if (silent && autoAbortRef.current) {
        // allow new attempts
      }
      setLoading(false);
    }
  };

  const cancelAutoLogin = () => {
    if (autoAbortRef.current) {
      try { autoAbortRef.current.abort(); } catch {}
      autoAbortRef.current = null;
    }
    setLoading(false);
    setLoginMsg('Auto-login cancelado. Ingresa tus credenciales.');
    sessionStorage.setItem('gg_erp_skip_auto_login', '1');
  };

  useEffect(() => {
    if (sessionStorage.getItem('gg_erp_skip_auto_login') === '1') {
      sessionStorage.removeItem('gg_erp_skip_auto_login');
      return;
    }
    const savedUser = localStorage.getItem('gg_erp_usuario') || '';
    const savedPass = localStorage.getItem('gg_erp_clave') || '';
    const shouldRemember = localStorage.getItem('gg_erp_recordar') !== '0';
    if (shouldRemember && savedUser && savedPass) {
      window.setTimeout(() => submit(true), 300);
    }
  }, []);

  return (
    <div
      className="min-h-screen bg-[#f5f7fb] bg-cover bg-right-bottom bg-no-repeat p-5 text-slate-950"
      style={profileBackground ? { backgroundImage: `linear-gradient(90deg, rgba(245,247,251,.98) 0%, rgba(245,247,251,.94) 42%, rgba(245,247,251,.45) 100%), ${profileBackground}` } : undefined}
    >
      <div className="mx-auto flex min-h-[calc(100vh-40px)] max-w-md items-center justify-center">
        <div className="w-full rounded-md border border-slate-200 bg-white px-7 py-8 shadow-sm">
          <div className="text-center">
            <h1 className="text-[19px] font-black text-slate-950">G&amp;G ERP</h1>
            <div className="mx-auto mt-3 flex h-[74px] w-[74px] items-center justify-center overflow-hidden rounded-md bg-white text-[42px] font-black text-[#0891b2]">
              {profilePhoto ? (
                <img src={profilePhoto} alt="" className="h-full w-full object-cover" />
              ) : initials ? (
                initials
              ) : (
                <img src="/app-logo.png" alt="" className="h-full w-full object-contain p-1" />
              )}
            </div>
            <div className="mt-2 text-base font-black uppercase text-[#0891b2]">{profile?.usuario || usuario || 'INICIAR SESION'}</div>
            <div className="mt-1 text-[11px] font-semibold text-slate-500">Versión {APP_VERSION} / {IS_PC_TESTER ? 'PC TESTER' : (deviceMode === 'dex' ? 'DeX' : 'Móvil')}</div>
          </div>

          <div className="mt-5 grid gap-3">
            <Field label="Empresa">
              <SelectInput value={empresa} onChange={(e) => setEmpresa(e.target.value)}>
                {branches.map((branch) => <option key={branch.value} value={branch.value}>{branch.label}</option>)}
              </SelectInput>
            </Field>
            <Field label="Usuario">
              <TextInput value={usuario} onChange={(e) => setUsuario(e.target.value)} autoCapitalize="none" />
            </Field>
            <Field label="Contraseña">
              <TextInput type={showPass ? 'text' : 'password'} value={clave} onChange={(e) => setClave(e.target.value)} onKeyDown={(e) => e.key === 'Enter' && submit()} />
            </Field>
            <label className="flex items-center justify-center gap-2 text-sm font-semibold text-slate-700">
              <input type="checkbox" checked={showPass} onChange={(e) => setShowPass(e.target.checked)} />
              Mostrar contraseña
            </label>
            <label className="flex items-center justify-center gap-2 text-sm font-semibold text-slate-700">
              <input type="checkbox" checked={remember} onChange={(e) => setRemember(e.target.checked)} />
              Recordar usuario y clave
            </label>
            <button
              disabled={loading}
              onClick={() => submit()}
              className="mx-auto mt-2 min-h-11 w-52 rounded-md bg-[#0891b2] px-4 py-2 text-sm font-black text-white shadow-sm active:bg-[#06748d] disabled:opacity-60"
            >
              {loading ? 'Ingresando...' : 'Ingresar'}
            </button>
            {loginMsg && <div className="rounded-md bg-amber-50 p-3 text-center text-xs font-bold text-amber-800">{loginMsg}</div>}
            {loading && <button type="button" onClick={cancelAutoLogin} className="mx-auto text-xs font-bold text-slate-500 underline">Cancelar ingreso automatico</button>}
          </div>
        </div>
      </div>
    </div>
  );
}

function AppShell({ user, view, setView, onLogout, sound, soundEnabled, setSoundEnabled, deviceMode, updateInfo, onCheckUpdate, radioEnabled, onUserUpdate, children }) {
  const isDex = deviceMode === 'dex';
  const userName = user && typeof user === 'object' ? (user.usuario || '') : String(user || '');
  const userRole = String(user && typeof user === 'object' ? user.rol || '' : '').toUpperCase();
  const userPermissions = user && typeof user === 'object' && user.permisos && typeof user.permisos === 'object' ? user.permisos : {};
  const isMaster = userName.trim().toLowerCase() === 'giomar' || userRole === 'ADMIN';
  const visibleModules = modules.filter((m) => (m.id !== 'radio' || radioEnabled) && (m.id === 'sunat' || isMaster || userPermissions[m.id] !== false));
  const active = visibleModules.find((m) => m.id === view) || visibleModules[0] || modules[0];
  useEffect(() => {
    if (visibleModules.length && !visibleModules.some((m) => m.id === view)) setView(visibleModules[0].id);
  }, [view, visibleModules.map((m) => m.id).join('|')]);
  const [themeColor, setThemeColor] = useState(() => normalizeThemeColor((user && typeof user === 'object' && user.color_tema) || localStorage.getItem(`gg_erp_theme_${userName}`)));
  const [backgroundUrl, setBackgroundUrl] = useState(() => safeImageSrc(user && typeof user === 'object' ? user.fondo_url : ''));
  const [paletteOpen, setPaletteOpen] = useState(false);
  useEffect(() => {
    if (!userName) return;
    const next = normalizeThemeColor((user && typeof user === 'object' && user.color_tema) || localStorage.getItem(`gg_erp_theme_${userName}`));
    setThemeColor(next);
  }, [userName, user && typeof user === 'object' ? user.color_tema : '']);
  useEffect(() => {
    setBackgroundUrl(safeImageSrc(user && typeof user === 'object' ? user.fondo_url : ''));
  }, [userName, user && typeof user === 'object' ? user.fondo_url : '']);
  const saveThemeColor = async (color) => {
    const clean = normalizeThemeColor(color);
    setThemeColor(clean);
    setPaletteOpen(false);
    if (userName) localStorage.setItem(`gg_erp_theme_${userName}`, clean);
    onUserUpdate?.({ color_tema: clean });
    apiFetch('/usuarios/color', { method: 'PUT', json: { usuario: userName, color_tema: clean } }).catch(() => {});
  };
  const saveBackground = async (nextUrl) => {
    const clean = safeImageSrc(nextUrl);
    setBackgroundUrl(clean);
    onUserUpdate?.({ fondo_url: clean });
    const res = await apiFetch('/usuarios/fondo', { method: 'PUT', json: { usuario: userName, fondo_url: clean } });
    if (!okResponse(res)) alert(res.msg || 'No se pudo guardar el fondo.');
  };
  const pickBackground = async (event) => {
    const file = event.target.files?.[0];
    event.target.value = '';
    if (!file) return;
    try {
      await saveBackground(await fileToDataUrl(file, 4));
    } catch (error) {
      alert(error?.message || 'No se pudo cargar la imagen.');
    }
  };
  const openView = async (nextView) => {
    if (soundEnabled) await sound?.click?.();
    setView(nextView);
  };
  const shellBackground = cssImageUrl(backgroundUrl);
  const shellBackgroundStyle = shellBackground
    ? {
        backgroundImage: `linear-gradient(rgba(245,247,251,.72), rgba(245,247,251,.72)), ${shellBackground}`,
        backgroundPosition: 'center center',
        backgroundSize: 'cover',
        backgroundAttachment: 'fixed',
      }
    : {};
  return (
    <div
      className="min-h-screen w-full bg-[#f5f7fb] bg-no-repeat text-slate-950"
      style={{
        '--gg-theme': themeColor,
        ...shellBackgroundStyle,
      }}
    >
      <header className="sticky top-0 z-10 border-b bg-white px-4 py-3 shadow-sm" style={{ borderColor: `${themeColor}33` }}>
        <div className="mx-auto flex max-w-7xl items-center gap-3">
          <button className="flex h-10 w-10 items-center justify-center overflow-hidden rounded-md bg-white ring-1 ring-slate-200" onClick={() => openView('dashboard')}>
            <img src="/app-logo.png" alt="" className="h-full w-full object-contain p-1" />
          </button>
          <div className="min-w-0 flex-1">
            <div className="text-base font-black">G&amp;G ERP / {active.label}</div>
            <div className="truncate text-xs text-slate-500">{userName} / {currentEmpresa()} / {IS_PC_TESTER ? 'PC TESTER local' : (isDex ? 'tablet DeX' : 'telefono')}</div>
          </div>
          {IS_PC_TESTER && <span className="hidden rounded-md bg-violet-100 px-3 py-2 text-xs font-black text-violet-700 sm:inline-flex">PC TESTER</span>}
          <button
            onClick={async () => {
              if (!soundEnabled) await sound?.click?.();
              setSoundEnabled(!soundEnabled);
            }}
            className={`h-10 rounded-md px-3 text-xs font-black ${soundEnabled ? 'bg-emerald-100 text-emerald-700' : 'bg-slate-100 text-slate-500'}`}
          >
            Sonido {soundEnabled ? 'ON' : 'OFF'}
          </button>
          <div className="relative">
            <button onClick={() => setPaletteOpen((current) => !current)} className="h-10 rounded-md px-3 text-xs font-black text-white" style={{ backgroundColor: themeColor }}>Color</button>
            {paletteOpen && (
              <div className="absolute right-0 top-12 z-30 grid w-48 grid-cols-4 gap-2 rounded-lg border border-slate-200 bg-white p-3 shadow-xl">
                {THEME_COLORS.map((color) => (
                  <button
                    key={color}
                    type="button"
                    title={color}
                    onClick={() => saveThemeColor(color)}
                    className={`h-9 rounded-md border-2 ${themeColor === color ? 'border-slate-950' : 'border-white'}`}
                    style={{ backgroundColor: color }}
                  />
                ))}
              </div>
            )}
          </div>
          <label className="inline-flex h-10 cursor-pointer items-center rounded-md bg-cyan-100 px-3 text-xs font-black text-cyan-800">
            Fondo
            <input className="hidden" type="file" accept="image/*" onChange={pickBackground} />
          </label>
          {backgroundUrl && <button onClick={() => saveBackground('')} className="h-10 rounded-md bg-slate-100 px-3 text-xs font-black text-slate-600">Quitar fondo</button>}
          <button onClick={() => openView('sunat')} className="h-10 rounded-md bg-amber-100 px-3 text-xs font-black text-amber-800">SUNAT</button>
          <button onClick={() => onCheckUpdate?.({ manual: true })} className="h-10 rounded-md bg-blue-100 px-3 text-xs font-black text-blue-700">{IS_PC_TESTER ? 'Tester' : 'Actualizar'}</button>
          <button onClick={onLogout} className="h-10 rounded-md bg-slate-100 px-3 text-xs font-black text-slate-600">Salir</button>
        </div>
        {updateInfo && (
          <div className="mx-auto mt-3 flex max-w-7xl flex-wrap items-center justify-between gap-2 rounded-md border border-amber-200 bg-amber-50 px-3 py-2 text-xs font-bold text-amber-900">
            <span>Actualización Android {updateInfo.version} disponible</span>
            <button onClick={() => updateInfo.url ? window.open(updateInfo.url, '_blank') : onCheckUpdate?.({ manual: true })} className="rounded-md bg-amber-500 px-3 py-1 text-white">Descargar</button>
          </div>
        )}
      </header>

      <main className={`grid w-full gap-4 p-3 ${isDex ? 'lg:grid-cols-[310px_minmax(0,1fr)]' : 'pb-24'}`}>
        <nav className={isDex ? 'sticky top-[86px] block h-[calc(100vh-104px)] overflow-auto rounded-lg p-4 text-white shadow-sm' : 'fixed inset-x-0 bottom-0 z-20 flex gap-2 overflow-x-auto border-t border-slate-200 bg-[#061826] p-2 shadow-lg'} style={isDex ? { backgroundColor: themeColor } : undefined}>
          {isDex && (
            <div className="mb-4 grid gap-3 text-center">
              <div className="mx-auto flex h-24 w-24 items-center justify-center overflow-hidden rounded-full bg-white p-2">
                <img src="/army-logo-doc.png" alt="" className="h-full w-full object-contain" />
              </div>
              <div className="text-xl font-black">G&amp;G ERP</div>
              <div className="rounded-lg bg-white/15 p-3 text-left text-xs font-bold">
                <div className="opacity-80">EMPRESA</div>
                <div className="truncate text-sm">{currentEmpresa().replace(/_/g, ' ').toUpperCase()}</div>
              </div>
            </div>
          )}
          <div className={isDex ? 'grid grid-cols-2 gap-3' : 'flex gap-2'}>
          {visibleModules.map((module) => (
            <button
              key={module.id}
              onClick={() => openView(module.id)}
              className={`flex shrink-0 items-center justify-center gap-2 rounded-lg border px-3 text-center font-black ${isDex ? 'min-h-24 flex-col text-white' : 'h-14 min-w-24'} ${
                view === module.id ? `${module.tone} border-transparent text-white` : (isDex ? 'border-white/10 bg-white/12 text-white hover:bg-white/18' : 'border-transparent bg-[#061826] text-slate-300')
              }`}
              style={view === module.id ? { backgroundColor: themeColor } : undefined}
            >
              <span className={`flex shrink-0 items-center justify-center rounded-md ${isDex ? 'h-10 w-10' : 'h-8 w-8'} ${view === module.id ? 'bg-white/20 text-white' : 'bg-white/14 text-white'}`}>
                <NavIcon name={module.icon} className={isDex ? 'h-6 w-6' : 'h-5 w-5'} />
              </span>
              <span className="text-xs sm:text-sm">{module.label}</span>
            </button>
          ))}
          </div>
        </nav>
        <section>{children}</section>
      </main>
    </div>
  );
}

function DashboardView({ setView }) {
  const [data, setData] = useState({});
  const [loading, setLoading] = useState(true);
  const [lastUpdate, setLastUpdate] = useState(new Date());

  const load = async () => {
    setLoading(true);
    try {
      setData(await apiFetch('/dashboard'));
      setLastUpdate(new Date());
    } finally {
      setLoading(false);
    }
  };

  useEffect(() => { load(); }, []);

  const maxVentas = Math.max(1, ...(data.ventas_por_dia || []).map(r => r.total || 0));

  return (
    <div className="grid gap-4">
      {/* Header didáctico */}
      <div className="flex items-center justify-between rounded-xl bg-gradient-to-r from-slate-900 to-slate-800 p-4 text-white shadow">
        <div>
          <div className="text-sm opacity-80">¡Bienvenido de vuelta!</div>
          <div className="text-2xl font-black">Panel de Control</div>
        </div>
        <div className="text-right text-xs">
          <div>Última actualización</div>
          <div className="font-mono">{lastUpdate.toLocaleTimeString()}</div>
          <ActionButton tone="neutral" onClick={load} className="mt-1 text-xs !py-1">🔄 Actualizar</ActionButton>
        </div>
      </div>

      {/* Stats con animación y más visuales */}
      <div className="grid grid-cols-2 gap-3 lg:grid-cols-4">
        <div className="group rounded-xl border bg-white p-4 shadow-sm transition-all hover:scale-[1.02] hover:shadow-md">
          <div className="flex items-center justify-between">
            <div>
              <div className="text-xs font-bold uppercase tracking-widest text-emerald-600">Ventas Hoy</div>
              <div className="mt-1 text-3xl font-black text-emerald-700 transition-all group-hover:text-emerald-800">{money(data.total_ventas_hoy ?? data.total_ventas)}</div>
            </div>
            <div className="text-4xl opacity-20 group-hover:opacity-40">💰</div>
          </div>
          <div className="mt-1 text-[10px] text-emerald-600">Ingresos del día actual</div>
        </div>

        <div className="group rounded-xl border bg-white p-4 shadow-sm transition-all hover:scale-[1.02] hover:shadow-md">
          <div className="flex items-center justify-between">
            <div>
              <div className="text-xs font-bold uppercase tracking-widest text-blue-600">Ventas Mes</div>
              <div className="mt-1 text-3xl font-black text-blue-700 transition-all group-hover:text-blue-800">{money(data.total_ventas_mes)}</div>
            </div>
            <div className="text-4xl opacity-20 group-hover:opacity-40">📈</div>
          </div>
          <div className="mt-1 text-[10px] text-blue-600">Acumulado este mes</div>
        </div>

        <div className="group rounded-xl border bg-white p-4 shadow-sm transition-all hover:scale-[1.02] hover:shadow-md">
          <div className="flex items-center justify-between">
            <div>
              <div className="text-xs font-bold uppercase tracking-widest text-violet-600">Documentos</div>
              <div className="mt-1 text-3xl font-black text-violet-700 transition-all group-hover:text-violet-800">{data.documentos || 0}</div>
            </div>
            <div className="text-4xl opacity-20 group-hover:opacity-40">📄</div>
          </div>
          <div className="mt-1 text-[10px] text-violet-600">Total emitidos</div>
        </div>

        <div className="group rounded-xl border bg-white p-4 shadow-sm transition-all hover:scale-[1.02] hover:shadow-md">
          <div className="flex items-center justify-between">
            <div>
              <div className="text-xs font-bold uppercase tracking-widest text-amber-600">Reservas</div>
              <div className="mt-1 text-3xl font-black text-amber-700 transition-all group-hover:text-amber-800">
                {data.reservas_activas || 0} / {money(data.reservas_saldo || 0)}
              </div>
            </div>
            <div className="text-4xl opacity-20 group-hover:opacity-40">📦</div>
          </div>
          <div className="mt-1 text-[10px] text-amber-600">Activas / Saldo pendiente</div>
        </div>
      </div>

      {/* Resumen comercial (full width, sin accesos rapidos) */}
      <Card title="📊 Resumen de ventas (últimos días)" actions={<ActionButton tone="neutral" onClick={load}>Actualizar</ActionButton>}>
        {loading ? <Empty text="Cargando panel..." /> : (
          <div className="space-y-2 pt-1">
            {(data.ventas_por_dia || []).slice(-10).map((row, idx) => {
              const pct = Math.round(((row.total || 0) / maxVentas) * 100);
              return (
                <div key={idx} className="group flex items-center gap-3 rounded-lg p-1 transition hover:bg-slate-50">
                  <div className="w-20 text-xs font-bold text-slate-500 tabular-nums">{row.dia}</div>
                  <div className="flex-1">
                    <div className="h-4 overflow-hidden rounded bg-emerald-100">
                      <div 
                        className="h-4 bg-emerald-600 transition-all duration-700 group-hover:bg-emerald-500" 
                        style={{ width: `${pct}%` }}
                      />
                    </div>
                  </div>
                  <div className="w-24 text-right text-sm font-black text-emerald-700 tabular-nums">{money(row.total)}</div>
                </div>
              );
            })}
            {!(data.ventas_por_dia || []).length && <Empty text="Sin datos de ventas recientes" />}
            <div className="pt-1 text-[10px] text-slate-500">Las barras muestran el porcentaje relativo del día con más ventas. Haz clic en Actualizar para refrescar.</div>
          </div>
        )}
      </Card>

      <div className="grid gap-4 xl:grid-cols-2">
        <Card title="💰 Cuentas por cobrar - Control" actions={<ActionButton tone="blue" onClick={() => setView('caja')}>Ir a caja</ActionButton>}>
          {(() => {
            const totalCobrar = (data.cuentas_cobrar || []).reduce((s, d) => s + (d.saldo || 0), 0);
            return (
              <>
                <div className="mb-2 flex justify-between text-sm font-bold">
                  <span>Total por cobrar:</span>
                  <span className="text-red-700">{money(totalCobrar)}</span>
                </div>
                <div className="grid gap-2 max-h-56 overflow-auto">
                  {(data.cuentas_cobrar || []).slice(0, 8).map((doc) => (
                    <div 
                      key={doc.id || `${doc.tipo}-${doc.numero}`} 
                      onClick={() => setView('caja')}
                      className="grid gap-1 rounded-md border border-red-200 bg-red-50 p-2 text-xs hover:bg-red-100 cursor-pointer md:grid-cols-[90px_1fr_90px]"
                    >
                      <b className="text-red-800">{doc.tipo} {doc.numero}</b>
                      <span className="truncate text-slate-700">{doc.cliente || 'Cliente general'} <span className="text-[10px] text-slate-500">({doc.fecha || ''})</span></span>
                      <b className="text-right text-red-700 font-black">{money(doc.saldo)}</b>
                    </div>
                  ))}
                  {!(data.cuentas_cobrar || []).length && <Empty text="Sin cuentas por cobrar. ¡Bien!" />}
                </div>
                <div className="mt-1 text-[10px] text-slate-500">Click en un documento para ir a Caja. Solo los deudores activos.</div>
              </>
            );
          })()}
        </Card>

        <Card title="🔍 Rastreador rápido de series"><SeriesQuickSearch /></Card>
      </div>

      <div className="grid gap-4 xl:grid-cols-2">
        <Card title="📄 Documentos recientes (últimos emitidos)">
          <div className="grid gap-1.5">
            {(data.recientes || []).map((doc, idx) => {
              const isPase = String(doc.tipo || '').toUpperCase() === 'PASE';
              return (
                <div key={`${doc.numero}-${idx}`} className={`grid grid-cols-[80px_1fr_85px] gap-2 rounded p-2 text-xs border ${isPase ? 'border-purple-200 bg-purple-50' : 'border-slate-100 bg-white'}`}>
                  <b className={`font-black ${isPase ? 'text-purple-700' : ''}`}>{doc.tipo}</b>
                  <span className="truncate text-slate-700">{doc.numero} • {doc.cliente || 'General'}</span>
                  <b className="text-right text-emerald-700">{money(doc.total)}</b>
                </div>
              );
            })}
            {!(data.recientes || []).length && <Empty text="Sin documentos recientes." />}
          </div>
          <div className="mt-1 text-[10px] text-slate-500">Últimos 8. PASEs marcados en morado.</div>
        </Card>

        <Card title="💳 Métodos de pago (hoy)">
          <div className="grid gap-2">
            {(data.metodos_pago || []).map((m) => (
              <div key={m.metodo} className="flex justify-between border-b border-slate-100 py-2 text-sm">
                <span>{m.metodo}</span>
                <b>{money(m.total)}</b>
              </div>
            ))}
            {!(data.metodos_pago || []).length && <Empty />}
          </div>
          <div className="mt-2 text-[10px] text-slate-500">Solo ventas PAGADAS del día de hoy. Útil para control de caja.</div>
        </Card>
      </div>
    </div>
  );
}

function VentasView({ user, sound, soundEnabled, setView }) {
  const saleUserName = user && typeof user === 'object' ? (user.usuario || '') : String(user || '');
  const saleTabsStorageKey = `gg_erp_sale_tabs_${EMPRESA}_${saleUserName || 'usuario'}`;
  const loadStoredSaleState = () => {
    try {
      const saved = JSON.parse(localStorage.getItem(saleTabsStorageKey) || '{}');
      const tabs = Array.isArray(saved.saleTabs) && saved.saleTabs.length ? saved.saleTabs : [{ id: 'sale-1', label: 'Cotizacion 1' }];
      return {
        saleTabs: tabs,
        activeTabId: saved.activeTabId && tabs.some((tab) => tab.id === saved.activeTabId) ? saved.activeTabId : tabs[0].id,
        saleDrafts: saved.saleDrafts && typeof saved.saleDrafts === 'object' ? saved.saleDrafts : {},
      };
    } catch {
      return { saleTabs: [{ id: 'sale-1', label: 'Cotizacion 1' }], activeTabId: 'sale-1', saleDrafts: {} };
    }
  };
  const storedSaleState = useMemo(loadStoredSaleState, []);
  const [products, setProducts] = useState([]);
  const [clientDoc, setClientDoc] = useState('');
  const [client, setClient] = useState({ tipo_documento_cliente: 'DNI', numero_documento_cliente: '', cliente_nombre: '', direccion_cliente: '' });
  const [docType, setDocType] = useState('PROFORMA');
  const [esPase, setEsPase] = useState(false);
  const [payment, setPayment] = useState({ estado_pago: 'DEUDA', metodo_pago: '' });
  const [saleObs, setSaleObs] = useState('');
  const [query, setQuery] = useState('');
  const [cart, setCart] = useState([]);
  const [productSeriesById, setProductSeriesById] = useState({});
  const [seriesTrace, setSeriesTrace] = useState({ lineIndex: null, loading: false, byKey: {} });
  const [selectedLineIndex, setSelectedLineIndex] = useState(null);
  const [saving, setSaving] = useState(false);
  const [viewer, setViewer] = useState(null);
  const [saleTabs, setSaleTabs] = useState(storedSaleState.saleTabs);
  const [activeTabId, setActiveTabId] = useState(storedSaleState.activeTabId);
  const [saleDrafts, setSaleDrafts] = useState(storedSaleState.saleDrafts);
  const hydratingSaleRef = useRef(false);

  const load = async () => setProducts(asArray(await apiFetch('/productos')));
  useEffect(() => { load(); }, []);

  const isProforma = docType === 'PROFORMA';
  const isTestProduct = (item) => /(^|\s)(producto\s*)?prueba($|\s)|random|varios/i.test(String(item?.nombre || item?.descripcion || ''));
  const filtered = filterProductsBySearch(products, query, { limit: 120 });
  const total = cart.reduce((sum, item) => sum + Number(item.total || 0), 0);
  const selectedLine = selectedLineIndex === null ? null : cart[selectedLineIndex] || null;
  const selectedLineSeries = selectedLine ? splitSeriesInput(selectedLine.series_texto) : [];
  const selectedLineQty = selectedLine ? Math.max(1, Number(selectedLine.cantidad || 1)) : 0;
  const selectedLineRegistered = selectedLine ? asArray(selectedLine.series_options) : [];
  const selectedLineRegisteredKeys = new Set(selectedLineRegistered.map((s) => normalizeSeriesKey(s.serie)).filter(Boolean));
  const selectedLineAvailableKeys = new Set(selectedLineRegistered
    .filter((s) => ['DISPONIBLE', 'RESERVADO'].includes(String(s.estado || 'DISPONIBLE').toUpperCase()))
    .map((s) => normalizeSeriesKey(s.serie))
    .filter(Boolean));
  const selectedLineRawSeries = selectedLine ? String(selectedLine.series_texto || '').split(/[,;\n\r|]+/).map((s) => s.trim()).filter(Boolean) : [];
  const selectedLineDuplicateKeys = selectedLineRawSeries.reduce((acc, serie, _, all) => {
    const key = normalizeSeriesKey(serie);
    if (all.filter((x) => normalizeSeriesKey(x) === key).length > 1) acc.add(key);
    return acc;
  }, new Set());
  const selectedLineSeriesCountOk = selectedLineSeries.length === selectedLineQty;
  useEffect(() => {
    const lineIndex = selectedLineIndex;
    const series = selectedLine ? splitSeriesInput(selectedLine.series_texto) : [];
    if (lineIndex === null || !series.length) {
      setSeriesTrace({ lineIndex, loading: false, byKey: {} });
      return;
    }
    let alive = true;
    setSeriesTrace((current) => ({ lineIndex, loading: true, byKey: current.lineIndex === lineIndex ? current.byKey : {} }));
    Promise.all(series.map(async (serie) => {
      const key = normalizeSeriesKey(serie);
      const rows = asArray(await apiFetch('/series', { params: { q: serie } }).catch(() => []))
        .filter((row) => normalizeSeriesKey(row.serie) === key);
      return [key, rows];
    })).then((entries) => {
      if (!alive) return;
      setSeriesTrace({ lineIndex, loading: false, byKey: Object.fromEntries(entries) });
    });
    return () => { alive = false; };
  }, [selectedLineIndex, selectedLine?.series_texto]);
  const blankSaleDraft = () => ({
    clientDoc: '',
    client: { tipo_documento_cliente: 'DNI', numero_documento_cliente: '', cliente_nombre: '', direccion_cliente: '' },
    docType: 'PROFORMA',
    esPase: false,
    payment: { estado_pago: 'DEUDA', metodo_pago: '' },
    saleObs: '',
    query: '',
    cart: [],
    selectedLineIndex: null,
  });
  const currentSaleDraft = () => ({ clientDoc, client, docType, esPase, payment, saleObs, query, cart, selectedLineIndex });
  const applySaleDraft = (draft) => {
    const next = { ...blankSaleDraft(), ...(draft || {}) };
    hydratingSaleRef.current = true;
    setClientDoc(next.clientDoc || '');
    setClient(next.client || blankSaleDraft().client);
    setDocType(next.docType || 'PROFORMA');
    setEsPase(!!next.esPase);
    setPayment(next.payment || blankSaleDraft().payment);
    setSaleObs(next.saleObs || '');
    setQuery(next.query || '');
    setCart(asArray(next.cart));
    setSelectedLineIndex(next.selectedLineIndex ?? null);
  };
  const saveCurrentTab = () => {
    const snapshot = currentSaleDraft();
    setSaleDrafts((current) => ({ ...current, [activeTabId]: snapshot }));
    return snapshot;
  };
  useEffect(() => {
    const initialDraft = saleDrafts[activeTabId] || blankSaleDraft();
    applySaleDraft(initialDraft);
  }, []);
  useEffect(() => {
    try {
      localStorage.setItem(saleTabsStorageKey, JSON.stringify({ saleTabs, activeTabId, saleDrafts }));
    } catch {
      // Local storage can be blocked in very old WebViews.
    }
  }, [saleTabs, activeTabId, saleDrafts]);
  const switchSaleTab = (id) => {
    if (id === activeTabId) return;
    const snapshot = currentSaleDraft();
    const nextDraft = saleDrafts[id] || blankSaleDraft();
    setSaleDrafts((current) => ({ ...current, [activeTabId]: snapshot, [id]: current[id] || nextDraft }));
    setActiveTabId(id);
    applySaleDraft(nextDraft);
  };
  const newSaleTab = () => {
    const snapshot = currentSaleDraft();
    const usedNumbers = saleTabs.map((tab) => Number(String(tab.label || '').match(/\d+/)?.[0] || 0));
    const nextNumber = Math.max(0, ...usedNumbers) + 1;
    const id = `sale-${Date.now()}`;
    const draft = blankSaleDraft();
    setSaleTabs((current) => [...current, { id, label: `Cotizacion ${nextNumber}` }]);
    setSaleDrafts((current) => ({ ...current, [activeTabId]: snapshot, [id]: draft }));
    setActiveTabId(id);
    applySaleDraft(draft);
  };
  const closeSaleTab = (id) => {
    if (saleTabs.length === 1) {
      resetSaleForm();
      return;
    }
    const nextTabs = saleTabs.filter((tab) => tab.id !== id);
    setSaleTabs(nextTabs);
    setSaleDrafts((current) => {
      const next = { ...current };
      delete next[id];
      return next;
    });
    if (id === activeTabId) {
      const next = nextTabs[0];
      setActiveTabId(next.id);
      applySaleDraft(saleDrafts[next.id] || blankSaleDraft());
    }
  };
  useEffect(() => {
    if (hydratingSaleRef.current) {
      hydratingSaleRef.current = false;
      return;
    }
    setSaleDrafts((current) => ({ ...current, [activeTabId]: currentSaleDraft() }));
  }, [activeTabId, clientDoc, client, docType, payment, saleObs, query, cart, selectedLineIndex]);

  const lineDescription = (item) => {
    const base = String(item.nombre || item.descripcion || '').trim();
    const detail = String(item.detalle_adicional || '').trim();
    return detail ? `${base}\n${detail}` : base;
  };
  const printableCart = cart.map(({ series_options, original_nombre, is_test_product, detalle_adicional, ...item }) => ({
    ...item,
    descripcion: lineDescription({ ...item, detalle_adicional }),
  }));

  const draftDoc = () => ({
    tipo: docType,
    numero: isProforma ? 'VISTA-PREVIA' : 'BORRADOR',
    cliente_nombre: client.cliente_nombre || (isProforma ? 'CLIENTE GENERAL' : ''),
    numero_documento_cliente: client.numero_documento_cliente,
    documento_cliente: client.numero_documento_cliente,
    direccion_cliente: client.direccion_cliente,
    fecha_emision: localTimestampForApi(),
    fecha_vencimiento: isProforma ? localDateForApi() : '',
    subtotal: total,
    igv: 0,
    total,
    estado_pago: isProforma ? 'PROFORMA' : payment.estado_pago,
    metodo_pago: isProforma ? '' : payment.metodo_pago,
    observacion: saleObs,
    usuario_emisor: user,
  });

  const resetSaleForm = () => {
    setClientDoc('');
    setClient({ tipo_documento_cliente: 'DNI', numero_documento_cliente: '', cliente_nombre: '', direccion_cliente: '' });
    setCart([]);
    setPayment({ estado_pago: 'DEUDA', metodo_pago: '' });
    setSaleObs('');
    setDocType('PROFORMA');
    setQuery('');
    setSelectedLineIndex(null);
  };
  const autoDocTypeForClient = (value, type = client.tipo_documento_cliente) => {
    if (!['PROFORMA', 'BOLETA', 'FACTURA'].includes(docType)) return;
    const digits = String(value || '').replace(/\D/g, '');
    if (!digits) {
      setDocType('PROFORMA');
      return;
    }
    setDocType(type === 'RUC' || digits.length === 11 ? 'FACTURA' : 'BOLETA');
  };
  const handleClientDocChange = (value) => {
    setClientDoc(value);
    setClient((current) => ({ ...current, numero_documento_cliente: value }));
    autoDocTypeForClient(value);
  };
  const handleClientTypeChange = (value) => {
    setClient((current) => ({ ...current, tipo_documento_cliente: value }));
    autoDocTypeForClient(clientDoc, value);
  };
  const clearProcessedSaleTab = () => {
    const blank = blankSaleDraft();
    hydratingSaleRef.current = true;
    const nextDrafts = { ...saleDrafts, [activeTabId]: blank };
    setSaleDrafts(nextDrafts);
    try {
      localStorage.setItem(saleTabsStorageKey, JSON.stringify({ saleTabs, activeTabId, saleDrafts: nextDrafts }));
    } catch {
      // Local storage can be blocked in very old WebViews.
    }
    applySaleDraft(blank);
  };

  const appendSeriesValue = (current, value) => {
    const pieces = `${current || ''},${value || ''}`
      .split(/[,;\n\r|]+/)
      .map((serie) => serie.trim())
      .filter(Boolean);
    const seen = new Set();
    return pieces.filter((serie) => {
      const key = serie.toUpperCase();
      if (seen.has(key)) return false;
      seen.add(key);
      return true;
    }).join(', ');
  };

  const lookupClient = async () => {
    const numero = documentDigits(clientDoc);
    if (!numero) return;
    const res = await lookupDocumentoApi(numero);
    if (res.found || res.ok) {
      setClient({
        tipo_documento_cliente: res.tipo_documento || (numero.length === 11 ? 'RUC' : 'DNI'),
        numero_documento_cliente: res.numero_documento || numero,
        cliente_nombre: res.nombre || res.razon_social || '',
        direccion_cliente: res.direccion || '',
      });
      setClientDoc(res.numero_documento || numero);
    } else {
      setClient({ ...client, numero_documento_cliente: numero });
      setClientDoc(numero);
      alert(res.msg || 'No se encontraron datos del cliente.');
    }
  };

  const addProduct = async (p) => {
    let seriesOptions = productSeriesById[p.id] || null;
    if (!seriesOptions) {
      seriesOptions = asArray(await apiFetch(`/productos/${p.id}/series`).catch(() => []));
      setProductSeriesById((current) => ({ ...current, [p.id]: seriesOptions }));
    }
    const qty = 1;
    const price = Number(p.precio_venta || p.precio || 0);
    const line = {
      id: p.id,
      producto_id: p.id,
      nombre: String(p.nombre || '').trim(),
      original_nombre: String(p.nombre || '').trim(),
      descripcion: String(p.nombre || '').trim(),
      categoria: p.categoria || '',
      marca: p.marca || '',
      modelo: p.modelo || '',
      imagen_url: p.imagen_url || '',
      almacen: p.almacen || 'TIENDA',
      is_test_product: isTestProduct(p),
      detalle_adicional: '',
      cantidad: qty,
      precio: price,
      total: qty * price,
      series_texto: '',
      series_options: seriesOptions,
    };
    setCart((current) => {
      const next = [...current, line];
      return next;
    });
    setQuery('');
  };

  const updateCart = (idx, patch) => {
    setCart((current) => current.map((item, i) => {
      if (i !== idx) return item;
      const cleanPatch = { ...patch };
      if (cleanPatch.nombre !== undefined && !item.is_test_product) delete cleanPatch.nombre;
      if (cleanPatch.precio !== undefined) cleanPatch.precio = cleanPriceInput(cleanPatch.precio);
      const next = { ...item, ...cleanPatch };
      if (cleanPatch.nombre !== undefined) next.descripcion = cleanPatch.nombre;
      next.cantidad = Math.max(1, Number(next.cantidad || 1));
      next.total = next.cantidad * parsePrice(next.precio);
      return next;
    }));
  };

  const previewDocument = async () => {
    if (!cart.length) {
      alert('Agrega productos para ver la vista previa.');
      return;
    }
    const doc = draftDoc();
    const config = await fetchDocumentConfig();
    try {
      setViewer(await buildDraftPdfViewer(doc, printableCart, config));
    } catch {
      setViewer({
        title: `${doc.tipo} ${doc.numero}`,
        fileName: documentFileName(`${doc.tipo} ${doc.numero}`),
        html: buildDocumentPreviewHtml(doc, printableCart, config),
      });
    }
  };

  const issue = async (legalSunat = false) => {
    if (!cart.length || (!isProforma && !client.cliente_nombre)) {
      alert(isProforma ? 'Agrega productos para procesar la proforma.' : 'Completa cliente y productos.');
      return;
    }
    if (legalSunat) {
      if (!['BOLETA', 'FACTURA'].includes(docType)) {
        alert('EMITIR solo aplica a boleta o factura legal.');
        return;
      }
      const serieLegal = docType === 'BOLETA' ? 'B002' : 'F002';
      if (!window.confirm(`Emitir ${docType} LEGAL (${serieLegal}) y enviar a SUNAT?`)) return;
    }
    setSaving(true);
    try {
      const clientPayload = {
        ...client,
        cliente_nombre: client.cliente_nombre || (isProforma ? 'CLIENTE GENERAL' : ''),
      };
      const issuedLines = printableCart.map(({ is_test_product, imagen_url, categoria, almacen, ...item }) => item);
      issuedLines.forEach((line) => { line.precio = parsePrice(line.precio); });
      const res = await apiFetch(isProforma ? '/proformas' : '/ventas', {
        method: 'POST',
        json: {
          tipo: isProforma ? 'PROFORMA' : docType,
          es_pase: docType === 'PASE' && esPase,
          emitir_legal_sunat: !!legalSunat,
          ...clientPayload,
          usuario_emisor: user,
          fecha_emision: localTimestampForApi(),
          fecha_vencimiento: isProforma ? localDateForApi() : '',
          estado_pago: isProforma ? 'DEUDA' : payment.estado_pago,
          metodo_pago: !isProforma && payment.estado_pago === 'PAGADO' ? payment.metodo_pago : '',
          observacion: saleObs,
          items: issuedLines,
        },
      });
      if (!okResponse(res)) {
        alert(res.msg || 'No se pudo emitir.');
        return;
      }
      if (legalSunat && res.sunat_auto) {
        const sa = res.sunat_auto;
        const err = sunatErrorSnippet(sa.respuesta);
        if (!okResponse(sa) && !sa.success) {
          alert(err ? `${res.numero || 'Documento'} creado. SUNAT: ${err}` : `${res.numero || 'Documento'} creado. ${sa.msg || 'Revisa configuracion SUNAT.'}`);
        }
      }
      if (soundEnabled) await sound.play();
      const issuedDoc = {
        id: res.id || res.data?.id,
        tipo: isProforma ? 'PROFORMA' : docType,
        es_pase: docType === 'PASE' && esPase,
        numero: res.numero || res.data?.numero || '',
        cliente_nombre: clientPayload.cliente_nombre,
        documento_cliente: clientPayload.numero_documento_cliente,
        numero_documento_cliente: clientPayload.numero_documento_cliente,
        direccion_cliente: clientPayload.direccion_cliente,
        fecha_emision: res.fecha_emision || localTimestampForApi(),
        fecha_vencimiento: res.fecha_vencimiento || (isProforma ? localDateForApi() : ''),
        subtotal: res.subtotal ?? total,
        igv: res.igv ?? 0,
        total: res.total ?? total,
        estado_pago: isProforma ? 'PROFORMA' : payment.estado_pago,
        metodo_pago: isProforma ? '' : payment.metodo_pago,
        observacion: saleObs,
        usuario_emisor: user,
      };
      clearProcessedSaleTab();
      setView?.('caja');
      if (issuedDoc.id) {
        setViewer(await buildDocumentViewer(issuedDoc));
      } else {
        const config = await fetchDocumentConfig();
        setViewer({
          title: `${issuedDoc.tipo} ${issuedDoc.numero || 'OK'}`,
          fileName: documentFileName(`${issuedDoc.tipo} ${issuedDoc.numero || 'OK'}`),
          html: buildDocumentPreviewHtml(issuedDoc, issuedLines, config),
        });
      }
    } finally {
      setSaving(false);
    }
  };
  const showProductSuggestions = query.trim().length > 0;
  const selectedLineTraceByKey = seriesTrace.lineIndex === selectedLineIndex ? (seriesTrace.byKey || {}) : {};

  return (
    <div className="grid min-h-[calc(100vh-96px)] grid-rows-[auto_auto_minmax(0,1fr)_auto] gap-3">
      <div className="flex flex-wrap items-center gap-2 rounded-lg border border-slate-200 bg-white p-2 shadow-sm">
        {saleTabs.map((tab) => {
          const draft = tab.id === activeTabId ? currentSaleDraft() : (saleDrafts[tab.id] || {});
          const count = asArray(draft.cart).length;
          return (
            <button
              key={tab.id}
              onClick={() => switchSaleTab(tab.id)}
              className={`flex items-center gap-2 rounded-md px-3 py-2 text-sm font-black ${tab.id === activeTabId ? 'bg-blue-600 text-white' : 'bg-slate-100 text-slate-700'}`}
            >
              <span>{tab.label}</span>
              <span className={tab.id === activeTabId ? 'text-blue-100' : 'text-slate-500'}>{count}</span>
              <span onClick={(e) => { e.stopPropagation(); closeSaleTab(tab.id); }} className="rounded px-1 text-xs opacity-80">x</span>
            </button>
          );
        })}
        <button onClick={newSaleTab} className="rounded-md bg-emerald-600 px-3 py-2 text-sm font-black text-white">+ Nueva cotizacion</button>
      </div>
      <section className="rounded-lg border border-slate-200 bg-white p-3 shadow-sm">
        <div className="mb-3 flex items-center justify-between gap-3">
          <h2 className="text-xl font-black text-slate-950">Nueva venta</h2>
          <label className="flex items-center gap-2 text-sm font-black text-slate-700">
            <input type="checkbox" checked={isProforma} onChange={(e) => setDocType(e.target.checked ? 'PROFORMA' : (client.tipo_documento_cliente === 'RUC' ? 'FACTURA' : 'BOLETA'))} />
            PROFORMA
          </label>
        </div>
        <div className="grid gap-3 xl:grid-cols-[minmax(280px,1.25fr)_220px_220px_180px]">
          <TextInput placeholder="Cliente" value={client.cliente_nombre} onChange={(e) => setClient({ ...client, cliente_nombre: e.target.value })} />
          <Field label="Fecha emision"><TextInput value={localDateForApi()} readOnly /></Field>
          <Field label="Fecha vcto"><TextInput value={isProforma ? localDateForApi() : '-'} readOnly /></Field>
          <Field label="Tipo comprobante">
            <SelectInput value={docType} onChange={(e) => { setDocType(e.target.value); if (e.target.value !== 'PASE') setEsPase(false); }}>{DOC_TYPES.map((t) => <option key={t}>{t}</option>)}</SelectInput>
            {docType === 'PASE' && (
              <label className="flex items-center gap-2 text-sm font-black text-slate-700 mt-1">
                <input type="checkbox" checked={esPase} onChange={(e) => setEsPase(e.target.checked)} />
                Pase (para mejor control, se marca en caja)
              </label>
            )}
          </Field>
          <div className="grid gap-2 md:grid-cols-[105px_minmax(0,1fr)_auto] xl:col-span-2">
            <SelectInput value={client.tipo_documento_cliente} onChange={(e) => handleClientTypeChange(e.target.value)}>
              <option>DNI</option>
              <option>RUC</option>
            </SelectInput>
            <TextInput placeholder="DNI / RUC" value={clientDoc} onChange={(e) => handleClientDocChange(e.target.value)} inputMode="numeric" />
            <ActionButton tone="blue" onClick={lookupClient}>Buscar</ActionButton>
          </div>
          <TextInput placeholder="Direccion" value={client.direccion_cliente} onChange={(e) => setClient({ ...client, direccion_cliente: e.target.value })} className="xl:col-span-2" />
          <div className="xl:col-span-2">
            <Field label="Observacion">
              <TextInput placeholder="Observacion del documento" value={saleObs} onChange={(e) => setSaleObs(e.target.value)} />
            </Field>
          </div>
        </div>
      </section>

      <section className="min-h-0 overflow-auto rounded-lg border border-slate-200 bg-white shadow-sm">
        <div className="grid gap-0">
          {cart.map((item, idx) => (
            <div
              key={`${item.id}-${idx}`}
              onClick={() => setSelectedLineIndex(idx)}
              className={`grid cursor-pointer gap-3 border-b border-slate-200 p-2 transition md:grid-cols-[74px_minmax(0,1fr)_118px_118px_118px_136px] ${selectedLineIndex === idx ? 'bg-blue-50' : 'bg-white hover:bg-slate-50'}`}
            >
              <div className="h-16 w-16 overflow-hidden rounded-md bg-slate-100">
                {safeImageSrc(item.imagen_url) ? <img src={safeImageSrc(item.imagen_url)} alt="" className="h-full w-full object-cover" /> : <div className="grid h-full w-full place-items-center text-xs font-black text-slate-400">IMG</div>}
              </div>
              <div className="min-w-0 py-1">
                <div className="line-clamp-1 text-sm font-black uppercase text-slate-950">{item.nombre}</div>
                {item.series_texto ? <div className="mt-1 line-clamp-1 text-xs font-black text-blue-700">S/N: {item.series_texto}</div> : <div className="mt-1 text-xs font-semibold uppercase text-slate-500">{item.almacen || 'TIENDA'} / {item.categoria || 'SIN CATEGORIA'}</div>}
              </div>
              <div className="rounded-md bg-slate-50 p-2">
                <div className="text-[11px] font-bold uppercase text-slate-500">Almacen</div>
                <div className="text-sm font-black text-slate-950">{item.almacen || 'TIENDA'}</div>
              </div>
              <div className="rounded-md bg-slate-50 p-2">
                <div className="text-[11px] font-bold uppercase text-slate-500">P. Unit.</div>
                <div className="text-sm font-black text-slate-950">{money(item.precio)}</div>
              </div>
              <div className="rounded-md bg-slate-50 p-2">
                <div className="text-[11px] font-bold uppercase text-slate-500">Total</div>
                <div className="text-sm font-black text-slate-950">{money(item.total)}</div>
              </div>
              <div className="grid gap-1">
                <div className="grid grid-cols-3 overflow-hidden rounded-md border border-slate-200 bg-white text-center">
                  <button onClick={(e) => { e.stopPropagation(); updateCart(idx, { cantidad: Number(item.cantidad || 1) - 1 }); }} className="bg-blue-600 py-2 text-lg font-black text-white">-</button>
                  <div className="grid place-items-center text-base font-black">{item.cantidad}</div>
                  <button onClick={(e) => { e.stopPropagation(); updateCart(idx, { cantidad: Number(item.cantidad || 1) + 1 }); }} className="bg-blue-600 py-2 text-lg font-black text-white">+</button>
                </div>
                <button onClick={(e) => { e.stopPropagation(); setCart(cart.filter((_, i) => i !== idx)); if (selectedLineIndex === idx) setSelectedLineIndex(null); }} className="rounded-md border border-slate-300 py-1.5 text-xs font-black">ELIMINAR</button>
              </div>
            </div>
          ))}
          {!cart.length && <div className="grid min-h-[260px] place-items-center p-8"><Empty text="Busca productos abajo y agrégalos con un clic." /></div>}
        </div>
      </section>

      <footer className="sticky bottom-0 z-20 rounded-lg border border-slate-200 bg-white p-3 shadow-[0_-8px_24px_rgba(15,23,42,0.08)]">
        {showProductSuggestions && (
          <div className="mb-3 max-h-[28vh] overflow-auto rounded-md border border-slate-200 bg-white">
            {filtered.map((p) => (
              <button key={p.id} onClick={() => addProduct(p)} className="grid w-full grid-cols-[58px_minmax(0,1fr)_120px_108px] items-center gap-3 border-b border-slate-100 px-3 py-2 text-left hover:bg-slate-50">
                <div className="h-12 w-12 overflow-hidden rounded bg-slate-100">
                  {safeImageSrc(p.imagen_url) ? <img src={safeImageSrc(p.imagen_url)} alt="" className="h-full w-full object-cover" /> : <div className="grid h-full w-full place-items-center text-xs font-black text-slate-400">IMG</div>}
                </div>
                <div className="min-w-0">
                  <div className="truncate text-sm font-black uppercase text-slate-950">{p.nombre}</div>
                  <div className="truncate text-xs uppercase text-slate-500">{p.categoria || 'SIN CATEGORIA'}{p.marca ? ` - ${p.marca}` : ''}</div>
                </div>
                <div className="text-right text-base font-black text-emerald-700">{money(p.precio_venta || p.precio)}</div>
                <div className={`rounded-md px-3 py-2 text-center text-xs font-black ${Number(p.stock || 0) <= 0 ? 'bg-red-100 text-red-700' : 'bg-amber-100 text-amber-700'}`}>Stock {p.stock || 0}</div>
              </button>
            ))}
            {!filtered.length && <Empty text="No hay productos con esa busqueda." />}
          </div>
        )}
        <TextInput placeholder="Buscar producto..." value={query} onChange={(e) => setQuery(e.target.value)} className="h-12 text-lg" />
        <div className="mt-3 grid gap-3 md:grid-cols-[minmax(220px,1fr)_220px_minmax(220px,1fr)]">
          <div className="flex items-center justify-between rounded-md bg-slate-100 px-6 py-4">
            <span className="text-2xl font-black uppercase text-slate-700">Total</span>
            <span className="text-3xl font-black text-slate-950">{money(total)}</span>
          </div>
          <ActionButton tone="blue" onClick={previewDocument}>Vista previa</ActionButton>
          <div className="flex gap-2">
            <ActionButton className="flex-1" disabled={saving} onClick={() => issue(false)}>{isProforma ? 'Procesar' : 'Procesar'}</ActionButton>
            {canEmitLegalSunat(saleUserName) && (
              <ActionButton
                tone="amber"
                className="flex-1 min-w-[150px] px-3 text-sm font-black"
                disabled={saving || isProforma || !['BOLETA', 'FACTURA'].includes(docType)}
                title={
                  isProforma
                    ? 'Desmarca PROFORMA y elige BOLETA o FACTURA para emitir legal a SUNAT'
                    : !['BOLETA', 'FACTURA'].includes(docType)
                      ? 'Elige BOLETA o FACTURA para EMITIR legal a SUNAT'
                      : `Emitir ${docType} legal ${docType === 'BOLETA' ? 'B002' : 'F002'} y enviar a SUNAT`
                }
                onClick={() => issue(true)}
              >
                EMITIR {docType === 'BOLETA' ? 'B002' : docType === 'FACTURA' ? 'F002' : 'SUNAT'}
              </ActionButton>
            )}
          </div>
        </div>
      </footer>

      {selectedLine && (
        <Modal title={`Detalle / ${selectedLine.nombre}`} onClose={() => setSelectedLineIndex(null)}>
          <div className="grid gap-4 md:grid-cols-2">
            <Field label="Nombre">
              <TextInput
                value={selectedLine.nombre}
                readOnly={!selectedLine.is_test_product}
                onChange={(e) => updateCart(selectedLineIndex, { nombre: e.target.value })}
                className={!selectedLine.is_test_product ? 'bg-slate-100 font-black uppercase' : 'font-black uppercase'}
              />
              {!selectedLine.is_test_product && <div className="mt-1 text-xs font-semibold text-slate-500">Nombre bloqueado para mantener el nombre real interno.</div>}
            </Field>
            <Field label="Almacen salida">
              <SelectInput value={selectedLine.almacen || 'TIENDA'} onChange={(e) => updateCart(selectedLineIndex, { almacen: e.target.value })}>
                {['TIENDA', 'ALMACEN', 'GARANTIA', 'RESERVADO'].map((name) => <option key={name}>{name}</option>)}
              </SelectInput>
            </Field>
            <Field label="Precio unitario">
              <div className="grid gap-2 sm:grid-cols-[1fr_auto_auto]">
                <TextInput value={selectedLine.precio} inputMode="decimal" onPaste={(e) => { e.preventDefault(); updateCart(selectedLineIndex, { precio: e.clipboardData?.getData('text') || '' }); }} onChange={(e) => updateCart(selectedLineIndex, { precio: e.target.value })} />
                <button type="button" className="rounded-md bg-emerald-100 px-3 py-2 text-sm font-black text-emerald-800" onClick={() => updateCart(selectedLineIndex, { precio: price99(selectedLine.precio) })}>.99</button>
                <button type="button" className="rounded-md bg-slate-100 px-3 py-2 text-sm font-black text-slate-700" onClick={() => copyText(parsePrice(selectedLine.precio).toFixed(2))}>Copiar</button>
              </div>
            </Field>
            <Field label="Cantidad">
              <TextInput value={selectedLine.cantidad} inputMode="numeric" onChange={(e) => updateCart(selectedLineIndex, { cantidad: e.target.value })} />
            </Field>
            <Field label="Serie registrada">
              <SelectInput value="" onChange={(e) => e.target.value && updateCart(selectedLineIndex, { series_texto: normalizeSeriesInput(appendSeriesValue(selectedLine.series_texto, e.target.value)) })}>
                <option value="">Escoger serie disponible</option>
                {asArray(selectedLine.series_options).filter((s) => ['DISPONIBLE', 'RESERVADO'].includes(String(s.estado || 'DISPONIBLE').toUpperCase())).map((s) => <option key={s.id || s.serie} value={s.serie}>{s.serie}</option>)}
              </SelectInput>
            </Field>
            <div className="md:col-span-2">
              <div className="grid gap-3 md:grid-cols-[minmax(0,1fr)_260px]">
                <Field label={`Series manuales - ${selectedLineSeries.length}/${selectedLineQty} detectada(s)`}>
                  <TextArea
                    placeholder={`Escanea o pega una serie por linea:\nSN001\nSN002\nSN003`}
                    value={selectedLine.series_texto || ''}
                    onChange={(e) => updateCart(selectedLineIndex, { series_texto: e.target.value })}
                    onPaste={(e) => {
                      const pasted = e.clipboardData?.getData('text');
                      if (!pasted) return;
                      e.preventDefault();
                      updateCart(selectedLineIndex, { series_texto: normalizeSeriesInput(`${selectedLine.series_texto || ''}\n${pasted}`) });
                    }}
                    className="min-h-44 font-mono text-base leading-7"
                  />
                </Field>
                <div className={`rounded-md border p-3 text-xs ${selectedLineSeriesCountOk ? 'border-emerald-200 bg-emerald-50' : 'border-amber-200 bg-amber-50'}`}>
                  <div className={`mb-2 font-black ${selectedLineSeriesCountOk ? 'text-emerald-800' : 'text-amber-800'}`}>
                    {selectedLineSeriesCountOk ? 'OK series completas' : `Faltan ${Math.max(selectedLineQty - selectedLineSeries.length, 0)} serie(s)`}
                  </div>
                  <div className="max-h-36 overflow-auto rounded bg-white/70">
                    {selectedLineSeries.map((serie, idx) => {
                      const key = normalizeSeriesKey(serie);
                      const duplicate = selectedLineDuplicateKeys.has(key);
                      const traceRows = asArray(selectedLineTraceByKey[key]);
                      const activeTraceRows = traceRows.filter((row) => ['DISPONIBLE', 'RESERVADO'].includes(String(row.estado || 'DISPONIBLE').toUpperCase()));
                      const traceProductNames = [...new Set(activeTraceRows.map((row) => String(row.producto_nombre || '').trim()).filter(Boolean))];
                      const hasRegistered = selectedLineRegisteredKeys.has(key);
                      const isAvailable = selectedLineAvailableKeys.has(key);
                      const acceptsManual = !selectedLineRegistered.length;
                      const foundElsewhere = activeTraceRows.length > 0;
                      const ok = !duplicate && (isAvailable || foundElsewhere || acceptsManual || !hasRegistered);
                      const label = duplicate ? 'Repetida' : isAvailable ? 'Disponible' : foundElsewhere ? 'Interna OK' : hasRegistered ? 'No disponible' : acceptsManual ? 'Manual OK' : 'Venta directa';
                      const traceLabel = traceProductNames.length ? traceProductNames.join(' / ') : '';
                      return (
                        <div key={`${serie}-${idx}`} className={`mb-1 rounded px-2 py-1 font-bold ${ok ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-700'}`}>
                          <div className="flex items-center justify-between gap-2">
                            <span className="truncate">{serie}</span>
                            <span>{ok ? 'OK' : '!' } {label}</span>
                          </div>
                          {traceLabel && <div className="mt-1 rounded border border-emerald-300 bg-white px-2 py-1 text-[11px] font-black uppercase text-slate-950">PRODUCTO INTERNO: {traceLabel}</div>}
                        </div>
                      );
                    })}
                    {!selectedLineSeries.length && <div className="p-2 font-bold text-slate-500">Pega o escanea las series para validar.</div>}
                  </div>
                  {seriesTrace.lineIndex === selectedLineIndex && (
                    <div className="mt-2 rounded bg-white/80 p-2 font-bold text-slate-700">
                      {seriesTrace.loading ? 'Buscando producto interno...' : 'El nombre visible puede cambiar; el descuento se guia por la serie interna detectada.'}
                    </div>
                  )}
                </div>
              </div>
              <div className="mt-2 grid grid-cols-2 gap-2">
                <ActionButton tone="neutral" onClick={() => updateCart(selectedLineIndex, { series_texto: normalizeSeriesInput(selectedLine.series_texto) })}>Ordenar en lineas</ActionButton>
                <ActionButton tone="neutral" onClick={() => updateCart(selectedLineIndex, { series_texto: `${String(selectedLine.series_texto || '').trim()}\n`.trimStart() })}>+ Otra serie</ActionButton>
              </div>
            </div>
            <Field label="Detalle adicional">
              <textarea className="min-h-28 w-full rounded-md border border-slate-200 px-4 py-3 text-sm outline-none focus:border-blue-400" placeholder="Observacion o detalle que saldra debajo del producto" value={selectedLine.detalle_adicional || ''} onChange={(e) => updateCart(selectedLineIndex, { detalle_adicional: e.target.value })} />
            </Field>
            <div className="grid content-end gap-3">
              <div className="rounded-md bg-slate-100 p-4 text-center text-xl font-black">TOTAL {money(selectedLine.total)}</div>
              <ActionButton tone="blue" onClick={() => setSelectedLineIndex(null)}>Guardar</ActionButton>
            </div>
          </div>
        </Modal>
      )}
      <DocumentViewerModal viewer={viewer} onClose={() => setViewer(null)} />
    </div>
  );
}

function LegacyVentasView({ user, sound, soundEnabled }) {
  const [products, setProducts] = useState([]);
  const [clientDoc, setClientDoc] = useState('');
  const [client, setClient] = useState({ tipo_documento_cliente: 'DNI', numero_documento_cliente: '', cliente_nombre: '', direccion_cliente: '' });
  const [docType, setDocType] = useState('BOLETA');
  const [payment, setPayment] = useState({ estado_pago: 'DEUDA', metodo_pago: '' });
  const [query, setQuery] = useState('');
  const [cart, setCart] = useState([]);
  const [productSeriesById, setProductSeriesById] = useState({});
  const [saving, setSaving] = useState(false);
  const [viewer, setViewer] = useState(null);
  const [lastIssuedDoc, setLastIssuedDoc] = useState(null);

  const load = async () => setProducts(asArray(await apiFetch('/productos')));
  useEffect(() => { load(); }, []);

  const filtered = filterProductsBySearch(products, query, { limit: 80 });
  const total = cart.reduce((sum, item) => sum + Number(item.total || 0), 0);
  const isProforma = docType === 'PROFORMA';
  const draftDoc = () => ({
    tipo: docType,
    numero: isProforma ? 'VISTA-PREVIA' : 'BORRADOR',
    cliente_nombre: client.cliente_nombre || (isProforma ? 'CLIENTE GENERAL' : ''),
    documento_cliente: client.numero_documento_cliente,
    direccion_cliente: client.direccion_cliente,
    fecha_emision: localTimestampForApi(),
    fecha_vencimiento: isProforma ? localDateForApi() : '',
    subtotal: total,
    igv: 0,
    total,
    estado_pago: isProforma ? 'PROFORMA' : payment.estado_pago,
    metodo_pago: isProforma ? '' : payment.metodo_pago,
    usuario_emisor: user,
  });

  const emptyClient = () => ({ tipo_documento_cliente: 'DNI', numero_documento_cliente: '', cliente_nombre: '', direccion_cliente: '' });
  const resetSaleForm = () => {
    setClientDoc('');
    setClient(emptyClient());
    setCart([]);
    setPayment({ estado_pago: 'DEUDA', metodo_pago: '' });
    setDocType('PROFORMA');
    setQuery('');
  };

  const appendSeriesValue = (current, value) => {
    const pieces = `${current || ''},${value || ''}`
      .split(/[,;\n\r|]+/)
      .map((serie) => serie.trim())
      .filter(Boolean);
    const seen = new Set();
    return pieces.filter((serie) => {
      const key = serie.toUpperCase();
      if (seen.has(key)) return false;
      seen.add(key);
      return true;
    }).join(', ');
  };

  const previewDocument = async () => {
    if (!cart.length) {
      alert('Agrega productos para ver la vista previa.');
      return;
    }
    const doc = draftDoc();
    const config = await fetchDocumentConfig();
    try {
      setViewer(await buildDraftPdfViewer(doc, cart, config));
    } catch {
      setViewer({
        title: `${doc.tipo} ${doc.numero}`,
        fileName: documentFileName(`${doc.tipo} ${doc.numero}`),
        html: buildDocumentPreviewHtml(doc, cart, config),
      });
    }
  };

  const lookupClient = async () => {
    const numero = documentDigits(clientDoc);
    if (!numero) return;
    const res = await lookupDocumentoApi(numero);
    if (res.found || res.ok) {
      setClient({
        tipo_documento_cliente: res.tipo_documento || (numero.length === 11 ? 'RUC' : 'DNI'),
        numero_documento_cliente: res.numero_documento || numero,
        cliente_nombre: res.nombre || res.razon_social || '',
        direccion_cliente: res.direccion || '',
      });
      setClientDoc(res.numero_documento || numero);
    } else {
      setClient({ ...client, numero_documento_cliente: numero });
      setClientDoc(numero);
      alert(res.msg || 'No se encontraron datos del cliente.');
    }
  };

  const addProduct = async (p) => {
    const docName = String(p.nombre || '').trim();
    if (!docName) return;
    let seriesOptions = productSeriesById[p.id] || null;
    if (!seriesOptions) {
      seriesOptions = asArray(await apiFetch(`/productos/${p.id}/series`).catch(() => []));
      setProductSeriesById((current) => ({ ...current, [p.id]: seriesOptions }));
    }
    const qty = 1;
    const price = Number(p.precio_venta || p.precio || 0);
    setCart([...cart, {
      id: p.id,
      producto_id: p.id,
      nombre: docName,
      descripcion: docName,
      marca: p.marca || '',
      modelo: p.modelo || '',
      cantidad: qty,
      precio: price,
      total: qty * price,
      series_texto: '',
      series_options: seriesOptions,
    }]);
    setQuery('');
  };

  const updateCart = (idx, patch) => {
    setCart(cart.map((item, i) => {
      if (i !== idx) return item;
      const cleanPatch = { ...patch };
      if (cleanPatch.precio !== undefined) cleanPatch.precio = cleanPriceInput(cleanPatch.precio);
      const next = { ...item, ...cleanPatch };
      if (patch.nombre !== undefined) next.descripcion = patch.nombre;
      next.cantidad = Number(next.cantidad || 0);
      next.total = next.cantidad * parsePrice(next.precio);
      return next;
    }));
  };

  const issue = async () => {
    if (!cart.length || (!isProforma && !client.cliente_nombre)) {
      alert(isProforma ? 'Agrega productos para procesar la proforma.' : 'Completa cliente y productos.');
      return;
    }
    setSaving(true);
    try {
      const selectedDocType = isProforma ? 'PROFORMA' : docType;
      const issuedLines = cart.map(({ series_options, ...item }) => ({ ...item, precio: parsePrice(item.precio) }));
      const clientPayload = {
        ...client,
        cliente_nombre: client.cliente_nombre || (isProforma ? 'CLIENTE GENERAL' : ''),
      };
      const res = await apiFetch(isProforma ? '/proformas' : '/ventas', {
        method: 'POST',
        json: {
          tipo: selectedDocType,
          ...clientPayload,
          usuario_emisor: user,
          fecha_emision: localTimestampForApi(),
          fecha_vencimiento: isProforma ? localDateForApi() : '',
          estado_pago: isProforma ? 'DEUDA' : payment.estado_pago,
          metodo_pago: !isProforma && payment.estado_pago === 'PAGADO' ? payment.metodo_pago : '',
          items: issuedLines,
        },
      });
      if (okResponse(res)) {
        if (soundEnabled) await sound.play();
        const issuedDoc = {
          id: res.id || res.data?.id,
          tipo: selectedDocType,
          numero: res.numero || res.data?.numero || '',
          cliente_nombre: clientPayload.cliente_nombre,
          documento_cliente: clientPayload.numero_documento_cliente,
          direccion_cliente: clientPayload.direccion_cliente,
          fecha_emision: res.fecha_emision || localTimestampForApi(),
          fecha_vencimiento: res.fecha_vencimiento || (isProforma ? localDateForApi() : ''),
          subtotal: res.subtotal ?? total,
          igv: res.igv ?? 0,
          total: res.total ?? total,
          estado_pago: isProforma ? 'PROFORMA' : payment.estado_pago,
          metodo_pago: isProforma ? '' : payment.metodo_pago,
          usuario_emisor: user,
        };
        setLastIssuedDoc(issuedDoc);
        resetSaleForm();
        if (issuedDoc.id) {
          setViewer(await buildDocumentViewer(issuedDoc));
        } else {
          const config = await fetchDocumentConfig();
          setViewer({
            title: `${issuedDoc.tipo} ${issuedDoc.numero || 'OK'}`,
            fileName: documentFileName(`${issuedDoc.tipo} ${issuedDoc.numero || 'OK'}`),
            html: buildDocumentPreviewHtml(issuedDoc, issuedLines, config),
          });
        }
      } else {
        alert(res.msg || 'No se pudo emitir.');
      }
    } finally {
      setSaving(false);
    }
  };

  return (
    <div className="grid gap-4 xl:grid-cols-[430px_1fr]">
      <Card title="Emitir documento">
        <div className="grid gap-3">
          <div className="grid grid-cols-2 gap-2">
            <Field label="Tipo">
              <SelectInput value={docType} onChange={(e) => setDocType(e.target.value)}>{DOC_TYPES.map((t) => <option key={t}>{t}</option>)}</SelectInput>
            </Field>
            <Field label="Estado pago">
              <SelectInput value={isProforma ? 'PROFORMA' : payment.estado_pago} disabled={isProforma} onChange={(e) => setPayment({ ...payment, estado_pago: e.target.value })}>{isProforma ? <option>PROFORMA</option> : PAY_STATES.map((s) => <option key={s}>{s}</option>)}</SelectInput>
            </Field>
          </div>
          <Field label="Metodo pago">
            <SelectInput value={isProforma ? '' : payment.metodo_pago} disabled={isProforma} onChange={(e) => setPayment({ ...payment, metodo_pago: e.target.value })}>
              <option value="">Sin metodo</option>
              {PAY_METHODS.map((m) => <option key={m}>{m}</option>)}
            </SelectInput>
          </Field>
          <div className="grid grid-cols-[1fr_auto] gap-2">
            <Field label="DNI/RUC">
              <TextInput value={clientDoc} onChange={(e) => setClientDoc(e.target.value)} inputMode="numeric" />
            </Field>
            <ActionButton className="self-end" tone="blue" onClick={lookupClient}>Buscar</ActionButton>
          </div>
          <Field label="Cliente">
            <TextInput value={client.cliente_nombre} onChange={(e) => setClient({ ...client, cliente_nombre: e.target.value })} />
          </Field>
          <Field label="Dirección">
            <TextInput value={client.direccion_cliente} onChange={(e) => setClient({ ...client, direccion_cliente: e.target.value })} />
          </Field>
          <div className="rounded-md bg-slate-50 p-3">
            <div className="flex justify-between text-sm"><span>Total</span><b>{money(total)}</b></div>
          </div>
          <div className="grid grid-cols-2 gap-2">
            <ActionButton tone="blue" onClick={previewDocument}>Vista previa</ActionButton>
            <ActionButton disabled={saving} onClick={issue}>{isProforma ? 'Procesar proforma' : 'Enviar a caja / emitir'}</ActionButton>
          </div>
          {lastIssuedDoc && (
            <ActionButton
              tone="blue"
              onClick={async () => {
                try {
                  setViewer({ title: 'Cargando documento...', html: '<p style="font-family:Arial;padding:20px">Cargando...</p>' });
                  setViewer(await buildDocumentViewer(lastIssuedDoc));
                } catch (error) {
                  setViewer(null);
                  alert(error?.message || 'No se pudo abrir el ultimo PDF.');
                }
              }}
            >
              Ver ultimo PDF
            </ActionButton>
          )}
        </div>
      </Card>
      <div className="grid gap-4">
        <Card title="Productos para venta">
          <div className="grid max-h-72 gap-2 overflow-auto md:grid-cols-2">
            {filtered.map((p) => (
              <button key={p.id} onClick={() => addProduct(p)} className="grid grid-cols-[58px_1fr] gap-3 rounded-md border border-slate-200 p-3 text-left active:bg-slate-50">
                <div className="h-14 w-14 overflow-hidden rounded-md bg-slate-100">
                  {safeImageSrc(p.imagen_url) ? <img src={safeImageSrc(p.imagen_url)} alt="" className="h-full w-full object-cover" /> : <div className="grid h-full w-full place-items-center text-xs font-black text-slate-400">IMG</div>}
                </div>
                <div className="min-w-0">
                  <div className="line-clamp-2 font-black">{p.nombre}</div>
                  <div className="text-xs text-slate-500">Stock {p.stock}</div>
                  <div className="mt-1 font-black">{money(p.precio_venta || p.precio)}</div>
                </div>
              </button>
            ))}
          </div>
          <TextInput placeholder="Buscar producto..." value={query} onChange={(e) => setQuery(e.target.value)} className="mt-3 h-12 w-full" />
        </Card>
        <Card title="Detalle de venta">
          <div className="grid gap-2">
            {cart.map((item, idx) => (
              <div key={`${item.id}-${idx}`} className="grid gap-2 rounded-md border border-slate-200 p-2 lg:grid-cols-[1fr_70px_90px_90px_34px]">
                <div>
                  <TextInput placeholder="Nombre en documento" value={item.nombre} onChange={(e) => updateCart(idx, { nombre: e.target.value })} className="h-9 font-black" />
                  <div className="mt-1 grid gap-1 sm:grid-cols-[1fr_1fr]">
                    <SelectInput value="" onChange={(e) => e.target.value && updateCart(idx, { series_texto: normalizeSeriesInput(appendSeriesValue(item.series_texto, e.target.value)) })} className="h-9">
                      <option value="">Escoger serie</option>
                      {asArray(item.series_options).filter((s) => ['DISPONIBLE', 'RESERVADO'].includes(String(s.estado || 'DISPONIBLE').toUpperCase())).map((s) => <option key={s.id || s.serie} value={s.serie}>{s.serie}</option>)}
                    </SelectInput>
                    <TextArea
                      placeholder="Series: una por linea"
                      value={item.series_texto}
                      onChange={(e) => updateCart(idx, { series_texto: e.target.value })}
                      onPaste={(e) => {
                        const pasted = e.clipboardData?.getData('text');
                        if (!pasted) return;
                        e.preventDefault();
                        updateCart(idx, { series_texto: normalizeSeriesInput(`${item.series_texto || ''}\n${pasted}`) });
                      }}
                      className="min-h-20 font-mono"
                    />
                  </div>
                </div>
                <TextInput value={item.cantidad} inputMode="numeric" onChange={(e) => updateCart(idx, { cantidad: e.target.value })} />
                <TextInput value={item.precio} inputMode="decimal" onChange={(e) => updateCart(idx, { precio: e.target.value })} />
                <div className="flex items-center justify-end font-black">{money(item.total)}</div>
                <button className="rounded-md bg-red-100 font-black text-red-700" onClick={() => setCart(cart.filter((_, i) => i !== idx))}>X</button>
              </div>
            ))}
            {!cart.length && <Empty text="Agrega productos a la venta." />}
          </div>
        </Card>
      </div>
      <DocumentViewerModal viewer={viewer} onClose={() => setViewer(null)} />
    </div>
  );
}

function CajaView({ sound, soundEnabled, user, setView }) {
  const [docs, setDocs] = useState([]);
  const [movs, setMovs] = useState([]);
  const [selectedId, setSelectedId] = useState('');
  const [filter, setFilter] = useState('TODOS');
  const [soloPases, setSoloPases] = useState(false);
  const [cashSearch, setCashSearch] = useState('');
  const [cashDate, setCashDate] = useState(localDateForApi());
  const [viewer, setViewer] = useState(null);
  const [selectedDetail, setSelectedDetail] = useState(null);
  const [payment, setPayment] = useState({ estado_pago: 'PAGADO', metodo_pago: 'EFECTIVO', monto_pagado: '', observacion_pago: '' });
  const [internalObs, setInternalObs] = useState('');
  const [paymentBreakdown, setPaymentBreakdown] = useState(() => Object.fromEntries(PAY_METHODS.map((method) => [method, ''])));
  const [movement, setMovement] = useState({ tipo: 'INGRESO', detalle: '', monto: '' });
  const [receiptFiles, setReceiptFiles] = useState([]);
  const [saving, setSaving] = useState(false);
  const selected = docs.find((doc) => String(doc.id) === String(selectedId));
  const canEditDocuments = canEditProcessedDocuments(user);

  const load = async (date = cashDate, query = cashSearch) => {
    const params = {};
    const cleanQuery = String(query || '').trim();
    if (date && !cleanQuery) params.fecha = date;
    if (cleanQuery) params.q = cleanQuery;
    const [docRes, cajaRes] = await Promise.all([
      apiFetch('/documentos', { params }),
      apiFetch('/caja'),
    ]);
    const cleanDocs = asArray(docRes).filter((doc) => DOCS_CAJA.has(String(doc.tipo || '').toUpperCase()));
    setDocs(cleanDocs);
    setMovs(asArray(cajaRes));
    if (!cleanDocs.some((doc) => String(doc.id) === String(selectedId))) setSelectedId(cleanDocs[0] ? String(cleanDocs[0].id) : '');
  };
  const shiftCashDate = (days) => {
    const base = cashDate ? new Date(`${cashDate}T12:00:00`) : new Date();
    base.setDate(base.getDate() + days);
    const next = localDateForApi(base);
    setCashDate(next);
    load(next, '');
  };
  useEffect(() => { load(cashDate); }, []);
  useEffect(() => {
    if (!selected) return;
    const total = Number(selected.total || 0);
    const pagado = Number(selected.monto_pagado || 0);
    const details = Array.isArray(selected.pagos_detalle) ? selected.pagos_detalle : [];
    const nextBreakdown = Object.fromEntries(PAY_METHODS.map((method) => [method, '']));
    if (details.length) {
      details.forEach((item) => {
        const method = String(item.metodo || item.metodo_pago || '').toUpperCase();
        if (PAY_METHODS.includes(method)) nextBreakdown[method] = String(item.monto || item.monto_pagado || '');
      });
    } else {
      const method = String(selected.metodo_pago || 'EFECTIVO').toUpperCase();
      if (PAY_METHODS.includes(method) && pagado > 0) nextBreakdown[method] = String(pagado);
    }
    setPaymentBreakdown(nextBreakdown);
    setPayment({
      estado_pago: String(selected.estado_pago || 'PAGADO').toUpperCase(),
      metodo_pago: String(selected.metodo_pago || 'EFECTIVO').toUpperCase(),
      monto_pagado: String(pagado > 0 ? pagado : total),
      observacion_pago: selected.observacion_pago || '',
    });
    setReceiptFiles([]);
    setSelectedDetail(null);
    setInternalObs(selected.observacion_interna || '');
    apiFetch(`/documentos/${selected.id}`).then((detail) => {
      setSelectedDetail(detail);
      setInternalObs(detail?.documento?.observacion_interna || selected.observacion_interna || '');
    }).catch(() => setSelectedDetail(null));
  }, [selectedId]);

  const totals = useMemo(() => docs.reduce((acc, doc) => {
    const estado = String(doc.estado_pago || 'DEUDA').toUpperCase();
    const total = Number(doc.total || 0);
    if (estado === 'PAGADO') acc.pagado += total;
    else if (estado === 'CREDITO') acc.credito += total;
    else acc.deuda += total;
    return acc;
  }, { pagado: 0, credito: 0, deuda: 0 }), [docs]);

  const filteredDocs = docs.filter((doc) => {
    const matchesState = filter === 'TODOS' || String(doc.estado_pago || 'DEUDA').toUpperCase() === filter;
    const matchesPase = !soloPases || (String(doc.tipo || '').toUpperCase() === 'PASE' && doc.es_pase);
    const q = cashSearch.trim().toLowerCase();
    if (!matchesState || !matchesPase) return false;
    if (!q) return true;
    return [
      docNumber(doc),
      doc.tipo,
      doc.numero,
      doc.cliente_nombre,
      doc.documento_cliente,
      doc.total,
      doc.observacion,
      doc.observacion_pago,
      doc.observacion_interna,
      doc.detalle_busqueda,
    ].some((value) => String(value || '').toLowerCase().includes(q));
  });
  const previewLines = detailLinesFromPayload(selectedDetail || {});
  const paymentDetails = PAY_METHODS.map((method) => ({ metodo: method, monto: Number(paymentBreakdown[method] || 0) })).filter((item) => item.monto > 0);
  const paymentTotal = paymentDetails.reduce((sum, item) => sum + item.monto, 0);

  const applyPayment = async () => {
    if (!selected) return;
    setSaving(true);
    try {
      const receiptPayload = await filesToReceiptsPayload(receiptFiles);
      const res = await apiFetch(`/documentos/${selected.id}/estado-pago`, {
        method: 'PUT',
        json: {
          estado_pago: payment.estado_pago,
          metodo_pago: paymentDetails.length ? paymentDetails.map((item) => item.metodo).join(' + ') : (payment.estado_pago === 'PAGADO' ? payment.metodo_pago : ''),
          monto_pagado: paymentDetails.length ? paymentTotal : Number(payment.monto_pagado || 0),
          pagos_detalle: paymentDetails,
          observacion_pago: payment.observacion_pago,
          ...receiptPayload,
        },
      });
      if (okResponse(res)) {
        if (soundEnabled) await sound.play();
        setReceiptFiles([]);
        await load();
        alert('Pago actualizado.');
      } else alert(res.msg || 'No se pudo actualizar el pago.');
    } catch (error) {
      alert(error?.message || 'No se pudo actualizar el pago.');
    } finally {
      setSaving(false);
    }
  };

  const saveInternalObs = async () => {
    if (!selected) return;
    setSaving(true);
    try {
      const res = await apiFetch(`/documentos/${selected.id}/observacion-interna`, {
        method: 'PUT',
        json: { observacion_interna: internalObs },
      });
      if (okResponse(res)) {
        await load();
        alert('Observacion interna guardada.');
      } else alert(res.msg || 'No se pudo guardar la observacion interna.');
    } finally {
      setSaving(false);
    }
  };

  const openReceipt = async () => {
    if (!selected) return;
    const detail = await apiFetch(`/documentos/${selected.id}`);
    const doc = detail?.documento || detail || {};
    const receipts = receiptListFromDoc({ ...selected, ...doc }, detail);
    if (!receipts.length) {
      alert('Este documento aun no tiene comprobante adjunto.');
      return;
    }
    const buttons = receipts.map((r, idx) => `<button onclick="showReceipt(${idx})">${idx + 1}. ${escapeHtml(r.nombre || 'Comprobante')}</button>`).join('');
    const payload = JSON.stringify(receipts).replace(/</g, '\\u003c');
    setViewer({
      title: receipts.length > 1 ? `${receipts.length} comprobantes` : receipts[0].nombre || 'Comprobante',
      html: `<!doctype html><html><head><meta name="viewport" content="width=device-width,initial-scale=1"><style>body{margin:0;background:#eef3f8;font-family:Arial}.bar{position:sticky;top:0;background:#fff;border-bottom:1px solid #d8e2ee;padding:8px;display:flex;gap:6px;overflow:auto}.bar button{border:0;border-radius:6px;background:#eef2ff;color:#3730a3;font-weight:800;padding:9px 10px}.wrap{width:100%;height:calc(100vh - 54px);display:grid;place-items:center}img{max-width:100%;max-height:100%;object-fit:contain}iframe{border:0;width:100%;height:100%}</style></head><body><div class="bar">${buttons}</div><div id="wrap" class="wrap"></div><script>const receipts=${payload};function showReceipt(i){const r=receipts[i]||{};let src=r.data_url||'';if(!src&&r.base64){src='data:'+(r.mime||'application/octet-stream')+';base64,'+r.base64}const isImage=/^data:image\\//i.test(src);document.getElementById('wrap').innerHTML=isImage?'<img src="'+src+'" alt="Comprobante">':'<iframe src="'+src+'" title="Comprobante"></iframe>';}showReceipt(0);</script></body></html>`,
    });
  };

  const openInternalDoc = async () => {
    if (!selected) return;
    try {
      setViewer({ title: 'Cargando documento...', html: '<p style="font-family:Arial;padding:20px">Cargando...</p>' });
      setViewer(await buildDocumentViewer(selected));
    } catch (error) {
      setViewer(null);
      alert(error?.message || 'No se pudo abrir el documento.');
    }
  };
  const annulSelected = async () => {
    if (!selected || !confirm(`Anular ${selected.tipo} ${docNumber(selected)} y restaurar stock?`)) return;
    const motivo = prompt('Motivo de anulacion', 'Anulado desde caja') || 'Anulado desde caja';
    const res = await apiFetch(`/documentos/${selected.id}/anular`, { method: 'POST', json: { usuario: user || 'CAJA', motivo } });
    if (okResponse(res)) {
      if (soundEnabled) await sound.play();
      await load(cashDate, cashSearch);
      alert(res.msg || 'Documento anulado.');
    } else alert(res.msg || 'No se pudo anular.');
  };

  const editSelectedDocument = () => {
    if (!selected) return;
    if (!canEditDocuments) {
      alert('Solo Giomar puede cambiar boletas/facturas ya procesadas.');
      return;
    }
    if (String(selected.estado || '').toUpperCase() === 'ANULADO') {
      alert('No se puede editar un documento anulado.');
      return;
    }
    localStorage.setItem('gg_erp_pending_document_edit', String(selected.id));
    setView?.('documentos');
  };

  const saveMovement = async () => {
    if (!movement.detalle || !movement.monto) return alert('Completa detalle y monto.');
    setSaving(true);
    try {
      const res = await apiFetch('/caja', { method: 'POST', json: { ...movement, monto: Number(movement.monto || 0) } });
      if (okResponse(res)) {
        if (soundEnabled) await sound.play();
        setMovement({ tipo: 'INGRESO', detalle: '', monto: '' });
        await load();
      } else alert(res.msg || 'No se pudo guardar.');
    } finally {
      setSaving(false);
    }
  };

  return (
    <div className="grid gap-4">
      <div className="grid grid-cols-2 gap-3 lg:grid-cols-4">
        <Stat label="Pagado" value={money(totals.pagado)} tone="green" />
        <Stat label="Credito" value={money(totals.credito)} tone="amber" />
        <Stat label="Deuda" value={money(totals.deuda)} tone="red" />
        <Stat label="Documentos" value={docs.length} tone="blue" />
      </div>
      <div className="grid gap-4 xl:grid-cols-[1fr_430px]">
        <Card title="Caja / Boletas y facturas" actions={<><TextInput placeholder="Buscar producto en todas las fechas..." value={cashSearch} onChange={(e) => setCashSearch(e.target.value)} onKeyDown={(e) => e.key === 'Enter' && load(cashDate, e.currentTarget.value)} className="w-72" /><ActionButton tone="blue" onClick={() => load(cashDate, cashSearch)}>Buscar</ActionButton><ActionButton tone="neutral" onClick={() => shiftCashDate(-1)}>‹</ActionButton><TextInput type="date" value={cashDate} onChange={(e) => { setCashDate(e.target.value); load(e.target.value, ''); }} className="w-36" /><ActionButton tone="neutral" onClick={() => shiftCashDate(1)}>›</ActionButton><SelectInput value={filter} onChange={(e) => setFilter(e.target.value)} className="w-36"><option>TODOS</option>{PAY_STATES.map((s) => <option key={s}>{s}</option>)}</SelectInput><label className="flex items-center gap-1 text-xs font-black"><input type="checkbox" checked={soloPases} onChange={(e) => setSoloPases(e.target.checked)} /> Solo PASEs</label><ActionButton tone="blue" onClick={() => { const today = localDateForApi(); setCashDate(today); load(today, ''); }}>Hoy</ActionButton><ActionButton tone="neutral" onClick={() => { setCashSearch(''); load(cashDate, ''); }}>Limpiar</ActionButton><ActionButton tone="neutral" onClick={() => load(cashDate, cashSearch)}>Actualizar</ActionButton></>}>
          <div className="max-h-[560px] overflow-auto">
            {filteredDocs.map((doc) => {
              const estado = String(doc.estado_pago || 'DEUDA').toUpperCase();
              return (
                <button key={doc.id} onClick={() => setSelectedId(String(doc.id))} onDoubleClick={async () => { setSelectedId(String(doc.id)); try { setViewer({ title: 'Cargando documento...', html: '<p style="font-family:Arial;padding:20px">Cargando...</p>' }); setViewer(await buildDocumentViewer(doc)); } catch (error) { setViewer(null); alert(error?.message || 'No se pudo abrir el documento.'); } }} className={`grid w-full gap-2 border-b border-slate-100 p-3 text-left md:grid-cols-[120px_1fr_120px_100px] ${String(selectedId) === String(doc.id) ? 'bg-teal-50' : 'bg-white'}`}>
                  <div className="font-black">{docNumber(doc)}</div>
                  <div>
                    <div className="font-semibold">{doc.cliente_nombre || 'Cliente general'}</div>
                    <div className="text-xs font-black text-slate-700">Fecha: {String(doc.fecha_emision || doc.fecha || '').slice(0, 10)}</div>
                    <div className="text-xs text-slate-500">{doc.tipo} / {doc.fecha_emision || doc.fecha || ''} {doc.tipo === 'PASE' && doc.es_pase && <span className="ml-1 px-1 bg-purple-200 text-purple-800 text-[10px] rounded">PASE</span>}</div>
                    {(doc.observacion_pago || doc.observacion) && <div className="mt-1 line-clamp-1 text-xs font-bold text-blue-700">Obs: {doc.observacion_pago || doc.observacion}</div>}
                    {doc.observacion_interna && <div className="mt-1 line-clamp-1 text-xs font-bold text-violet-700">Interna: {doc.observacion_interna}</div>}
                  </div>
                  <div className="font-black">{money(doc.total)}</div>
                  <span className={`h-7 rounded-md px-2 py-1 text-center text-xs font-black ${estado === 'PAGADO' ? 'bg-emerald-100 text-emerald-700' : estado === 'CREDITO' ? 'bg-amber-100 text-amber-700' : 'bg-red-100 text-red-700'}`}>{estado}</span>
                </button>
              );
            })}
            {!filteredDocs.length && <Empty />}
          </div>
        </Card>
        <aside className="grid gap-4">
          <Card title="Aplicar pago">
            {selected ? (
              <div className="grid gap-3">
                <div className="rounded-md bg-slate-50 p-3 text-sm">
                  <div className="font-black">{selected.tipo} {docNumber(selected)}</div>
                  <div className="text-slate-500">{selected.cliente_nombre}</div>
                  {selected.observacion && <div className="mt-1 text-xs font-bold text-slate-600">Doc: {selected.observacion}</div>}
                  {selected.observacion_pago && <div className="mt-1 text-xs font-bold text-blue-700">Caja: {selected.observacion_pago}</div>}
                  {selected.observacion_interna && <div className="mt-1 text-xs font-bold text-violet-700">Interna: {selected.observacion_interna}</div>}
                  <div className="mt-1 font-black">{money(selected.total)}</div>
                </div>
                {canEditDocuments && <ActionButton tone="violet" disabled={saving || String(selected.estado || '').toUpperCase() === 'ANULADO'} onClick={editSelectedDocument}>Cambiar boleta / productos / series</ActionButton>}
                <ActionButton tone="danger" disabled={saving || String(selected.estado || '').toUpperCase() === 'ANULADO'} onClick={annulSelected}>Anular boleta y restaurar stock</ActionButton>
                <div className="rounded-md border border-violet-100 bg-violet-50 p-2">
                  <Field label="Observacion interna">
                    <TextArea value={internalObs} onChange={(e) => setInternalObs(e.target.value)} className="min-h-24" placeholder="Nota interna de esta boleta/factura. No sale en PDF." />
                  </Field>
                  <ActionButton tone="violet" disabled={saving} onClick={saveInternalObs}>Guardar nota interna</ActionButton>
                </div>
                <div className="grid grid-cols-2 gap-2">
                  <Field label="Estado"><SelectInput value={payment.estado_pago} onChange={(e) => setPayment({ ...payment, estado_pago: e.target.value })}>{PAY_STATES.map((s) => <option key={s}>{s}</option>)}</SelectInput></Field>
                  <Field label="Metodo"><SelectInput value={payment.metodo_pago} onChange={(e) => setPayment({ ...payment, metodo_pago: e.target.value })}>{PAY_METHODS.map((m) => <option key={m}>{m}</option>)}</SelectInput></Field>
                </div>
                <div className="rounded-md border border-slate-200 p-2">
                  <div className="mb-2 text-xs font-black text-slate-500">Montos por metodo</div>
                  <div className="grid grid-cols-2 gap-2">
                    {PAY_METHODS.map((method) => <Field key={method} label={method}><TextInput inputMode="decimal" value={paymentBreakdown[method] || ''} onChange={(e) => setPaymentBreakdown({ ...paymentBreakdown, [method]: e.target.value })} /></Field>)}
                  </div>
                  <div className="mt-2 text-right text-sm font-black text-slate-800">Total pagado: {money(paymentTotal)}</div>
                </div>
                <Field label="Monto pagado manual"><TextInput inputMode="decimal" value={payment.monto_pagado} onChange={(e) => setPayment({ ...payment, monto_pagado: e.target.value })} /></Field>
                <Field label="Observacion"><TextInput value={payment.observacion_pago} onChange={(e) => setPayment({ ...payment, observacion_pago: e.target.value })} /></Field>
                <Field label="Comprobantes PNG/JPG/PDF">
                  <input type="file" multiple accept="image/*,.pdf" onChange={(e) => setReceiptFiles(Array.from(e.target.files || []))} className="min-h-11 w-full rounded-md border border-slate-200 bg-white px-3 py-2 text-sm file:mr-3 file:rounded-md file:border-0 file:bg-slate-100 file:px-3 file:py-2 file:text-xs file:font-black" />
                </Field>
                {(receiptFiles.length > 0 || receiptListFromDoc(selected, selectedDetail).length > 0) && (
                  <div className="rounded-md bg-violet-50 px-3 py-2 text-xs font-bold text-violet-700">
                    {receiptFiles.length ? receiptFiles.map((f) => f.name).join(' / ') : `${receiptListFromDoc(selected, selectedDetail).length} comprobante(s) guardado(s)`}
                  </div>
                )}
                <div className="grid grid-cols-3 gap-2">
                  <ActionButton disabled={saving} onClick={applyPayment}>Aprobar</ActionButton>
                  <ActionButton tone="blue" onClick={openInternalDoc}>Ver PDF</ActionButton>
                  <ActionButton tone="neutral" onClick={openReceipt}>Comprobante</ActionButton>
                </div>
              </div>
            ) : <Empty text="Selecciona un documento." />}
          </Card>
          <Card title="Ingreso / egreso manual">
            <div className="grid gap-3">
              <Field label="Tipo"><SelectInput value={movement.tipo} onChange={(e) => setMovement({ ...movement, tipo: e.target.value })}><option>INGRESO</option><option>EGRESO</option></SelectInput></Field>
              <Field label="Detalle"><TextInput value={movement.detalle} onChange={(e) => setMovement({ ...movement, detalle: e.target.value })} /></Field>
              <Field label="Monto"><TextInput inputMode="decimal" value={movement.monto} onChange={(e) => setMovement({ ...movement, monto: e.target.value })} /></Field>
              <ActionButton tone={movement.tipo === 'EGRESO' ? 'danger' : 'primary'} disabled={saving} onClick={saveMovement}>Guardar movimiento</ActionButton>
            </div>
          </Card>
          <Card title="Vista previa de boleta">
            {selected ? (
              <div className="grid gap-3 text-sm">
                <div className="rounded-md border border-slate-200 bg-white p-3">
                  <div className="flex items-start justify-between gap-3">
                    <div><b>{selected.tipo} {docNumber(selected)}</b><div className="text-xs text-slate-500">{selected.fecha_emision || selected.fecha || ''}</div></div>
                    <b className="text-blue-700">{money(selected.total)}</b>
                  </div>
                  <div className="mt-2 text-xs text-slate-600">{selected.cliente_nombre || 'Cliente general'}</div>
                  {(selected.observacion_pago || selected.observacion) && <div className="mt-2 text-xs font-bold text-blue-700">Obs: {selected.observacion_pago || selected.observacion}</div>}
                  {selected.observacion_interna && <div className="mt-2 text-xs font-bold text-violet-700">Interna: {selected.observacion_interna}</div>}
                </div>
                <div className="max-h-72 overflow-auto rounded-md border border-slate-200">
                  {previewLines.slice(0, 8).map((line, idx) => (
                    <div key={`${line.id || idx}`} className="grid grid-cols-[1fr_46px_72px] gap-2 border-b border-slate-100 p-2 text-xs">
                      <span className="font-bold text-slate-800">{line.descripcion || line.nombre || 'Producto'}</span>
                      <span className="text-right">{line.cantidad || 0}</span>
                      <b className="text-right">{money(line.total)}</b>
                    </div>
                  ))}
                  {!previewLines.length && <Empty text="Selecciona una boleta para ver detalle." />}
                </div>
                <div className="grid grid-cols-2 gap-2">
                  <ActionButton tone="blue" onClick={openInternalDoc}>Abrir documento</ActionButton>
                  <ActionButton tone="neutral" onClick={openReceipt}>Comprobantes</ActionButton>
                </div>
              </div>
            ) : <Empty text="Selecciona un documento." />}
          </Card>
        </aside>
      </div>
      <DocumentViewerModal viewer={viewer} onClose={() => setViewer(null)} />
    </div>
  );
}

function ReservasView({ user, setView }) {
  const emptyForm = {
    tipo_documento: 'DNI',
    numero_documento: '',
    cliente_nombre: '',
    producto_id: '',
    producto_nombre: '',
    cantidad: 1,
    monto_total: '',
    monto_reserva: '',
    observacion: '',
  };
  const [form, setForm] = useState(emptyForm);
  const [products, setProducts] = useState([]);
  const [productQuery, setProductQuery] = useState('');
  const [documentQuery, setDocumentQuery] = useState('');
  const [reservas, setReservas] = useState([]);
  const [documentos, setDocumentos] = useState([]);
  const [estadoFilter, setEstadoFilter] = useState('TODOS');
  const [saving, setSaving] = useState(false);
  const [receiptFiles, setReceiptFiles] = useState([]);
  const [viewer, setViewer] = useState(null);
  const [uploadingReservaId, setUploadingReservaId] = useState('');
  const [editingReserva, setEditingReserva] = useState(null);
  const [editingFiles, setEditingFiles] = useState([]);

  const loadProducts = async () => setProducts(asArray(await apiFetch('/productos')));
  const loadReservas = async (doc = documentQuery) => {
    const res = await apiFetch('/reservas', { params: { documento: doc, estado: estadoFilter } });
    setReservas(asArray(res));
    setDocumentos(asArray(res?.documentos));
  };
  useEffect(() => { loadProducts(); loadReservas(''); }, []);
  useEffect(() => { loadReservas(documentQuery); }, [estadoFilter]);

  const lookupClient = async () => {
    const doc = documentDigits(documentQuery || form.numero_documento);
    if (!doc) return;
    const res = await lookupDocumentoApi(doc);
    if (res.found || res.ok) {
      const next = {
        ...form,
        tipo_documento: res.tipo_documento || (doc.length === 11 ? 'RUC' : 'DNI'),
        numero_documento: res.numero_documento || doc,
        cliente_nombre: res.nombre || res.razon_social || form.cliente_nombre || '',
      };
      setForm(next);
      setDocumentQuery(next.numero_documento);
      await loadReservas(next.numero_documento);
    } else {
      setForm({ ...form, numero_documento: doc });
      setDocumentQuery(doc);
      await loadReservas(doc);
      alert(res.msg || 'No se encontraron datos, puedes registrar el nombre manual.');
    }
  };

  const pickProduct = (p) => {
    const price = Number(p.precio_venta || p.precio || 0);
    const qty = Number(form.cantidad || 1);
    setForm({
      ...form,
      producto_id: p.id,
      producto_nombre: p.nombre || '',
      monto_total: price ? String((price * qty).toFixed(2)) : form.monto_total,
    });
  };

  const save = async () => {
    setSaving(true);
    try {
      const receiptPayload = await filesToReceiptsPayload(receiptFiles);
      const res = await apiFetch('/reservas', {
        method: 'POST',
        json: {
          ...form,
          producto_id: form.producto_id ? Number(form.producto_id) : null,
          cantidad: Number(form.cantidad || 1),
          monto_total: Number(form.monto_total || 0),
          monto_reserva: Number(form.monto_reserva || 0),
          usuario: user || '',
          ...receiptPayload,
        },
      });
      if (okResponse(res)) {
        const keepDoc = form.numero_documento;
        setReceiptFiles([]);
        setForm({ ...emptyForm, numero_documento: keepDoc, cliente_nombre: form.cliente_nombre, tipo_documento: form.tipo_documento });
        await loadReservas(keepDoc);
      } else {
        alert(res.msg || 'No se pudo guardar la reserva.');
      }
    } catch (error) {
      alert(error?.message || 'No se pudo guardar la reserva.');
    } finally {
      setSaving(false);
    }
  };

  const updateReserva = async (row, estado) => {
    const res = await apiFetch(`/reservas/${row.id}`, { method: 'PUT', json: { estado } });
    if (okResponse(res)) await loadReservas(documentQuery);
    else alert(res.msg || 'No se pudo actualizar la reserva.');
  };

  const openEditReserva = (row) => {
    setEditingFiles([]);
    setEditingReserva({
      id: row.id,
      tipo_documento: row.tipo_documento || 'DNI',
      numero_documento: row.numero_documento || '',
      cliente_nombre: row.cliente_nombre || '',
      producto_id: row.producto_id || '',
      producto_nombre: row.producto_nombre || '',
      cantidad: row.cantidad || 1,
      monto_total: row.monto_total ?? '',
      monto_reserva: row.monto_reserva ?? '',
      estado: row.estado || 'RESERVADO',
      observacion: row.observacion || '',
    });
  };

  const saveEditReserva = async () => {
    if (!editingReserva?.id) return;
    try {
      const receiptPayload = await filesToReceiptsPayload(editingFiles);
      const res = await apiFetch(`/reservas/${editingReserva.id}`, {
        method: 'PUT',
        json: {
          ...editingReserva,
          producto_id: editingReserva.producto_id ? Number(editingReserva.producto_id) : null,
          cantidad: Number(editingReserva.cantidad || 1),
          monto_total: Number(editingReserva.monto_total || 0),
          monto_reserva: Number(editingReserva.monto_reserva || 0),
          ...receiptPayload,
        },
      });
      if (okResponse(res)) {
        setEditingReserva(null);
        setEditingFiles([]);
        await loadReservas(documentQuery);
      } else {
        alert(res.msg || 'No se pudo editar la reserva.');
      }
    } catch (error) {
      alert(error?.message || 'No se pudo editar la reserva.');
    }
  };

  const uploadReservaReceipt = async (row, files) => {
    const selectedFiles = Array.from(files || []);
    if (!row?.id || !selectedFiles.length) return;
    setUploadingReservaId(String(row.id));
    try {
      const receiptPayload = await filesToReceiptsPayload(selectedFiles);
      const res = await apiFetch(`/reservas/${row.id}`, {
        method: 'PUT',
        json: {
          estado: row.estado || 'RESERVADO',
          ...receiptPayload,
        },
      });
      if (okResponse(res)) {
        await loadReservas(documentQuery);
        alert('Comprobante agregado a la reserva.');
      } else {
        alert(res.msg || 'No se pudo subir el comprobante.');
      }
    } catch (error) {
      alert(error?.message || 'No se pudo subir el comprobante.');
    } finally {
      setUploadingReservaId('');
    }
  };

  const openReservaReceipts = async (row) => {
    if (!row?.id) return;
    const detail = await apiFetch(`/reservas/${row.id}`);
    const reserva = detail?.reserva || detail?.data || {};
    const receipts = receiptListFromDoc({ ...row, ...reserva }, detail);
    if (!receipts.length) {
      alert('Esta reserva aun no tiene comprobantes.');
      return;
    }
    const buttons = receipts.map((r, idx) => `<button onclick="showReceipt(${idx})">${idx + 1}. ${escapeHtml(r.nombre || 'Comprobante')}</button>`).join('');
    const payload = JSON.stringify(receipts).replace(/</g, '\\u003c');
    setViewer({
      title: receipts.length > 1 ? `${receipts.length} comprobantes de reserva` : receipts[0].nombre || 'Comprobante',
      html: `<!doctype html><html><head><meta name="viewport" content="width=device-width,initial-scale=1"><style>body{margin:0;background:#eef3f8;font-family:Arial}.bar{position:sticky;top:0;background:#fff;border-bottom:1px solid #d8e2ee;padding:8px;display:flex;gap:6px;overflow:auto}.bar button{border:0;border-radius:6px;background:#eef2ff;color:#3730a3;font-weight:800;padding:9px 10px}.wrap{width:100%;height:calc(100vh - 54px);display:grid;place-items:center}img{max-width:100%;max-height:100%;object-fit:contain}iframe{border:0;width:100%;height:100%}</style></head><body><div class="bar">${buttons}</div><div id="wrap" class="wrap"></div><script>const receipts=${payload};function showReceipt(i){const r=receipts[i]||{};let src=r.data_url||'';if(!src&&r.base64){src='data:'+(r.mime||'application/octet-stream')+';base64,'+r.base64}const isImage=/^data:image\\//i.test(src);document.getElementById('wrap').innerHTML=isImage?'<img src="'+src+'" alt="Comprobante">':'<iframe src="'+src+'" title="Comprobante"></iframe>';}showReceipt(0);</script></body></html>`,
    });
  };

  const filteredProducts = filterProductsBySearch(products, productQuery, { limit: 40 });
  const totalReservado = reservas
    .filter((r) => !['ANULADO', 'ENTREGADO'].includes(String(r.estado || '').toUpperCase()))
    .reduce((sum, r) => sum + Number(r.monto_reserva || 0), 0);
  const totalSaldo = reservas
    .filter((r) => !['ANULADO', 'ENTREGADO'].includes(String(r.estado || '').toUpperCase()))
    .reduce((sum, r) => sum + Number(r.saldo || 0), 0);

  return (
    <div className="grid gap-4 xl:grid-cols-[430px_minmax(0,1fr)]">
      <div className="grid gap-4">
        <Card title="Nueva reserva">
          <div className="grid gap-3">
            <div className="grid grid-cols-[110px_1fr_auto] gap-2">
              <SelectInput value={form.tipo_documento} onChange={(e) => setForm({ ...form, tipo_documento: e.target.value })}>
                <option>DNI</option>
                <option>RUC</option>
                <option>CE</option>
              </SelectInput>
              <TextInput placeholder="DNI/RUC" value={form.numero_documento} onChange={(e) => { setForm({ ...form, numero_documento: e.target.value }); setDocumentQuery(e.target.value); }} />
              <ActionButton tone="blue" onClick={lookupClient}>Buscar</ActionButton>
            </div>
            <Field label="Cliente"><TextInput value={form.cliente_nombre} onChange={(e) => setForm({ ...form, cliente_nombre: e.target.value })} /></Field>
            <Field label="Producto reservado"><TextInput value={form.producto_nombre} onChange={(e) => setForm({ ...form, producto_nombre: e.target.value, producto_id: '' })} /></Field>
            <div className="grid grid-cols-3 gap-2">
              <Field label="Cantidad"><TextInput inputMode="numeric" value={form.cantidad} onChange={(e) => setForm({ ...form, cantidad: e.target.value })} /></Field>
              <Field label="Total venta"><TextInput inputMode="decimal" value={form.monto_total} onChange={(e) => setForm({ ...form, monto_total: e.target.value })} /></Field>
              <Field label="Separado"><TextInput inputMode="decimal" value={form.monto_reserva} onChange={(e) => setForm({ ...form, monto_reserva: e.target.value })} /></Field>
            </div>
            <Field label="Observacion"><TextArea value={form.observacion} onChange={(e) => setForm({ ...form, observacion: e.target.value })} /></Field>
            <Field label="Comprobantes PNG/JPG/PDF">
              <input type="file" multiple accept="image/*,.pdf" onChange={(e) => setReceiptFiles(Array.from(e.target.files || []))} className="min-h-11 w-full rounded-md border border-slate-200 bg-white px-3 py-2 text-sm file:mr-3 file:rounded-md file:border-0 file:bg-slate-100 file:px-3 file:py-2 file:text-xs file:font-black" />
            </Field>
            {receiptFiles.length > 0 && <div className="rounded-md bg-violet-50 px-3 py-2 text-xs font-bold text-violet-700">{receiptFiles.map((f) => f.name).join(' / ')}</div>}
            <ActionButton disabled={saving} onClick={save}>{saving ? 'Guardando...' : 'Guardar reserva'}</ActionButton>
          </div>
        </Card>
        <Card title="Buscar producto">
          <div className="grid gap-3">
            <TextInput placeholder="Buscar producto..." value={productQuery} onChange={(e) => setProductQuery(e.target.value)} />
            <div className="max-h-72 overflow-auto rounded-md border border-slate-100">
              {filteredProducts.map((p) => (
                <button key={p.id} onClick={() => pickProduct(p)} className="grid w-full grid-cols-[1fr_92px_82px] gap-2 border-b border-slate-100 p-2 text-left text-xs hover:bg-slate-50">
                  <span className="font-black uppercase text-slate-900">{p.nombre}</span>
                  <b className="text-right text-emerald-700">{money(p.precio_venta || p.precio)}</b>
                  <span className="rounded bg-amber-50 px-2 py-1 text-center font-black text-amber-700">Stock {p.stock || 0}</span>
                </button>
              ))}
              {!filteredProducts.length && <Empty text="Escribe para buscar un producto." />}
            </div>
          </div>
        </Card>
      </div>

      <div className="grid gap-4">
        <div className="grid grid-cols-1 gap-3 md:grid-cols-3">
          <Stat label="Separado activo" value={money(totalReservado)} tone="green" />
          <Stat label="Saldo pendiente" value={money(totalSaldo)} tone="red" />
          <Stat label="Reservas" value={reservas.length} tone="blue" />
        </div>
        <Card title="Reservas por cliente" actions={<><TextInput placeholder="DNI/RUC" value={documentQuery} onChange={(e) => setDocumentQuery(e.target.value)} className="w-40" /><SelectInput value={estadoFilter} onChange={(e) => setEstadoFilter(e.target.value)} className="w-36"><option>TODOS</option><option>RESERVADO</option><option>PAGADO</option><option>ENTREGADO</option><option>ANULADO</option></SelectInput><ActionButton tone="blue" onClick={() => loadReservas(documentQuery)}>Buscar</ActionButton></>}>
          <div className="max-h-[390px] overflow-auto">
            {reservas.map((r) => {
              const estado = String(r.estado || 'RESERVADO').toUpperCase();
              return (
                <div key={r.id} className="grid gap-2 border-b border-slate-100 p-3 text-sm xl:grid-cols-[1fr_120px_120px_210px]">
                  <div className="min-w-0">
                    <b className="block truncate uppercase">{r.producto_nombre}</b>
                    <span className="text-xs text-slate-500">{r.fecha} / {r.cliente_nombre} / {r.numero_documento}</span>
                    {r.observacion && <div className="mt-1 text-xs text-slate-500">{r.observacion}</div>}
                    {!!Number(r.comprobantes_pago_count || 0) && <div className="mt-1 text-xs font-black text-violet-700">{r.comprobantes_pago_count} comprobante(s)</div>}
                  </div>
                  <div><span className="text-xs text-slate-500">Separado</span><b className="block">{money(r.monto_reserva)}</b></div>
                  <div><span className="text-xs text-slate-500">Saldo</span><b className="block text-red-700">{money(r.saldo)}</b></div>
                  <div className="grid grid-cols-2 gap-1">
                    <span className={`col-span-2 rounded-md px-2 py-1 text-center text-xs font-black ${estado === 'RESERVADO' ? 'bg-amber-100 text-amber-700' : estado === 'ANULADO' ? 'bg-red-100 text-red-700' : 'bg-emerald-100 text-emerald-700'}`}>{estado}</span>
                    <button onClick={() => updateReserva(r, 'ENTREGADO')} className="rounded bg-emerald-600 px-2 py-1 text-xs font-black text-white">Entregar</button>
                    <button onClick={() => updateReserva(r, 'ANULADO')} className="rounded bg-red-600 px-2 py-1 text-xs font-black text-white">Anular</button>
                    <button onClick={() => openEditReserva(r)} className="col-span-2 rounded bg-blue-600 px-2 py-1 text-xs font-black text-white">Editar reserva</button>
                    <label className="col-span-2 cursor-pointer rounded bg-violet-600 px-2 py-1 text-center text-xs font-black text-white">
                      {String(uploadingReservaId) === String(r.id) ? 'Subiendo...' : '+ Comprobante'}
                      <input type="file" multiple accept="image/*,.pdf" className="hidden" onChange={(e) => { uploadReservaReceipt(r, e.target.files); e.target.value = ''; }} />
                    </label>
                    <button onClick={() => openReservaReceipts(r)} className="col-span-2 rounded bg-slate-100 px-2 py-1 text-xs font-black text-slate-700">Ver comprobantes</button>
                  </div>
                </div>
              );
            })}
            {!reservas.length && <Empty text="Busca un DNI/RUC o registra una reserva." />}
          </div>
        </Card>
        <Card title="Boletas y separaciones del cliente" actions={<ActionButton tone="neutral" onClick={() => setView?.('caja')}>Ir a caja</ActionButton>}>
          <div className="max-h-72 overflow-auto">
            {documentos.map((d) => (
              <div key={d.id} className="grid grid-cols-[120px_1fr_110px_90px] gap-2 border-b border-slate-100 p-2 text-sm">
                <b>{d.tipo} {d.numero}</b>
                <span className="truncate">{d.fecha} / {d.cliente_nombre}</span>
                <b className="text-right">{money(d.total)}</b>
                <span className="text-xs font-black">{d.estado_pago}</span>
              </div>
            ))}
            {!documentos.length && <Empty text="Al buscar DNI/RUC veras aqui sus boletas y facturas." />}
          </div>
        </Card>
      </div>
      {editingReserva && (
        <Modal title={`Editar reserva #${editingReserva.id}`} onClose={() => setEditingReserva(null)}>
          <div className="grid gap-3">
            <div className="grid grid-cols-[110px_1fr] gap-2">
              <Field label="Tipo"><SelectInput value={editingReserva.tipo_documento} onChange={(e) => setEditingReserva({ ...editingReserva, tipo_documento: e.target.value })}><option>DNI</option><option>RUC</option><option>CE</option></SelectInput></Field>
              <Field label="Documento"><TextInput value={editingReserva.numero_documento} onChange={(e) => setEditingReserva({ ...editingReserva, numero_documento: e.target.value })} /></Field>
            </div>
            <Field label="Cliente"><TextInput value={editingReserva.cliente_nombre} onChange={(e) => setEditingReserva({ ...editingReserva, cliente_nombre: e.target.value })} /></Field>
            <Field label="Producto reservado"><TextInput value={editingReserva.producto_nombre} onChange={(e) => setEditingReserva({ ...editingReserva, producto_nombre: e.target.value, producto_id: '' })} /></Field>
            <div className="grid grid-cols-3 gap-2">
              <Field label="Cantidad"><TextInput inputMode="numeric" value={editingReserva.cantidad} onChange={(e) => setEditingReserva({ ...editingReserva, cantidad: e.target.value })} /></Field>
              <Field label="Total venta"><TextInput inputMode="decimal" value={editingReserva.monto_total} onChange={(e) => setEditingReserva({ ...editingReserva, monto_total: e.target.value })} /></Field>
              <Field label="Separado"><TextInput inputMode="decimal" value={editingReserva.monto_reserva} onChange={(e) => setEditingReserva({ ...editingReserva, monto_reserva: e.target.value })} /></Field>
            </div>
            <Field label="Estado"><SelectInput value={editingReserva.estado} onChange={(e) => setEditingReserva({ ...editingReserva, estado: e.target.value })}><option>RESERVADO</option><option>PAGADO</option><option>ENTREGADO</option><option>ANULADO</option></SelectInput></Field>
            <Field label="Observacion"><TextArea value={editingReserva.observacion} onChange={(e) => setEditingReserva({ ...editingReserva, observacion: e.target.value })} /></Field>
            <Field label="Agregar comprobantes PNG/JPG/PDF">
              <input type="file" multiple accept="image/*,.pdf" onChange={(e) => setEditingFiles(Array.from(e.target.files || []))} className="min-h-11 w-full rounded-md border border-slate-200 bg-white px-3 py-2 text-sm file:mr-3 file:rounded-md file:border-0 file:bg-slate-100 file:px-3 file:py-2 file:text-xs file:font-black" />
            </Field>
            {editingFiles.length > 0 && <div className="rounded-md bg-violet-50 px-3 py-2 text-xs font-bold text-violet-700">{editingFiles.map((f) => f.name).join(' / ')}</div>}
            <div className="grid grid-cols-2 gap-2">
              <ActionButton tone="neutral" onClick={() => setEditingReserva(null)}>Cancelar</ActionButton>
              <ActionButton onClick={saveEditReserva}>Guardar cambios</ActionButton>
            </div>
          </div>
        </Modal>
      )}
      <DocumentViewerModal viewer={viewer} onClose={() => setViewer(null)} />
    </div>
  );
}

function ClientesView() {
  const [clients, setClients] = useState([]);
  const [doc, setDoc] = useState('');
  const [form, setForm] = useState({ tipo_documento: 'DNI', numero_documento: '', nombre: '', direccion: '' });
  const [query, setQuery] = useState('');
  const load = async () => setClients(asArray(await apiFetch('/clientes')));
  useEffect(() => { load(); }, []);
  const lookup = async () => {
    const numero = documentDigits(doc);
    if (!numero) return;
    const res = await lookupDocumentoApi(numero);
    if (res.found || res.ok) {
      setForm({
        tipo_documento: res.tipo_documento || (numero.length === 11 ? 'RUC' : 'DNI'),
        numero_documento: res.numero_documento || numero,
        nombre: res.nombre || res.razon_social || '',
        direccion: res.direccion || '',
      });
      setDoc(numero);
    } else {
      alert(res.msg || 'Sin datos.');
    }
  };
  const save = async () => {
    const res = await apiFetch('/clientes', { method: 'POST', json: form });
    if (okResponse(res)) { setForm({ tipo_documento: 'DNI', numero_documento: '', nombre: '', direccion: '' }); await load(); } else alert(res.msg || 'No se pudo guardar.');
  };
  const filtered = clients.filter((c) => `${c.nombre} ${c.numero_documento}`.toLowerCase().includes(query.toLowerCase()));
  return (
    <div className="grid gap-4 xl:grid-cols-[420px_1fr]">
      <Card title="Cliente">
        <div className="grid gap-3">
          <div className="grid grid-cols-[1fr_auto] gap-2"><Field label="DNI/RUC"><TextInput value={doc} onChange={(e) => setDoc(e.target.value)} /></Field><ActionButton className="self-end" tone="blue" onClick={lookup}>Buscar</ActionButton></div>
          <Field label="Tipo"><SelectInput value={form.tipo_documento} onChange={(e) => setForm({ ...form, tipo_documento: e.target.value })}><option>DNI</option><option>RUC</option><option>CE</option></SelectInput></Field>
          <Field label="Numero"><TextInput value={form.numero_documento} onChange={(e) => setForm({ ...form, numero_documento: e.target.value })} /></Field>
          <Field label="Nombre"><TextInput value={form.nombre} onChange={(e) => setForm({ ...form, nombre: e.target.value })} /></Field>
          <Field label="Dirección"><TextInput value={form.direccion} onChange={(e) => setForm({ ...form, direccion: e.target.value })} /></Field>
          <ActionButton onClick={save}>Guardar cliente</ActionButton>
        </div>
      </Card>
      <Card title="Clientes" actions={<><TextInput placeholder="Buscar..." value={query} onChange={(e) => setQuery(e.target.value)} className="w-56" /><ActionButton tone="neutral" onClick={load}>Actualizar</ActionButton></>}>
        <div className="max-h-[680px] overflow-auto">{filtered.map((c) => <button key={c.id} onClick={() => setForm({ tipo_documento: c.tipo_documento || 'DNI', numero_documento: c.numero_documento || '', nombre: c.nombre || '', direccion: c.direccion || '' })} className="grid w-full gap-1 border-b border-slate-100 p-3 text-left"><b>{c.nombre}</b><span className="text-xs text-slate-500">{c.tipo_documento} {c.numero_documento} / {c.direccion}</span></button>)}</div>
      </Card>
    </div>
  );
}

function ProductosView({ user }) {
  const canSeries = canEditSeries(user);
  const [products, setProducts] = useState([]);
  const [search, setSearch] = useState('');
  const [category, setCategory] = useState('TODAS');
  const [selectedId, setSelectedId] = useState('');
  const [form, setForm] = useState(emptyProduct);
  const [editorOpen, setEditorOpen] = useState(false);
  const [newCategoryOpen, setNewCategoryOpen] = useState(false);
  const [newCategory, setNewCategory] = useState('');
  const [series, setSeries] = useState([]);
  const [movements, setMovements] = useState([]);
  const [seriesEditorOpen, setSeriesEditorOpen] = useState(false);
  const [serieForm, setSerieForm] = useState(() => ({ id: '', serie: '', proveedor: localStorage.getItem('gg_erp_last_provider') || '', estado: localStorage.getItem('gg_erp_last_series_state') || 'DISPONIBLE', almacen: localStorage.getItem('gg_erp_last_warehouse') || 'TIENDA', fecha_ingreso: localDateForApi(), usuario_ingreso: user || '' }));
  const [warehouseOptions, setWarehouseOptions] = useState(() => {
    try {
      const saved = JSON.parse(localStorage.getItem('gg_erp_warehouses') || '[]');
      return [...new Set([...DEFAULT_WAREHOUSES, ...asArray(saved)].map((w) => String(w || '').trim().toUpperCase()).filter(Boolean))];
    } catch {
      return DEFAULT_WAREHOUSES;
    }
  });
  const [newWarehouse, setNewWarehouse] = useState('');
  const [selectedSerieIds, setSelectedSerieIds] = useState([]);
  const [bulkWarehouse, setBulkWarehouse] = useState(localStorage.getItem('gg_erp_last_warehouse') || 'TIENDA');
  const [salidaForm, setSalidaForm] = useState(() => ({ tipo: 'PASE', numero: '', cliente_nombre: '', observacion: '', series_texto: '' }));
  const [loadingInfo, setLoadingInfo] = useState(false);
  const [saving, setSaving] = useState(false);
  const imageInputRef = useRef(null);
  const productSerieInputRef = useRef(null);
  const load = async () => {
    const rows = asArray(await apiFetch('/productos'));
    setProducts(rows);
    if (!selectedId && rows[0]) setSelectedId(String(rows[0].id));
  };
  useEffect(() => { load(); }, []);
  useEffect(() => {
    setSelectedSerieIds([]);
    if (!selectedId) {
      setSeries([]);
      setMovements([]);
      return;
    }
    setLoadingInfo(true);
    Promise.all([
      apiFetch('/series').catch(() => []),
      apiFetch(`/productos/${selectedId}/movimientos`).catch(() => []),
    ]).then(([seriesRes, movementsRes]) => {
      setSeries(asArray(seriesRes).filter((s) => String(s.producto_id) === String(selectedId)));
      setMovements(asArray(movementsRes));
    }).finally(() => setLoadingInfo(false));
  }, [selectedId]);
  const categories = useMemo(() => ['TODAS', ...Array.from(new Set(products.map((p) => String(p.categoria || '').trim()).filter(Boolean))).sort()], [products]);
  const selected = products.find((p) => String(p.id) === String(selectedId));
  const selectedSeries = series.filter((s) => String(s.producto_id) === String(selectedId));
  const saveWarehouseOptions = (next) => {
    const clean = [...new Set(next.map((w) => String(w || '').trim().toUpperCase()).filter(Boolean))];
    setWarehouseOptions(clean);
    localStorage.setItem('gg_erp_warehouses', JSON.stringify(clean));
    return clean;
  };
  const addWarehouseOption = () => {
    const name = newWarehouse.trim().toUpperCase();
    if (!name) return;
    saveWarehouseOptions([...warehouseOptions, name]);
    setBulkWarehouse(name);
    setSerieForm((current) => ({ ...current, almacen: name }));
    setForm((current) => ({ ...current, almacen: current.almacen || name }));
    setNewWarehouse('');
  };
  const removeWarehouseOption = (name) => {
    const clean = String(name || '').trim().toUpperCase();
    if (!clean || DEFAULT_WAREHOUSES.includes(clean)) return;
    saveWarehouseOptions(warehouseOptions.filter((w) => w !== clean));
  };
  const toggleSerieSelection = (id) => {
    const key = Number(id);
    if (!key) return;
    setSelectedSerieIds((current) => current.includes(key) ? current.filter((x) => x !== key) : [...current, key]);
  };
  const moveSelectedSeries = async () => {
    if (!canSeries) {
      alert('Solo giomar y mily pueden mover series.');
      return;
    }
    if (!selectedSerieIds.length) {
      alert('Selecciona series para mover.');
      return;
    }
    const destino = String(bulkWarehouse || '').trim().toUpperCase();
    if (!destino) {
      alert('Ingresa almacen destino.');
      return;
    }
    saveWarehouseOptions([...warehouseOptions, destino]);
    const res = await apiFetch('/series/mover-almacen', { method: 'POST', json: { serie_ids: selectedSerieIds, almacen: destino, usuario: user || '' } });
    if (okResponse(res)) {
      setSelectedSerieIds([]);
      const rows = asArray(await apiFetch(`/productos/${selected.id}/series`));
      setSeries(rows);
      alert(`Series movidas a ${destino}: ${res.actualizadas || selectedSerieIds.length}`);
    } else alert(res.msg || 'No se pudieron mover las series.');
  };
  const moveOneSerie = async (serie) => {
    if (!canSeries) {
      alert('Solo giomar y mily pueden mover series.');
      return;
    }
    const destino = String(bulkWarehouse || '').trim().toUpperCase();
    if (!serie?.id || !destino) {
      alert('Selecciona almacen destino.');
      return;
    }
    saveWarehouseOptions([...warehouseOptions, destino]);
    const res = await apiFetch('/series/mover-almacen', { method: 'POST', json: { serie_ids: [Number(serie.id)], almacen: destino, usuario: user || '' } });
    if (okResponse(res)) {
      setSelectedSerieIds((current) => current.filter((id) => Number(id) !== Number(serie.id)));
      const rows = asArray(await apiFetch(`/productos/${selected.id}/series`));
      setSeries(rows);
      alert(`Serie transferida a ${destino}.`);
    } else alert(res.msg || 'No se pudo transferir la serie.');
  };
  const productSeries = [...selectedSeries].sort((a, b) => {
    const rank = { DISPONIBLE: 0, RESERVADO: 1, VENDIDO: 2, AGOTADO: 3 };
    const ea = String(a.estado || 'DISPONIBLE').toUpperCase();
    const eb = String(b.estado || 'DISPONIBLE').toUpperCase();
    return (rank[ea] ?? 9) - (rank[eb] ?? 9) || String(a.serie || '').localeCompare(String(b.serie || ''));
  });
  const pendingSeriesCount = splitSeriesInput(serieForm.serie).length;
  const filtered = filterProductsBySearch(
    products.filter((p) => category === 'TODAS' || String(p.categoria || '').toLowerCase() === category.toLowerCase()),
    search,
  );
  const edit = (p) => {
    if (!p) {
      alert('Selecciona un producto para editar.');
      return;
    }
    setForm({ id: p.id || '', nombre: p.nombre || '', categoria: p.categoria || '', marca: p.marca || '', modelo: p.modelo || '', precio_compra: p.precio_compra ?? '', precio_venta: p.precio_venta ?? p.precio ?? '', stock: p.stock ?? '', almacen: p.almacen || 'TIENDA', sku_woo: p.sku_woo || `ERP-${p.id || ''}`, observacion: p.observacion || '', imagen_url: p.imagen_url || '' });
    setNewCategory('');
    setNewCategoryOpen(false);
    setEditorOpen(true);
  };
  const create = () => {
    setForm({ ...emptyProduct, categoria: category !== 'TODAS' ? category : '' });
    setNewCategory('');
    setNewCategoryOpen(false);
    setEditorOpen(true);
  };
  const applyNewCategory = () => {
    const name = newCategory.trim().toUpperCase();
    if (!name) return;
    setForm((current) => ({ ...current, categoria: name }));
    setCategory(name);
    setNewCategory('');
    setNewCategoryOpen(false);
  };
  const save = async () => {
    setSaving(true);
    try {
      const payload = { ...form, precio_compra: Number(form.precio_compra || 0), precio_venta: Number(form.precio_venta || 0), stock: Number(form.stock || 0) };
      const res = await apiFetch(form.id ? `/productos/${form.id}` : '/productos', { method: form.id ? 'PUT' : 'POST', json: payload });
      if (okResponse(res)) { setForm(emptyProduct); setEditorOpen(false); await load(); } else alert(res.msg || 'No se pudo guardar.');
    } finally { setSaving(false); }
  };
  const remove = async () => {
    if (!form.id || !confirm(`Eliminar ${form.nombre}?`)) return;
    const res = await apiFetch(`/productos/${form.id}`, { method: 'DELETE' });
    if (okResponse(res)) { setForm(emptyProduct); setEditorOpen(false); setSelectedId(''); await load(); } else alert(res.msg || 'No se pudo eliminar.');
  };
  const selectImage = async (event) => {
    const file = event.target.files?.[0];
    event.target.value = '';
    if (!file) return;
    try {
      const imageData = await fileToProductImage(file);
      setForm((current) => ({ ...current, imagen_url: imageData }));
    } catch (e) {
      alert(e.message || 'No se pudo cargar la imagen.');
    }
  };
  const resetSerie = () => setSerieForm((current) => ({ id: '', serie: '', proveedor: current.proveedor || localStorage.getItem('gg_erp_last_provider') || '', estado: current.estado || localStorage.getItem('gg_erp_last_series_state') || 'DISPONIBLE', almacen: current.almacen || localStorage.getItem('gg_erp_last_warehouse') || 'TIENDA', fecha_ingreso: current.fecha_ingreso || localDateForApi(), usuario_ingreso: user || '' }));
  const editSerie = (serie) => setSerieForm({ id: serie.id || '', serie: serie.serie || '', proveedor: serie.proveedor || localStorage.getItem('gg_erp_last_provider') || '', estado: serie.estado || 'DISPONIBLE', almacen: serie.almacen || localStorage.getItem('gg_erp_last_warehouse') || 'TIENDA', fecha_ingreso: String(serie.fecha_ingreso || localDateForApi()).slice(0, 10), usuario_ingreso: serie.usuario_ingreso || user || '' });
  const appendSerieLine = () => {
    setSerieForm((current) => ({ ...current, serie: current.serie.trim() ? `${current.serie.trim()}\n` : '' }));
    window.setTimeout(() => productSerieInputRef.current?.focus?.(), 50);
  };
  const normalizeSerieForm = () => {
    setSerieForm((current) => ({ ...current, serie: normalizeSeriesInput(current.serie) }));
    window.setTimeout(() => productSerieInputRef.current?.focus?.(), 50);
  };
  const pasteSeriesAsLines = (event) => {
    const pasted = event.clipboardData?.getData('text');
    if (!pasted) return;
    event.preventDefault();
    const input = event.currentTarget;
    const start = input.selectionStart ?? serieForm.serie.length;
    const end = input.selectionEnd ?? serieForm.serie.length;
    const mixed = `${serieForm.serie.slice(0, start)}\n${pasted}\n${serieForm.serie.slice(end)}`;
    setSerieForm((current) => ({ ...current, serie: normalizeSeriesInput(mixed) }));
    window.setTimeout(() => productSerieInputRef.current?.focus?.(), 50);
  };
  const saveSerie = async () => {
    if (!canSeries) {
      alert('Solo giomar y mily pueden ingresar o editar series.');
      return;
    }
    if (!selected || !serieForm.serie.trim()) return;
    const endpoint = serieForm.id ? `/series/${serieForm.id}` : '/series';
    const res = await apiFetch(endpoint, { method: serieForm.id ? 'PUT' : 'POST', json: { ...serieForm, producto_id: Number(selected.id), usuario_ingreso: user || serieForm.usuario_ingreso || '' } });
    if (okResponse(res)) {
      localStorage.setItem('gg_erp_last_provider', serieForm.proveedor || '');
      localStorage.setItem('gg_erp_last_series_state', serieForm.estado || 'DISPONIBLE');
      localStorage.setItem('gg_erp_last_warehouse', serieForm.almacen || 'TIENDA');
      resetSerie();
      const rows = asArray(await apiFetch(`/productos/${selected.id}/series`));
      setSeries(rows);
      await load();
      setMovements(asArray(await apiFetch(`/productos/${selected.id}/movimientos`)));
      window.setTimeout(() => productSerieInputRef.current?.focus?.(), 50);
    } else alert(res.msg || 'No se pudo guardar la serie.');
  };
  const deleteSerie = async () => {
    if (!canSeries) {
      alert('Solo giomar y mily pueden eliminar series.');
      return;
    }
    if (!serieForm.id || !confirm(`Eliminar serie ${serieForm.serie}?`)) return;
    const res = await apiFetch(`/series/${serieForm.id}`, { method: 'DELETE', params: { usuario: user || '' } });
    if (okResponse(res)) {
      resetSerie();
      setSeries(asArray(await apiFetch(`/productos/${selected.id}/series`)));
      await load();
    } else alert(res.msg || 'No se pudo eliminar.');
  };
  const deleteSerieDirect = async (serie) => {
    if (!canSeries) {
      alert('Solo giomar y mily pueden eliminar series.');
      return;
    }
    if (!serie?.id || !confirm(`Eliminar serie ${serie.serie}?`)) return;
    const res = await apiFetch(`/series/${serie.id}`, { method: 'DELETE', params: { usuario: user || '' } });
    if (okResponse(res)) {
      if (String(serieForm.id) === String(serie.id)) resetSerie();
      setSelectedSerieIds((current) => current.filter((id) => Number(id) !== Number(serie.id)));
      setSeries(asArray(await apiFetch(`/productos/${selected.id}/series`)));
      await load();
    } else alert(res.msg || 'No se pudo eliminar.');
  };
  const saveSalidaManual = async () => {
    const seriesTexto = salidaForm.series_texto.trim();
    if (!seriesTexto) {
      alert('Ingresa una o mas series para dar salida.');
      return;
    }
    const res = await apiFetch('/documentos/manual-series', {
      method: 'POST',
      json: {
        tipo: salidaForm.tipo || 'PASE',
        numero: salidaForm.numero,
        cliente_nombre: salidaForm.cliente_nombre || 'SALIDA MANUAL',
        fecha_emision: localDateForApi(),
        series_texto: seriesTexto,
        usuario_emisor: user || '',
        observacion: salidaForm.observacion || 'Salida manual registrada desde series',
      },
    });
    if (okResponse(res)) {
      alert(`Salida registrada: ${res.tipo} ${res.numero}`);
      setSalidaForm({ tipo: 'PASE', numero: '', cliente_nombre: '', observacion: '', series_texto: '' });
      const rows = asArray(await apiFetch(`/productos/${selected.id}/series`));
      setSeries(rows);
      await load();
      setMovements(asArray(await apiFetch(`/productos/${selected.id}/movimientos`)));
    } else alert(res.msg || 'No se pudo dar salida a las series.');
  };
  const syncSelectedPrice = async () => {
    if (!selected) return;
    if (!String(selected.sku_woo || '').trim()) {
      alert('Este producto no tiene Codigo web / SKU.');
      return;
    }
    const res = await apiFetch(`/web/woocommerce/sync-price/${selected.id}`, { method: 'POST' });
    alert(res.msg || (okResponse(res) ? 'Precio sincronizado.' : 'No se pudo sincronizar precio.'));
  };
  return (
    <div className="grid gap-4 xl:grid-cols-[1fr_390px]">
      <Card title="Productos" actions={<><SelectInput value={category} onChange={(e) => setCategory(e.target.value)} className="w-44">{categories.map((c) => <option key={c}>{c}</option>)}</SelectInput><TextInput placeholder="Buscar producto..." value={search} onChange={(e) => setSearch(e.target.value)} className="w-60" /><ActionButton tone="neutral" onClick={load}>Actualizar</ActionButton></>}>
        <div className="mb-3 grid gap-2 sm:grid-cols-3">
          <ActionButton onClick={create}>Nuevo producto</ActionButton>
          <ActionButton tone="blue" onClick={() => edit(selected)}>Editar producto</ActionButton>
          <ActionButton tone="violet" onClick={() => { setForm({ ...emptyProduct, categoria: category !== 'TODAS' ? category : '' }); setNewCategoryOpen(true); setEditorOpen(true); }}>Nueva categorÃ­a</ActionButton>
        </div>
        <div className="max-h-[68vh] overflow-auto">
          {filtered.map((p) => {
            const isSelected = String(selectedId) === String(p.id);
            return (
              <button
                key={p.id}
                onClick={() => setSelectedId(String(p.id))}
                onDoubleClick={() => edit(p)}
                className={`relative grid w-full gap-2 border-b px-3 py-2 text-left transition md:grid-cols-[48px_1fr_105px_95px] ${
                  isSelected
                    ? 'border-teal-300 bg-teal-100 shadow-inner ring-2 ring-inset ring-teal-500'
                    : 'border-slate-100 bg-white hover:bg-slate-50'
                }`}
              >
                {isSelected && <span className="absolute bottom-0 left-0 top-0 w-1.5 bg-teal-600" />}
                <div className="h-11 w-11 overflow-hidden rounded-md border border-slate-200 bg-slate-100">
                  {safeImageSrc(p.imagen_url) ? <img src={safeImageSrc(p.imagen_url)} alt="" className="h-full w-full object-cover" /> : <div className="grid h-full w-full place-items-center text-[10px] font-black text-slate-400">SIN IMG</div>}
                </div>
                <div className="min-w-0">
                  <b className={`block truncate text-sm ${isSelected ? 'text-teal-950' : ''}`}>{p.nombre}</b>
                  <div className="truncate text-xs text-slate-500">{p.categoria || 'Sin categorÃ­a'} / {p.marca} {p.modelo} / {p.almacen || 'TIENDA'}</div>
                </div>
                <b className="text-sm">{money(p.precio_venta || p.precio)}</b>
                <span className={`rounded-md px-2 py-1 text-center text-xs font-black ${Number(p.stock || 0) <= 0 ? 'bg-red-100 text-red-700' : Number(p.stock || 0) <= 5 ? 'bg-amber-100 text-amber-700' : 'bg-slate-100 text-slate-800'}`}>Stock {p.stock || 0}</span>
              </button>
            );
          })}
        </div>
        {!filtered.length && <Empty text="Sin productos con ese filtro." />}
      </Card>
      <aside className="grid gap-4">
        <Card title={selected ? selected.nombre : 'Producto seleccionado'}>
          {selected ? <div className="grid gap-3 text-sm"><div className="rounded-md bg-slate-50 p-3"><b>{selected.nombre}</b><div className="text-xs text-slate-500">{selected.categoria || 'Sin categorÃ­a'} / {selected.marca} {selected.modelo} / {selected.almacen || 'TIENDA'}</div><div className="text-xs font-black text-indigo-700">SKU web: {selected.sku_woo || `ERP-${selected.id}`}</div><div className="mt-1 font-black">{money(selected.precio_venta || selected.precio)} / Stock {selected.stock || 0}</div></div><ActionButton tone="blue" onClick={() => edit(selected)}>Editar seleccionado</ActionButton><ActionButton tone="violet" onClick={syncSelectedPrice}>Sincronizar precio web</ActionButton>{canSeries ? <ActionButton tone="violet" onClick={() => { resetSerie(); setSeriesEditorOpen(true); }}>Agregar / administrar series</ActionButton> : <div className="rounded-md bg-amber-50 p-2 text-xs font-bold text-amber-800">Solo giomar y mily pueden ingresar, editar o eliminar series.</div>}</div> : <Empty text="Selecciona un producto." />}
        </Card>
        <Card title="Rastreador de series"><SeriesQuickSearch compact /></Card>
        <Card title="Series del producto">
          {selected ? <div className="grid gap-2">
            {canSeries && <div className="grid gap-2 rounded-md bg-slate-50 p-2 text-xs">
              <div className="grid grid-cols-[1fr_auto] gap-2">
                <SelectInput value={bulkWarehouse} onChange={(e) => setBulkWarehouse(e.target.value)}>
                  {warehouseOptions.map((w) => <option key={w}>{w}</option>)}
                </SelectInput>
                <ActionButton tone="blue" onClick={moveSelectedSeries}>Mover seleccionadas</ActionButton>
              </div>
              <div className="grid grid-cols-[1fr_auto] gap-2">
                <TextInput placeholder="Nuevo almacen" value={newWarehouse} onChange={(e) => setNewWarehouse(e.target.value)} />
                <ActionButton tone="neutral" onClick={addWarehouseOption}>Agregar almacen</ActionButton>
              </div>
              <div className="flex flex-wrap gap-1">
                {warehouseOptions.map((w) => <button key={w} type="button" onClick={() => removeWarehouseOption(w)} className={`rounded px-2 py-1 text-[11px] font-black ${DEFAULT_WAREHOUSES.includes(w) ? 'bg-slate-200 text-slate-600' : 'bg-red-50 text-red-700'}`}>{w}</button>)}
              </div>
            </div>}
            <div className="max-h-48 overflow-auto rounded-md border border-slate-100">
              {loadingInfo && <div className="p-3 text-xs font-bold text-slate-500">Cargando...</div>}
              {!loadingInfo && productSeries.map((s) => {
                const estado = String(s.estado || 'DISPONIBLE').toUpperCase();
                const tone = estado === 'VENDIDO' ? 'bg-red-100 text-red-700' : estado === 'RESERVADO' ? 'bg-amber-100 text-amber-700' : estado === 'DISPONIBLE' ? 'bg-green-100 text-green-700' : 'bg-slate-100 text-slate-700';
                const checked = selectedSerieIds.includes(Number(s.id));
                return (
                  <div key={s.id || `${s.producto_id}-${s.serie}`} className={`grid gap-2 border-b border-slate-100 p-2 text-xs ${checked ? 'bg-blue-50' : 'bg-white'} ${canSeries ? 'grid-cols-[auto_1fr]' : ''}`}>
                    {canSeries && <input type="checkbox" checked={checked} onChange={() => toggleSerieSelection(s.id)} className="mt-1" />}
                    <div className="grid gap-2">
                      <div className="flex items-start justify-between gap-2">
                        <b className="break-all text-sm text-slate-950">{s.serie}</b>
                        <span className={`rounded-md px-2 py-1 font-black ${tone}`}>{estado}</span>
                      </div>
                      <span className="text-slate-500">{s.proveedor || 'Sin proveedor'} / {s.almacen || 'TIENDA'}</span>
                      <span className="text-slate-400">Ingreso: {String(s.fecha_ingreso || s.creado_en || '').slice(0, 10) || '-'} / Salida: {String(s.fecha_salida || '').slice(0, 10) || '-'}</span>
                      <span className="text-slate-400">{s.usuario_ingreso || 'Sin usuario'}</span>
                      {canSeries && <div className="grid grid-cols-3 gap-1">
                        <button
                          type="button"
                          onClick={() => { editSerie(s); setSeriesEditorOpen(true); }}
                          className="rounded-md bg-blue-100 px-2 py-1 font-black text-blue-700"
                        >
                          Editar
                        </button>
                        <button
                          type="button"
                          onClick={() => moveOneSerie(s)}
                          className="rounded-md bg-violet-100 px-2 py-1 font-black text-violet-700"
                        >
                          Transferir
                        </button>
                        <button
                          type="button"
                          onClick={() => deleteSerieDirect(s)}
                          className="rounded-md bg-red-100 px-2 py-1 font-black text-red-700"
                        >
                          Eliminar
                        </button>
                      </div>}
                    </div>
                  </div>
                );
              })}
              {!loadingInfo && !productSeries.length && <Empty text="Sin series registradas." />}
            </div>
          </div> : <Empty text="Selecciona un producto." />}
        </Card>
        <Card title="Movimientos del producto">
          {selected ? <div className="max-h-[38vh] overflow-auto rounded-md border border-slate-100">
            {loadingInfo && <div className="p-3 text-xs font-bold text-slate-500">Cargando...</div>}
            {!loadingInfo && movements.map((m, idx) => {
              const isSale = m.origen === 'VENTA';
              const tone = isSale ? 'bg-emerald-50 text-emerald-800' : m.origen === 'GARANTIA' ? 'bg-amber-50 text-amber-800' : 'bg-blue-50 text-blue-800';
              return <div key={`${m.origen}-${m.fecha}-${idx}`} className="grid gap-1 border-b border-slate-100 p-3 text-xs">
                <div className="flex items-start justify-between gap-2"><b className="text-sm text-slate-900">{m.titulo || m.origen}</b><span className={`rounded-md px-2 py-1 font-black ${tone}`}>{m.origen}</span></div>
                <span className="text-slate-500">{m.fecha || ''}</span>
                {isSale && <span className="font-black text-slate-700">Boleta/Doc: {m.documento_tipo || 'DOC'} {m.documento || ''}</span>}
                <span className="text-slate-600">{m.detalle || ''}</span>
                {m.serie && <span className="text-slate-500">Serie: {m.serie}</span>}
                {m.usuario && <span className="text-slate-500">Usuario: {m.usuario}</span>}
                {m.total != null && <span className="font-bold text-slate-700">Total: {money(m.total)}</span>}
              </div>;
            })}
            {!loadingInfo && !movements.length && <Empty text="Sin movimientos registrados." />}
          </div> : <Empty text="Selecciona un producto." />}
        </Card>
      </aside>
      {editorOpen && (
        <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/60 p-3">
          <section className="grid max-h-[94vh] w-full max-w-3xl grid-rows-[auto_1fr_auto] overflow-hidden rounded-lg bg-white shadow-2xl">
            <header className="flex items-center justify-between border-b border-slate-200 p-3">
              <h2 className="text-lg font-black text-slate-950">{form.id ? 'Editar producto' : 'Agregar producto'}</h2>
              <button type="button" className="rounded-md bg-slate-100 px-3 py-2 text-xs font-black text-slate-700" onClick={() => setEditorOpen(false)}>Cerrar</button>
            </header>
            <div className="grid gap-3 overflow-auto p-3">
              <Field label="Nombre"><TextInput value={form.nombre} onChange={(e) => setForm({ ...form, nombre: e.target.value })} /></Field>
              <div className="grid gap-2 sm:grid-cols-[1fr_auto]">
                <Field label="Categoría">
                  <SelectInput value={form.categoria || ''} onChange={(e) => setForm({ ...form, categoria: e.target.value })}>
                    <option value="">Sin categorÃ­a</option>
                    {categories.filter((c) => c !== 'TODAS').map((c) => <option key={c}>{c}</option>)}
                  </SelectInput>
                </Field>
                <ActionButton className="self-end" tone="violet" onClick={() => setNewCategoryOpen((value) => !value)}>Nueva categorÃ­a</ActionButton>
              </div>
              {newCategoryOpen && <div className="grid gap-2 rounded-md bg-violet-50 p-2 sm:grid-cols-[1fr_auto]"><TextInput placeholder="Nombre de categorÃ­a" value={newCategory} onChange={(e) => setNewCategory(e.target.value)} /><ActionButton tone="violet" onClick={applyNewCategory}>Agregar</ActionButton></div>}
              <div className="grid grid-cols-2 gap-2"><Field label="Marca"><TextInput value={form.marca} onChange={(e) => setForm({ ...form, marca: e.target.value })} /></Field><Field label="Modelo"><TextInput value={form.modelo} onChange={(e) => setForm({ ...form, modelo: e.target.value })} /></Field></div>
              <Field label="Codigo web / SKU"><TextInput value={form.sku_woo || ''} onChange={(e) => setForm({ ...form, sku_woo: e.target.value.trim().toUpperCase() })} placeholder="Ejemplo: ERP-1024 o RAM-K16-001" /></Field>
              <div className="grid grid-cols-3 gap-2"><Field label="P. compra"><TextInput value={form.precio_compra} onChange={(e) => setForm({ ...form, precio_compra: e.target.value })} /></Field><Field label="P. venta"><TextInput value={form.precio_venta} onChange={(e) => setForm({ ...form, precio_venta: e.target.value })} /></Field><Field label="Stock"><TextInput value={form.stock} onChange={(e) => setForm({ ...form, stock: e.target.value })} /></Field></div>
              <Field label="AlmacÃ©n"><SelectInput value={form.almacen || 'TIENDA'} onChange={(e) => setForm({ ...form, almacen: e.target.value })}>{warehouseOptions.map((w) => <option key={w}>{w}</option>)}</SelectInput></Field>
              <Field label="Imagen">
                <input ref={imageInputRef} type="file" accept="image/*" className="hidden" onChange={selectImage} />
                <div className="grid gap-2 sm:grid-cols-[96px_1fr]">
                  <div className="h-24 overflow-hidden rounded-md border border-slate-200 bg-slate-50">
                    {safeImageSrc(form.imagen_url) ? <img src={safeImageSrc(form.imagen_url)} alt="" className="h-full w-full object-cover" /> : <div className="grid h-full w-full place-items-center text-xs font-black text-slate-400">IMG</div>}
                  </div>
                  <div className="grid gap-2">
                    <ActionButton tone="blue" onClick={() => imageInputRef.current?.click()}>Subir / actualizar foto</ActionButton>
                    {form.imagen_url?.startsWith('data:image/')
                      ? <TextInput value="Imagen subida desde archivo" readOnly />
                      : <TextInput placeholder="O pega una URL de imagen" value={form.imagen_url} onChange={(e) => setForm({ ...form, imagen_url: e.target.value })} />}
                    {form.imagen_url && <ActionButton tone="neutral" onClick={() => setForm({ ...form, imagen_url: '' })}>Quitar imagen</ActionButton>}
                  </div>
                </div>
              </Field>
              <Field label="Observacion"><TextInput value={form.observacion} onChange={(e) => setForm({ ...form, observacion: e.target.value })} /></Field>
            </div>
            <footer className="grid grid-cols-2 gap-2 border-t border-slate-200 p-3 sm:flex sm:justify-end">
              {form.id && <ActionButton tone="danger" onClick={remove}>Eliminar</ActionButton>}
              <ActionButton tone="neutral" onClick={() => setForm(emptyProduct)}>Limpiar</ActionButton>
              <ActionButton disabled={saving} onClick={save}>{form.id ? 'Actualizar' : 'Guardar'}</ActionButton>
            </footer>
          </section>
        </div>
      )}
      {seriesEditorOpen && canSeries && selected && (
        <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/60 p-3">
          <section className="grid max-h-[92vh] w-full max-w-4xl grid-rows-[auto_1fr_auto] overflow-hidden rounded-lg bg-white shadow-2xl">
            <header className="flex items-center justify-between border-b border-slate-200 p-3">
              <h2 className="text-lg font-black text-slate-950">{serieForm.id ? 'Editar serie' : 'Agregar series masivas'} / {selected.nombre}</h2>
              <button className="rounded-md bg-slate-100 px-3 py-2 text-xs font-black text-slate-700" onClick={() => setSeriesEditorOpen(false)}>Cerrar</button>
            </header>
            <div className="grid gap-3 overflow-auto p-3">
              <Field label={`Series a registrar - ${pendingSeriesCount} detectada(s)`}>
                <TextArea
                  ref={productSerieInputRef}
                  value={serieForm.serie}
                  onChange={(e) => setSerieForm({ ...serieForm, serie: e.target.value })}
                  onPaste={pasteSeriesAsLines}
                  className="min-h-56 font-mono text-base leading-7"
                  placeholder={`Pega o pistolea una serie por linea:\nSN001\nSN002\nSN003`}
                />
                <div className="mt-1 text-xs font-bold text-slate-500">Puedes pegar desde Excel o escanear seguido. Se guardan todas las lineas detectadas para este producto.</div>
              </Field>
              <div className="grid grid-cols-3 gap-2">
                <ActionButton tone="neutral" onClick={appendSerieLine}>+ Otra serie</ActionButton>
                <ActionButton tone="neutral" onClick={normalizeSerieForm}>Ordenar en lineas</ActionButton>
                <ActionButton tone="blue" onClick={() => setSalidaForm((current) => ({ ...current, series_texto: serieForm.serie }))}>Usar para salida</ActionButton>
              </div>
              <div className="grid grid-cols-2 gap-2">
                <Field label="Proveedor"><TextInput value={serieForm.proveedor} onChange={(e) => setSerieForm({ ...serieForm, proveedor: e.target.value })} /></Field>
                <Field label="Estado"><SelectInput value={serieForm.estado} onChange={(e) => setSerieForm({ ...serieForm, estado: e.target.value })}>{SERIES_STATES.map((state) => <option key={state}>{state}</option>)}</SelectInput></Field>
              </div>
              <Field label="AlmacÃ©n"><SelectInput value={serieForm.almacen || 'TIENDA'} onChange={(e) => setSerieForm({ ...serieForm, almacen: e.target.value })}>{warehouseOptions.map((w) => <option key={w}>{w}</option>)}</SelectInput></Field>
              {String(serieForm.estado || '').toUpperCase() === 'AGOTADO' && <div className="rounded-md bg-red-50 p-2 text-xs font-bold text-red-700">Al guardar como AGOTADO, el producto queda con stock 0 sin eliminar el producto.</div>}
              <div className="max-h-64 overflow-auto rounded-md border border-slate-100">
                {selectedSeries.map((s) => <button key={s.id || s.serie} onClick={() => editSerie(s)} className={`grid w-full border-b border-slate-100 px-3 py-2 text-left text-xs ${String(serieForm.id) === String(s.id) ? 'bg-violet-50' : 'bg-white'}`}><b>{s.serie}</b><span className="text-slate-500">{s.estado || 'DISPONIBLE'} / {s.proveedor || 'Sin proveedor'} / {s.almacen || 'TIENDA'}</span><span className="text-slate-400">Ingreso: {String(s.fecha_ingreso || '').slice(0, 10) || '-'} / {s.usuario_ingreso || 'Sin usuario'}</span></button>)}
                {!selectedSeries.length && <Empty text="Sin series registradas." />}
              </div>
              <div className="grid gap-2 rounded-md border border-blue-100 bg-blue-50 p-3">
                <div className="text-sm font-black text-blue-950">Salida manual</div>
                <div className="grid grid-cols-2 gap-2">
                  <Field label="Tipo"><SelectInput value={salidaForm.tipo} onChange={(e) => setSalidaForm({ ...salidaForm, tipo: e.target.value })}><option>PASE</option><option>BOLETA</option><option>FACTURA</option></SelectInput></Field>
                  <Field label="Doc. externo"><TextInput value={salidaForm.numero} onChange={(e) => setSalidaForm({ ...salidaForm, numero: e.target.value })} /></Field>
                </div>
                <Field label="Nombre / tienda"><TextInput value={salidaForm.cliente_nombre} onChange={(e) => setSalidaForm({ ...salidaForm, cliente_nombre: e.target.value })} /></Field>
                <Field label="Series salida"><TextArea value={salidaForm.series_texto} onChange={(e) => setSalidaForm({ ...salidaForm, series_texto: e.target.value })} /></Field>
                <Field label="Observacion"><TextInput value={salidaForm.observacion} onChange={(e) => setSalidaForm({ ...salidaForm, observacion: e.target.value })} /></Field>
                <ActionButton tone="danger" onClick={saveSalidaManual}>Dar salida como venta</ActionButton>
              </div>
            </div>
            <footer className="grid grid-cols-2 gap-2 border-t border-slate-200 p-3 sm:flex sm:justify-end">
              {serieForm.id && <ActionButton tone="danger" onClick={deleteSerie}>Eliminar</ActionButton>}
              <ActionButton tone="neutral" onClick={resetSerie}>Limpiar</ActionButton>
              <ActionButton tone="violet" onClick={saveSerie}>{serieForm.id ? 'Actualizar serie' : `Guardar ${pendingSeriesCount || ''} serie(s)`}</ActionButton>
            </footer>
          </section>
        </div>
      )}
    </div>
  );
}

function InventarioView({ user }) {
  const canSeries = canEditSeries(user);
  const [products, setProducts] = useState([]);
  const [series, setSeries] = useState([]);
  const [selectedId, setSelectedId] = useState('');
  const [category, setCategory] = useState('TODAS');
  const [mode, setMode] = useState('series');
  const [stockReal, setStockReal] = useState('');
  const [serieForm, setSerieForm] = useState(() => ({ id: '', serie: '', proveedor: localStorage.getItem('gg_erp_last_provider') || '', estado: localStorage.getItem('gg_erp_last_series_state') || 'DISPONIBLE', almacen: localStorage.getItem('gg_erp_last_warehouse') || 'TIENDA', fecha_ingreso: localDateForApi(), usuario_ingreso: user || '' }));
  const [transfer, setTransfer] = useState({ cantidad: '', destino: '', nota: '' });
  const [salidaForm, setSalidaForm] = useState({ tipo: 'PASE', numero: '', cliente_nombre: '', observacion: '', series_texto: '' });
  const [query, setQuery] = useState('');
  const [movements, setMovements] = useState([]);
  const [loadingMovements, setLoadingMovements] = useState(false);
  const [countSession, setCountSession] = useState(null);
  const [scanSerie, setScanSerie] = useState('');
  const [countMsg, setCountMsg] = useState('');
  const inventorySerieInputRef = useRef(null);
  const load = async () => { const p = asArray(await apiFetch('/productos')); setProducts(p); setSeries(asArray(await apiFetch('/series'))); if (!selectedId && p[0]) setSelectedId(String(p[0].id)); };
  useEffect(() => { load(); }, []);
  const selected = products.find((p) => String(p.id) === String(selectedId));
  useEffect(() => { if (selected) setStockReal(String(selected.stock ?? 0)); }, [selectedId]);
  const loadMovements = async (productId = selectedId) => {
    if (!productId) {
      setMovements([]);
      return;
    }
    setLoadingMovements(true);
    try {
      setMovements(asArray(await apiFetch(`/productos/${productId}/movimientos`)));
    } finally {
      setLoadingMovements(false);
    }
  };
  useEffect(() => { loadMovements(selectedId); }, [selectedId]);
  const categories = useMemo(() => ['TODAS', ...Array.from(new Set(products.map((p) => String(p.categoria || '').trim()).filter(Boolean))).sort()], [products]);
  const selectedSeries = series.filter((s) => String(s.producto_id) === String(selected?.id));
  const pendingSeriesCount = splitSeriesInput(serieForm.serie).length;
  const filtered = filterProductsBySearch(
    products.filter((p) => category === 'TODAS' || String(p.categoria || '').toLowerCase() === category.toLowerCase()),
    query,
  );
  const adjust = async () => { if (!selected) return; const res = await apiFetch(`/productos/${selected.id}/ajustar-stock`, { method: 'POST', json: { stock: Number(stockReal || 0) } }); if (okResponse(res)) { await load(); await loadMovements(selected.id); alert('Stock actualizado.'); } else alert(res.msg || 'Error.'); };
  const editSerie = (serie) => setSerieForm({ id: serie.id || '', serie: serie.serie || '', proveedor: serie.proveedor || localStorage.getItem('gg_erp_last_provider') || '', estado: serie.estado || 'DISPONIBLE', almacen: serie.almacen || localStorage.getItem('gg_erp_last_warehouse') || 'TIENDA', fecha_ingreso: String(serie.fecha_ingreso || localDateForApi()).slice(0, 10), usuario_ingreso: serie.usuario_ingreso || user || '' });
  const resetSerie = () => setSerieForm((current) => ({ id: '', serie: '', proveedor: current.proveedor || localStorage.getItem('gg_erp_last_provider') || '', estado: current.estado || localStorage.getItem('gg_erp_last_series_state') || 'DISPONIBLE', almacen: current.almacen || localStorage.getItem('gg_erp_last_warehouse') || 'TIENDA', fecha_ingreso: current.fecha_ingreso || localDateForApi(), usuario_ingreso: user || '' }));
  const addSerie = async () => {
    if (!canSeries) {
      alert('Solo giomar y mily pueden ingresar o editar series.');
      return;
    }
    if (!selected || !serieForm.serie) return;
    const endpoint = serieForm.id ? `/series/${serieForm.id}` : '/series';
    const res = await apiFetch(endpoint, { method: serieForm.id ? 'PUT' : 'POST', json: { ...serieForm, producto_id: Number(selected.id), usuario_ingreso: user || serieForm.usuario_ingreso || '' } });
    if (okResponse(res)) {
      localStorage.setItem('gg_erp_last_provider', serieForm.proveedor || '');
      localStorage.setItem('gg_erp_last_series_state', serieForm.estado || 'DISPONIBLE');
      localStorage.setItem('gg_erp_last_warehouse', serieForm.almacen || 'TIENDA');
      if (String(serieForm.estado || '').toUpperCase() === 'AGOTADO') {
        await apiFetch(`/productos/${selected.id}/ajustar-stock`, { method: 'POST', json: { stock: 0 } });
      }
      resetSerie();
      await load();
      await loadMovements(selected.id);
      window.setTimeout(() => inventorySerieInputRef.current?.focus?.(), 50);
    } else alert(res.msg || 'No se pudo guardar serie.');
  };
  const deleteSerie = async () => {
    if (!canSeries) {
      alert('Solo giomar y mily pueden eliminar series.');
      return;
    }
    if (!serieForm.id || !confirm(`Eliminar serie ${serieForm.serie}?`)) return;
    const res = await apiFetch(`/series/${serieForm.id}`, { method: 'DELETE', params: { usuario: user || '' } });
    if (okResponse(res)) { resetSerie(); await load(); await loadMovements(selected?.id); } else alert(res.msg || 'No se pudo eliminar serie.');
  };
  const transferStock = async () => { if (!selected || !transfer.cantidad || !transfer.destino) return; const res = await apiFetch('/stock/transferir', { method: 'POST', json: { producto_id: Number(selected.id), cantidad: Number(transfer.cantidad), sucursal_origen: EMPRESA, sucursal_destino: transfer.destino, usuario: user, nota: transfer.nota } }); if (okResponse(res)) { setTransfer({ cantidad: '', destino: '', nota: '' }); await load(); await loadMovements(selected.id); alert('Transferencia enviada.'); } else alert(res.msg || 'Error.'); };
  const saveSalidaManual = async () => {
    const seriesTexto = salidaForm.series_texto.trim();
    if (!seriesTexto) {
      alert('Ingresa una o mas series para dar salida.');
      return;
    }
    const res = await apiFetch('/documentos/manual-series', {
      method: 'POST',
      json: {
        tipo: salidaForm.tipo || 'PASE',
        numero: salidaForm.numero,
        cliente_nombre: salidaForm.cliente_nombre || 'SALIDA MANUAL',
        fecha_emision: localDateForApi(),
        series_texto: seriesTexto,
        usuario_emisor: user || '',
        observacion: salidaForm.observacion || 'Salida manual registrada desde inventario',
      },
    });
    if (okResponse(res)) {
      alert(`Salida registrada: ${res.tipo} ${res.numero}`);
      setSalidaForm({ tipo: 'PASE', numero: '', cliente_nombre: '', observacion: '', series_texto: '' });
      await load();
      await loadMovements(selected?.id);
    } else alert(res.msg || 'No se pudo dar salida a las series.');
  };
  const startCount = async () => {
    if (!category || category === 'TODAS') {
      alert('Selecciona una categorÃ­a para comenzar inventario.');
      return;
    }
    const res = await apiFetch('/inventario/conteos', { method: 'POST', json: { categoria: category, usuario: user, sucursal: EMPRESA } });
    if (okResponse(res)) { setCountSession(res); setCountMsg(`Inventario iniciado: ${category}`); }
    else alert(res.msg || 'No se pudo comenzar inventario.');
  };
  const scanCount = async () => {
    const id = countSession?.conteo?.id;
    if (!id) { alert('Primero comienza inventario.'); return; }
    if (!scanSerie.trim()) return;
    const res = await apiFetch(`/inventario/conteos/${id}/scan`, { method: 'POST', json: { serie: scanSerie, usuario: user } });
    if (okResponse(res)) { setCountSession(res); setCountMsg(res.msg || 'Serie contada.'); setScanSerie(''); }
    else { setCountMsg(res.msg || 'Serie no aceptada.'); alert(res.msg || 'Serie no aceptada.'); }
  };
  const closeCount = async () => {
    const id = countSession?.conteo?.id;
    if (!id) return;
    const res = await apiFetch(`/inventario/conteos/${id}/cerrar`, { method: 'POST' });
    if (okResponse(res)) { setCountSession(res); setCountMsg('Inventario cerrado.'); }
    else alert(res.msg || 'No se pudo cerrar inventario.');
  };
  return (
    <div className="grid gap-4 xl:grid-cols-[minmax(0,1fr)_420px]">
      <Card title="Inventario" actions={<><SelectInput value={category} onChange={(e) => setCategory(e.target.value)} className="w-40">{categories.map((c) => <option key={c}>{c}</option>)}</SelectInput><TextInput placeholder="Buscar producto..." value={query} onChange={(e) => setQuery(e.target.value)} className="w-60" /><ActionButton tone="neutral" onClick={load}>Actualizar</ActionButton></>}>
        <div className="mb-2 grid grid-cols-2 gap-2 text-center text-xs font-black md:grid-cols-5">
          <button className={`rounded-md px-2 py-2 ${mode === 'series' ? 'bg-violet-100 text-violet-800' : 'bg-slate-100 text-slate-600'}`} onClick={() => setMode('series')}>Series</button>
          <button className={`rounded-md px-2 py-2 ${mode === 'stock' ? 'bg-emerald-100 text-emerald-800' : 'bg-slate-100 text-slate-600'}`} onClick={() => setMode('stock')}>Stock</button>
          <button className={`rounded-md px-2 py-2 ${mode === 'transfer' ? 'bg-blue-100 text-blue-800' : 'bg-slate-100 text-slate-600'}`} onClick={() => setMode('transfer')}>Transferir</button>
          <button className={`rounded-md px-2 py-2 ${mode === 'conteo' ? 'bg-teal-100 text-teal-800' : 'bg-slate-100 text-slate-600'}`} onClick={() => setMode('conteo')}>Inventariar</button>
          <button className={`rounded-md px-2 py-2 ${mode === 'salida' ? 'bg-red-100 text-red-800' : 'bg-slate-100 text-slate-600'}`} onClick={() => setMode('salida')}>Salida</button>
        </div>
        <div className="max-h-[62vh] overflow-auto">{filtered.map((p) => <button key={p.id} onClick={() => { setSelectedId(String(p.id)); resetSerie(); }} className={`grid w-full gap-1 border-b border-slate-100 px-3 py-2 text-left md:grid-cols-[48px_1fr_105px_95px] ${String(selectedId) === String(p.id) ? 'bg-teal-50' : 'bg-white'}`}><div className="h-11 w-11 overflow-hidden rounded-md border border-slate-200 bg-slate-100">{safeImageSrc(p.imagen_url) ? <img src={safeImageSrc(p.imagen_url)} alt="" className="h-full w-full object-cover" /> : <div className="grid h-full w-full place-items-center text-[10px] font-black text-slate-400">SIN IMG</div>}</div><div className="min-w-0"><b className="block truncate text-sm">{p.nombre}</b><div className="truncate text-xs text-slate-500">{p.categoria || 'Sin categorÃ­a'} / {p.marca} {p.modelo} / {p.almacen || 'TIENDA'}</div></div><b className="text-sm">{money(p.precio_venta || p.precio)}</b><span className={`rounded-md px-2 py-1 text-center text-xs font-black ${Number(p.stock || 0) <= 0 ? 'bg-red-100 text-red-700' : Number(p.stock || 0) <= 5 ? 'bg-amber-100 text-amber-700' : 'bg-slate-100 text-slate-800'}`}>Stock {p.stock || 0}</span></button>)}</div>
        {!filtered.length && <Empty text="Sin productos con ese filtro." />}
      </Card>
      <aside className="grid gap-4 content-start">
        <Card title={selected ? selected.nombre : 'Producto seleccionado'}>{selected ? <div className="grid gap-2 text-sm"><div className="rounded-md bg-slate-50 p-3"><b>{selected.nombre}</b><div className="text-xs text-slate-500">{selected.categoria || 'Sin categorÃ­a'} / {selected.marca} {selected.modelo} / {selected.almacen || 'TIENDA'}</div><div className="mt-1 font-black">{money(selected.precio_venta || selected.precio)} / Stock {selected.stock || 0}</div></div></div> : <Empty text="Selecciona un producto." />}</Card>
        <Card title="Rastreador de series"><SeriesQuickSearch compact /></Card>
        <Card title="Movimientos" actions={selected && <ActionButton tone="neutral" onClick={() => loadMovements(selected.id)}>Actualizar</ActionButton>}>
          {selected ? <div className="max-h-72 overflow-auto rounded-md border border-slate-100">
            {loadingMovements && <div className="p-3 text-sm font-bold text-slate-500">Cargando movimientos...</div>}
            {!loadingMovements && movements.map((m, idx) => {
              const tone = m.origen === 'VENTA' ? 'bg-emerald-50 text-emerald-800' : m.origen === 'GARANTIA' ? 'bg-amber-50 text-amber-800' : 'bg-blue-50 text-blue-800';
              return <div key={`${m.origen}-${m.fecha}-${idx}`} className="grid gap-1 border-b border-slate-100 p-3 text-xs">
                <div className="flex items-start justify-between gap-2">
                  <b className="text-sm text-slate-900">{m.titulo || m.origen}</b>
                  <span className={`rounded-md px-2 py-1 font-black ${tone}`}>{m.origen}</span>
                </div>
                <span className="font-semibold text-slate-600">{m.fecha || ''}</span>
                <span className="text-slate-600">{m.detalle || ''}</span>
                {m.documento && <span className="font-bold text-slate-700">Doc: {m.documento}</span>}
                {m.serie && <span className="text-slate-500">Serie: {m.serie}</span>}
                {m.usuario && <span className="text-slate-500">Usuario: {m.usuario}</span>}
                {m.nota && <span className="text-slate-500">{m.nota}</span>}
              </div>;
            })}
            {!loadingMovements && !movements.length && <Empty text="Sin movimientos para este producto." />}
          </div> : <Empty text="Selecciona un producto." />}
        </Card>
        {mode === 'stock' && <Card title="Ajuste rapido">{selected ? <div className="grid gap-3"><Field label="Stock real"><TextInput value={stockReal} onChange={(e) => setStockReal(e.target.value)} inputMode="numeric" /></Field><ActionButton onClick={adjust}>Aplicar ajuste</ActionButton></div> : <Empty />}</Card>}
        {mode === 'series' && <Card title={canSeries ? (serieForm.id ? 'Editar serie' : 'Agregar series masivas') : 'Series (solo lectura)'}>{selected ? <div className="grid gap-3">{!canSeries && <div className="rounded-md bg-amber-50 p-2 text-xs font-bold text-amber-800">Solo giomar y mily pueden ingresar, editar o eliminar series.</div>}{canSeries && <><Field label={`Serie(s) - ${pendingSeriesCount} detectada(s)`}><TextArea ref={inventorySerieInputRef} value={serieForm.serie} onChange={(e) => setSerieForm({ ...serieForm, serie: e.target.value })} className="min-h-48 font-mono text-base" placeholder="Pega o pistolea varias series. Puedes separarlas por Enter, coma, punto y coma o barra vertical." /><div className="mt-1 text-xs font-bold text-slate-500">Si pegas varias, se guardan todas para el producto seleccionado.</div></Field><div className="grid grid-cols-2 gap-2"><Field label="Proveedor"><TextInput value={serieForm.proveedor} onChange={(e) => setSerieForm({ ...serieForm, proveedor: e.target.value })} /></Field><Field label="Estado"><SelectInput value={serieForm.estado} onChange={(e) => setSerieForm({ ...serieForm, estado: e.target.value })}>{SERIES_STATES.map((state) => <option key={state}>{state}</option>)}</SelectInput></Field></div><Field label="AlmacÃ©n"><SelectInput value={serieForm.almacen || 'TIENDA'} onChange={(e) => setSerieForm({ ...serieForm, almacen: e.target.value })}>{WAREHOUSES.map((w) => <option key={w}>{w}</option>)}</SelectInput></Field>{String(serieForm.estado || '').toUpperCase() === 'AGOTADO' && <div className="rounded-md bg-red-50 p-2 text-xs font-bold text-red-700">Al guardar como AGOTADO, el producto queda con stock 0 sin eliminarse.</div>}<div className="grid grid-cols-2 gap-2"><ActionButton tone="violet" onClick={addSerie}>{serieForm.id ? 'Actualizar' : `Guardar ${pendingSeriesCount || ''} serie(s)`}</ActionButton><ActionButton tone="neutral" onClick={resetSerie}>Limpiar</ActionButton></div>{serieForm.id && <ActionButton tone="danger" onClick={deleteSerie}>Eliminar serie</ActionButton>}</>}<div className="max-h-56 overflow-auto rounded-md border border-slate-100">{selectedSeries.slice(0, 80).map((s) => canSeries ? <button key={s.id} onClick={() => editSerie(s)} className={`grid w-full border-b border-slate-100 px-3 py-2 text-left text-xs ${String(serieForm.id) === String(s.id) ? 'bg-violet-50' : 'bg-white'}`}><b>{s.serie}</b><span className="text-slate-500">{s.estado || 'DISPONIBLE'} / {s.proveedor || 'Sin proveedor'} / {s.almacen || 'TIENDA'}</span><span className="text-slate-400">Ingreso: {String(s.fecha_ingreso || s.creado_en || '').slice(0, 10) || '-'} / {s.usuario_ingreso || 'Sin usuario'}</span></button> : <div key={s.id} className="grid border-b border-slate-100 px-3 py-2 text-xs bg-white"><b>{s.serie}</b><span className="text-slate-500">{s.estado || 'DISPONIBLE'} / {s.proveedor || 'Sin proveedor'} / {s.almacen || 'TIENDA'}</span><span className="text-slate-400">Ingreso: {String(s.fecha_ingreso || s.creado_en || '').slice(0, 10) || '-'} / {s.usuario_ingreso || 'Sin usuario'}</span></div>)}{!selectedSeries.length && <Empty text="Sin series registradas." />}</div></div> : <Empty />}</Card>}
        {mode === 'conteo' && <Card title="Inventariado por categorÃ­a">{category !== 'TODAS' ? <div className="grid gap-3"><div className="rounded-md bg-teal-50 p-3 text-xs font-bold text-teal-800">Categoría: {category}</div><div className="grid grid-cols-2 gap-2"><ActionButton tone="blue" onClick={startCount}>Comenzar</ActionButton><ActionButton tone="danger" onClick={closeCount}>Cerrar</ActionButton></div><Field label="Pistolear serie"><TextInput value={scanSerie} onChange={(e) => setScanSerie(e.target.value)} onKeyDown={(e) => e.key === 'Enter' && scanCount()} /></Field><ActionButton onClick={scanCount}>Contar serie</ActionButton>{countSession?.resumen && <div className="grid grid-cols-2 gap-2 text-xs font-black"><Stat label="Físico" value={countSession.resumen.fisico || 0} tone="green" /><Stat label="Sistema" value={countSession.resumen.sistema || 0} tone="blue" /><Stat label="Faltan" value={countSession.resumen.faltantes || 0} tone="amber" /><Stat label="Alertas" value={countSession.resumen.errores || 0} tone="red" /></div>}{countMsg && <div className="rounded-md bg-slate-50 p-2 text-xs font-bold text-slate-700">{countMsg}</div>}<div className="max-h-64 overflow-auto rounded-md border border-slate-100">{(countSession?.scans || []).slice(0, 80).map((s) => <div key={s.id || s.serie} className={`grid border-b border-slate-100 p-2 text-xs ${s.estado === 'OK' ? 'bg-emerald-50' : 'bg-red-50'}`}><b>{s.serie}</b><span>{s.producto_nombre || 'Sin producto'} / {s.estado}</span></div>)}{!(countSession?.scans || []).length && <Empty text="Sin series contadas." />}</div></div> : <Empty text="Selecciona una categorÃ­a antes de comenzar." />}</Card>}
        {mode === 'transfer' && <Card title="Transferir stock">{selected ? <div className="grid gap-3"><Field label="Cantidad"><TextInput value={transfer.cantidad} onChange={(e) => setTransfer({ ...transfer, cantidad: e.target.value })} inputMode="numeric" /></Field><Field label="Sucursal destino"><TextInput value={transfer.destino} onChange={(e) => setTransfer({ ...transfer, destino: e.target.value })} /></Field><Field label="Nota"><TextInput value={transfer.nota} onChange={(e) => setTransfer({ ...transfer, nota: e.target.value })} /></Field><ActionButton tone="blue" onClick={transferStock}>Enviar transferencia</ActionButton></div> : <Empty />}</Card>}
        {mode === 'salida' && <Card title="Salida manual por series"><div className="grid gap-3"><div className="rounded-md bg-amber-50 p-2 text-xs font-bold text-amber-800">Usa esto para boletas de otro sistema, pases o salidas manuales. No elimina la serie: la marca como vendida y queda en historial.</div><div className="grid grid-cols-2 gap-2"><Field label="Tipo"><SelectInput value={salidaForm.tipo} onChange={(e) => setSalidaForm({ ...salidaForm, tipo: e.target.value })}><option>PASE</option><option>BOLETA</option><option>FACTURA</option></SelectInput></Field><Field label="Doc. externo"><TextInput value={salidaForm.numero} onChange={(e) => setSalidaForm({ ...salidaForm, numero: e.target.value })} /></Field></div><Field label="Nombre / tienda"><TextInput value={salidaForm.cliente_nombre} onChange={(e) => setSalidaForm({ ...salidaForm, cliente_nombre: e.target.value })} /></Field><Field label="Series salida"><TextArea value={salidaForm.series_texto} onChange={(e) => setSalidaForm({ ...salidaForm, series_texto: e.target.value })} /></Field><Field label="Observacion"><TextInput value={salidaForm.observacion} onChange={(e) => setSalidaForm({ ...salidaForm, observacion: e.target.value })} /></Field><ActionButton tone="danger" onClick={saveSalidaManual}>Dar salida</ActionButton></div></Card>}
      </aside>
    </div>
  );
}

function resizeCompraSeriesList(current, qty) {
  const n = Math.max(1, Math.min(100, Number(qty) || 1));
  const next = [...(current || [])];
  while (next.length < n) next.push('');
  return next.slice(0, n);
}

function ComprasView({ user }) {
  const canSeries = canEditSeries(user);
  const [compras, setCompras] = useState([]);
  const [proveedores, setProveedores] = useState([]);
  const [products, setProducts] = useState([]);
  const [proveedor, setProveedor] = useState({ nombre: '', id: null });
  const [comprobante, setComprobante] = useState('');
  const [detalle, setDetalle] = useState('');
  const [compraItems, setCompraItems] = useState([]);
  const [productQuery, setProductQuery] = useState('');
  const [selectedItemIndex, setSelectedItemIndex] = useState(null);
  const [loading, setLoading] = useState(false);
  const [creatingProveedor, setCreatingProveedor] = useState(false);
  const [msg, setMsg] = useState('');

  const load = async () => setCompras(asArray(await apiFetch('/compras')));
  const loadProveedores = async () => setProveedores(asArray(await apiFetch('/proveedores')));
  const loadProducts = async () => setProducts(asArray(await apiFetch('/productos')));

  useEffect(() => { load(); loadProveedores(); loadProducts(); }, []);

  const proveedorNombre = proveedor.nombre.trim();
  const proveedorExiste = proveedores.some((p) =>
    String(p.nombre || p.proveedor_nombre || '').trim().toLowerCase() === proveedorNombre.toLowerCase()
  );
  const showCreateProveedor = proveedorNombre && !proveedor.id && !proveedorExiste;

  const filteredProducts = filterProductsBySearch(products, productQuery, { limit: 12 });
  const showProductSuggestions = productQuery.trim().length > 0;

  const addProduct = (p) => {
    setCompraItems((current) => {
      setSelectedItemIndex(current.length);
      return [...current, {
        producto_id: p.id,
        nombre: p.nombre,
        marca: p.marca || '',
        modelo: p.modelo || '',
        categoria: p.categoria || '',
        imagen_url: p.imagen_url || '',
        cantidad: 1,
        precio: p.precio_compra || p.precio_venta || 0,
        series_list: [''],
      }];
    });
    setProductQuery('');
  };

  const updateItem = (idx, field, value) => {
    setCompraItems((current) => {
      const next = [...current];
      next[idx] = { ...next[idx], [field]: value };
      if (field === 'cantidad') {
        next[idx].series_list = resizeCompraSeriesList(next[idx].series_list, value);
      }
      return next;
    });
  };

  const updateSeries = (idx, seriesIdx, value) => {
    setCompraItems((current) => {
      const next = [...current];
      const list = [...(next[idx].series_list || [])];
      list[seriesIdx] = String(value || '').trim().toUpperCase();
      next[idx] = { ...next[idx], series_list: list };
      return next;
    });
  };

  const removeItem = (idx) => {
    setCompraItems((current) => current.filter((_, i) => i !== idx));
    if (selectedItemIndex === idx) setSelectedItemIndex(null);
    else if (selectedItemIndex > idx) setSelectedItemIndex(selectedItemIndex - 1);
  };

  const createProveedorManual = async () => {
    if (!proveedorNombre) return;
    setCreatingProveedor(true);
    try {
      const res = await apiFetch('/proveedores', { method: 'POST', json: { nombre: proveedorNombre } });
      if (okResponse(res)) {
        const updated = asArray(await apiFetch('/proveedores'));
        setProveedores(updated);
        const created = updated.find((p) =>
          String(p.nombre || p.proveedor_nombre || '').trim().toLowerCase() === proveedorNombre.toLowerCase()
        );
        setProveedor({ nombre: proveedorNombre, id: created?.id || res.id || null });
        setMsg(`Proveedor "${proveedorNombre}" creado manualmente.`);
      } else {
        alert(res.msg || 'No se pudo crear el proveedor');
      }
    } catch (e) {
      alert('Error: ' + e.message);
    }
    setCreatingProveedor(false);
  };

  const total = compraItems.reduce((sum, i) => sum + (Number(i.cantidad) * Number(i.precio) || 0), 0);

  const saveCompra = async () => {
    if (!proveedorNombre && !comprobante) return alert('Proveedor o comprobante requerido');
    for (const item of compraItems) {
      const qty = Math.max(1, Number(item.cantidad) || 1);
      const series = (item.series_list || []).map((s) => String(s || '').trim()).filter(Boolean);
      if (series.length > 0 && !canSeries) {
        return alert('Solo giomar y mily pueden ingresar series en compras.');
      }
      if (series.length > 0 && series.length !== qty) {
        return alert(`${item.nombre}: ingresa exactamente ${qty} serie(s), una por unidad.`);
      }
      const seen = new Set();
      for (const serie of series) {
        const key = normalizeSeriesKey(serie);
        if (seen.has(key)) return alert(`${item.nombre}: la serie "${serie}" esta repetida.`);
        seen.add(key);
      }
    }
    setLoading(true);
    setMsg('');
    try {
      const items = compraItems.map((item) => {
        const series = (item.series_list || []).map((s) => String(s || '').trim()).filter(Boolean);
        return {
          producto_id: item.producto_id,
          nombre: item.nombre,
          cantidad: Number(item.cantidad) || 1,
          precio: Number(item.precio) || 0,
          series_list: series,
          series_texto: series.join('\n'),
        };
      });
      const res = await apiFetch('/compras', {
        method: 'POST',
        json: {
          proveedor_nombre: proveedorNombre,
          comprobante,
          total,
          usuario_registro: user || '',
          usuario: user || '',
          detalle,
          items,
        },
      });
      if (okResponse(res)) {
        setMsg('Compra registrada. Mercaderia y series agregadas al inventario.');
        setProveedor({ nombre: '', id: null });
        setComprobante('');
        setDetalle('');
        setCompraItems([]);
        setProductQuery('');
        setSelectedItemIndex(null);
        await load();
        await loadProveedores();
      } else {
        alert(res.msg || 'Error al guardar compra');
      }
    } catch (e) {
      alert('Error: ' + e.message);
    }
    setLoading(false);
  };

  return (
    <div className="grid gap-4 xl:grid-cols-[1fr_1fr]">
      <Card title="Registrar Compra / Ingreso Mercaderia">
        <div className="grid gap-3">
          <Field label="Proveedor">
            <div className="grid gap-2 md:grid-cols-[minmax(0,1fr)_auto]">
              <SelectInput value={proveedor.id || ''} onChange={(e) => {
                const p = proveedores.find((pp) => pp.id == e.target.value);
                if (p) setProveedor({ nombre: p.nombre || p.proveedor_nombre, id: p.id });
                else setProveedor({ nombre: '', id: null });
              }}>
                <option value="">-- Seleccionar proveedor existente --</option>
                {proveedores.map((p) => <option key={p.id} value={p.id}>{p.nombre || p.proveedor_nombre}</option>)}
              </SelectInput>
              <TextInput
                placeholder="O escribe nombre manual"
                value={proveedor.nombre}
                onChange={(e) => setProveedor({ nombre: e.target.value, id: null })}
                className="font-black uppercase"
              />
            </div>
            {showCreateProveedor && (
              <div className="mt-2 rounded-md border border-amber-200 bg-amber-50 p-3">
                <div className="text-xs font-bold uppercase text-amber-800">Este proveedor no esta registrado</div>
                <div className="mt-1 text-sm font-black uppercase text-slate-900">{proveedorNombre}</div>
                <ActionButton tone="blue" className="mt-2" disabled={creatingProveedor} onClick={createProveedorManual}>
                  {creatingProveedor ? 'Creando...' : 'Crear proveedor manual'}
                </ActionButton>
              </div>
            )}
            {proveedorNombre && proveedorExiste && !proveedor.id && (
              <div className="mt-1 text-xs font-bold text-emerald-700">Proveedor existente detectado. Se usara al guardar la compra.</div>
            )}
          </Field>

          <Field label="Comprobante / Factura">
            <TextInput value={comprobante} onChange={(e) => setComprobante(e.target.value)} className="font-black uppercase" />
          </Field>

          <section className="min-h-[220px] overflow-auto rounded-lg border border-slate-200 bg-white shadow-sm">
            <div className="grid gap-0">
              {compraItems.map((item, idx) => {
                const qty = Math.max(1, Number(item.cantidad) || 1);
                const seriesFilled = (item.series_list || []).map((s) => String(s || '').trim()).filter(Boolean).length;
                const seriesOk = seriesFilled === 0 || seriesFilled === qty;
                return (
                  <div
                    key={`${item.producto_id}-${idx}`}
                    onClick={() => setSelectedItemIndex(idx)}
                    className={`grid cursor-pointer gap-3 border-b border-slate-200 p-2 transition md:grid-cols-[74px_minmax(0,1fr)_100px_100px_100px_120px] ${selectedItemIndex === idx ? 'bg-blue-50' : 'bg-white hover:bg-slate-50'}`}
                  >
                    <div className="h-16 w-16 overflow-hidden rounded-md bg-slate-100">
                      {safeImageSrc(item.imagen_url)
                        ? <img src={safeImageSrc(item.imagen_url)} alt="" className="h-full w-full object-cover" />
                        : <div className="grid h-full w-full place-items-center text-xs font-black text-slate-400">IMG</div>}
                    </div>
                    <div className="min-w-0 py-1">
                      <div className="line-clamp-1 text-sm font-black uppercase text-slate-950">{item.nombre}</div>
                      <div className="mt-1 text-xs font-semibold uppercase text-slate-500">
                        {item.categoria || 'SIN CATEGORIA'}{item.marca ? ` - ${item.marca}` : ''}{item.modelo ? ` ${item.modelo}` : ''}
                      </div>
                      <div className={`mt-1 text-[11px] font-black uppercase ${seriesOk ? 'text-emerald-700' : 'text-amber-700'}`}>
                        Series: {seriesFilled}/{qty}
                      </div>
                    </div>
                    <div className="rounded-md bg-slate-50 p-2">
                      <div className="text-[11px] font-bold uppercase text-slate-500">Cant.</div>
                      <div className="text-sm font-black text-slate-950">{item.cantidad}</div>
                    </div>
                    <div className="rounded-md bg-slate-50 p-2">
                      <div className="text-[11px] font-bold uppercase text-slate-500">P. Unit.</div>
                      <div className="text-sm font-black text-slate-950">{money(item.precio)}</div>
                    </div>
                    <div className="rounded-md bg-slate-50 p-2">
                      <div className="text-[11px] font-bold uppercase text-slate-500">Total</div>
                      <div className="text-sm font-black text-slate-950">{money(Number(item.cantidad) * Number(item.precio))}</div>
                    </div>
                    <div className="grid gap-1">
                      <div className="grid grid-cols-3 overflow-hidden rounded-md border border-slate-200 bg-white text-center">
                        <button type="button" onClick={(e) => { e.stopPropagation(); updateItem(idx, 'cantidad', Math.max(1, qty - 1)); }} className="bg-blue-600 py-2 text-lg font-black text-white">-</button>
                        <div className="grid place-items-center text-base font-black">{item.cantidad}</div>
                        <button type="button" onClick={(e) => { e.stopPropagation(); updateItem(idx, 'cantidad', qty + 1); }} className="bg-blue-600 py-2 text-lg font-black text-white">+</button>
                      </div>
                      <button type="button" onClick={(e) => { e.stopPropagation(); removeItem(idx); }} className="rounded-md border border-slate-300 py-1.5 text-xs font-black">ELIMINAR</button>
                    </div>
                  </div>
                );
              })}
              {!compraItems.length && <div className="grid min-h-[180px] place-items-center p-6"><Empty text="Busca productos abajo y agregalos con un clic." /></div>}
            </div>
          </section>

          {selectedItemIndex !== null && compraItems[selectedItemIndex] && (() => {
            const item = compraItems[selectedItemIndex];
            const qty = Math.max(1, Number(item.cantidad) || 1);
            const seriesList = resizeCompraSeriesList(item.series_list, qty);
            return (
              <div className="rounded-lg border border-blue-200 bg-blue-50/40 p-3">
                <div className="mb-2 flex items-center gap-3">
                  <div className="h-12 w-12 overflow-hidden rounded bg-slate-100">
                    {safeImageSrc(item.imagen_url)
                      ? <img src={safeImageSrc(item.imagen_url)} alt="" className="h-full w-full object-cover" />
                      : <div className="grid h-full w-full place-items-center text-xs font-black text-slate-400">IMG</div>}
                  </div>
                  <div>
                    <div className="text-sm font-black uppercase text-slate-950">{item.nombre}</div>
                    {canSeries
                      ? <div className="text-xs font-bold uppercase text-slate-500">Ingresa {qty} serie(s), una por casilla</div>
                      : <div className="text-xs font-bold uppercase text-amber-700">Solo giomar y mily pueden ingresar series en compras.</div>}
                  </div>
                </div>
                <div className="grid gap-2 sm:grid-cols-2">
                  <Field label="Precio unitario">
                    <TextInput value={item.precio} inputMode="decimal" onChange={(e) => updateItem(selectedItemIndex, 'precio', e.target.value)} className="font-black" />
                  </Field>
                  <Field label="Cantidad">
                    <TextInput value={item.cantidad} inputMode="numeric" onChange={(e) => updateItem(selectedItemIndex, 'cantidad', e.target.value)} className="font-black" />
                  </Field>
                </div>
                {canSeries && <div className="mt-2">
                  <div className="mb-2 text-[11px] font-bold uppercase text-slate-500">Series individuales ({qty})</div>
                  <div className="grid gap-2 sm:grid-cols-2">
                    {seriesList.map((serie, sIdx) => (
                      <div key={sIdx}>
                        <div className="mb-1 text-[10px] font-black uppercase text-slate-500">Serie {sIdx + 1}</div>
                        <TextInput
                          value={serie}
                          placeholder={`Serie unidad ${sIdx + 1}`}
                          onChange={(e) => updateSeries(selectedItemIndex, sIdx, e.target.value)}
                          className="font-mono font-black uppercase"
                        />
                      </div>
                    ))}
                  </div>
                </div>}
              </div>
            );
          })()}

          <div className="rounded-lg border border-slate-200 bg-white p-3 shadow-sm">
            {showProductSuggestions && (
              <div className="mb-3 max-h-[28vh] overflow-auto rounded-md border border-slate-200 bg-white">
                {filteredProducts.map((p) => (
                  <button key={p.id} type="button" onClick={() => addProduct(p)} className="grid w-full grid-cols-[58px_minmax(0,1fr)_120px_108px] items-center gap-3 border-b border-slate-100 px-3 py-2 text-left hover:bg-slate-50">
                    <div className="h-12 w-12 overflow-hidden rounded bg-slate-100">
                      {safeImageSrc(p.imagen_url)
                        ? <img src={safeImageSrc(p.imagen_url)} alt="" className="h-full w-full object-cover" />
                        : <div className="grid h-full w-full place-items-center text-xs font-black text-slate-400">IMG</div>}
                    </div>
                    <div className="min-w-0">
                      <div className="truncate text-sm font-black uppercase text-slate-950">{p.nombre}</div>
                      <div className="truncate text-xs uppercase text-slate-500">{p.categoria || 'SIN CATEGORIA'}{p.marca ? ` - ${p.marca}` : ''}</div>
                    </div>
                    <div className="text-right text-base font-black text-emerald-700">{money(p.precio_compra || p.precio_venta || p.precio)}</div>
                    <div className={`rounded-md px-3 py-2 text-center text-xs font-black ${Number(p.stock || 0) <= 0 ? 'bg-red-100 text-red-700' : 'bg-amber-100 text-amber-700'}`}>Stock {p.stock || 0}</div>
                  </button>
                ))}
                {!filteredProducts.length && <Empty text="No hay productos con esa busqueda." />}
              </div>
            )}
            <TextInput placeholder="Buscar producto..." value={productQuery} onChange={(e) => setProductQuery(e.target.value)} className="h-12 text-lg" />
          </div>

          <div className="flex items-center justify-between rounded-md bg-slate-100 px-4 py-3">
            <span className="text-xl font-black uppercase text-slate-700">Total compra</span>
            <span className="text-2xl font-black text-slate-950">{money(total)}</span>
          </div>

          <Field label="Detalle / Nota">
            <TextArea value={detalle} onChange={(e) => setDetalle(e.target.value)} />
          </Field>

          <ActionButton onClick={saveCompra} disabled={loading || compraItems.length === 0}>
            {loading ? 'Guardando...' : 'Guardar Compra e Ingresar a Inventario'}
          </ActionButton>
          {msg && <div className="text-sm font-bold text-emerald-700">{msg}</div>}
        </div>
      </Card>

      <Card title="Compras registradas">
        <div className="max-h-[680px] overflow-auto">
          {compras.map((c) => (
            <div key={c.id} className="grid border-b border-slate-100 p-3 text-sm">
              <b>{c.proveedor_nombre || c.proveedor || 'Proveedor'}</b>
              <span>{c.comprobante} / {money(c.total)}</span>
              {c.detalle && <span className="text-xs text-slate-500">{c.detalle}</span>}
            </div>
          ))}
          {!compras.length && <Empty text="Sin compras registradas." />}
        </div>
      </Card>
    </div>
  );
}

function ServiciosTecnicosView({ user }) {
  const empty = { tipo_documento: 'DNI', numero_documento: '', cliente_nombre: '', telefono: '', equipo: '', servicio: '', diagnostico: '', observacion: '', precio: '' };
  const [form, setForm] = useState(empty);
  const [rows, setRows] = useState([]);
  const [q, setQ] = useState('');
  const [viewer, setViewer] = useState(null);
  const load = async (query = q) => setRows(asArray(await apiFetch('/servicios-tecnicos', { params: query ? { q: query } : {} })));
  useEffect(() => { load(''); }, []);
  const lookup = async () => {
    if (!form.numero_documento) return;
    const res = await lookupDocumentoApi(form.numero_documento);
    if (res?.found || res?.nombre) {
      setForm((current) => ({
        ...current,
        tipo_documento: res.tipo_documento || current.tipo_documento,
        cliente_nombre: res.nombre || current.cliente_nombre,
        telefono: res.telefono || current.telefono,
      }));
    }
  };
  const printService = (row) => {
    const r = row || form;
    setViewer({
      title: `Servicio tecnico ${r.id || ''}`,
      html: `<!doctype html><html><head><meta name="viewport" content="width=device-width,initial-scale=1"><style>body{font-family:Arial;margin:24px;color:#111}.box{border:2px solid #111;padding:18px;max-width:760px;margin:auto}h1{font-size:22px;margin:0 0 14px}.grid{display:grid;grid-template-columns:150px 1fr;gap:8px;border-top:1px solid #ddd;padding-top:12px}.label{font-weight:800}.total{font-size:20px;font-weight:900;text-align:right;margin-top:20px}@media print{button{display:none}}</style></head><body><div class="box"><h1>ORDEN DE SERVICIO TECNICO</h1><div class="grid"><div class="label">Fecha</div><div>${escapeHtml(r.fecha || localTimestampForApi())}</div><div class="label">Cliente</div><div>${escapeHtml(r.cliente_nombre || '')}</div><div class="label">Documento</div><div>${escapeHtml(r.tipo_documento || 'DNI')} ${escapeHtml(r.numero_documento || '')}</div><div class="label">Telefono</div><div>${escapeHtml(r.telefono || '')}</div><div class="label">Equipo</div><div>${escapeHtml(r.equipo || '')}</div><div class="label">Servicio</div><div>${escapeHtml(r.servicio || '')}</div><div class="label">Diagnostico</div><div>${escapeHtml(r.diagnostico || '')}</div><div class="label">Observacion</div><div>${escapeHtml(r.observacion || '')}</div><div class="label">Tecnico</div><div>${escapeHtml(user || r.usuario || '')}</div></div><div class="total">TOTAL ${money(r.precio || 0)}</div><button onclick="print()">Imprimir</button></div></body></html>`,
    });
  };
  const save = async () => {
    if (!form.cliente_nombre || !form.servicio) return alert('Completa cliente y servicio.');
    const res = await apiFetch('/servicios-tecnicos', { method: 'POST', json: { ...form, precio: Number(form.precio || 0), usuario: user || '' } });
    if (okResponse(res)) {
      const saved = { ...form, id: res.id, fecha: res.fecha, usuario: user || '' };
      setForm(empty);
      await load('');
      printService(saved);
    } else alert(res.msg || 'No se pudo guardar el servicio.');
  };
  return <div className="grid gap-4 xl:grid-cols-[430px_1fr]"><Card title="Servicio tecnico"><div className="grid gap-3"><div className="grid grid-cols-[120px_1fr_auto] gap-2"><SelectInput value={form.tipo_documento} onChange={(e) => setForm({ ...form, tipo_documento: e.target.value })}><option>DNI</option><option>RUC</option></SelectInput><TextInput placeholder="Documento" value={form.numero_documento} onChange={(e) => setForm({ ...form, numero_documento: e.target.value })} /><ActionButton tone="neutral" onClick={lookup}>Buscar</ActionButton></div><Field label="Cliente"><TextInput value={form.cliente_nombre} onChange={(e) => setForm({ ...form, cliente_nombre: e.target.value })} /></Field><Field label="Telefono"><TextInput value={form.telefono} onChange={(e) => setForm({ ...form, telefono: e.target.value })} /></Field><Field label="Equipo"><TextInput value={form.equipo} onChange={(e) => setForm({ ...form, equipo: e.target.value })} placeholder="PC, laptop, impresora, componente..." /></Field><Field label="Servicio"><TextArea value={form.servicio} onChange={(e) => setForm({ ...form, servicio: e.target.value })} placeholder="Formateo, diagnostico, mantenimiento..." /></Field><Field label="Diagnostico"><TextArea value={form.diagnostico} onChange={(e) => setForm({ ...form, diagnostico: e.target.value })} /></Field><Field label="Observacion"><TextInput value={form.observacion} onChange={(e) => setForm({ ...form, observacion: e.target.value })} /></Field><Field label="Precio"><TextInput inputMode="decimal" value={form.precio} onChange={(e) => setForm({ ...form, precio: cleanPriceInput(e.target.value) })} /></Field><div className="grid grid-cols-2 gap-2"><ActionButton tone="neutral" onClick={() => printService(form)}>Vista/Imprimir</ActionButton><ActionButton onClick={save}>Guardar servicio</ActionButton></div></div></Card><Card title="Servicios registrados" actions={<><TextInput placeholder="Buscar..." value={q} onChange={(e) => setQ(e.target.value)} className="w-56" /><ActionButton tone="blue" onClick={() => load(q)}>Buscar</ActionButton><ActionButton tone="neutral" onClick={() => { setQ(''); load(''); }}>Todos</ActionButton></>}><div className="max-h-[700px] overflow-auto">{rows.map((r) => <button key={r.id} onClick={() => printService(r)} className="grid w-full gap-1 border-b border-slate-100 p-3 text-left text-sm md:grid-cols-[120px_1fr_110px]"><b>{r.fecha}</b><span><b>{r.cliente_nombre}</b><div className="text-xs text-slate-500">{r.numero_documento} / {r.telefono} / {r.equipo}</div><div className="text-xs font-bold">{r.servicio}</div></span><b>{money(r.precio || 0)}</b></button>)}{!rows.length && <Empty text="Sin servicios registrados." />}</div></Card><DocumentViewerModal viewer={viewer} onClose={() => setViewer(null)} /></div>;
}

function DocumentosView({ sound, soundEnabled, user }) {
  const [docs, setDocs] = useState([]);
  const [selectedId, setSelectedId] = useState('');
  const [detail, setDetail] = useState(null);
  const [filter, setFilter] = useState('TODOS');
  const [docSearch, setDocSearch] = useState('');
  const [docDate, setDocDate] = useState(localDateForApi());
  const [viewer, setViewer] = useState(null);
  const [manualOpen, setManualOpen] = useState(false);
  const [manualForm, setManualForm] = useState({ tipo: 'BOLETA', numero: '', cliente_nombre: '', fecha_emision: localDateForApi(), series_texto: '', observacion: '' });
  const [proformaEditor, setProformaEditor] = useState(null);
  const [editorProducts, setEditorProducts] = useState([]);
  const [editorProductQuery, setEditorProductQuery] = useState('');
  const [editorTargetLine, setEditorTargetLine] = useState(0);
  const [convertOpen, setConvertOpen] = useState(false);
  const [convertForm, setConvertForm] = useState({ tipo: 'BOLETA', estado_pago: 'PAGADO', metodo_pago: 'EFECTIVO', observacion: '' });
  const [sunatInfo, setSunatInfo] = useState(null);
  const [sunatPolling, setSunatPolling] = useState(false);
  const load = async (date = docDate, query = docSearch) => {
    const cleanQuery = String(query || '').trim();
    const params = {};
    if (date && !cleanQuery) params.fecha = date;
    if (cleanQuery) params.q = cleanQuery;
    setDocs(asArray(await apiFetch('/documentos', { params })));
  };
  useEffect(() => { load(); }, []);
  const shiftDocDate = (days) => {
    const base = docDate ? new Date(`${docDate}T12:00:00`) : new Date();
    base.setDate(base.getDate() + days);
    const next = localDateForApi(base);
    setDocDate(next);
    load(next, '');
  };
  const selected = docs.find((d) => String(d.id) === String(selectedId));
  const canEditDocuments = canEditProcessedDocuments(user);
  const filtered = docs.filter((d) => filter === 'TODOS' || String(d.tipo || '').toUpperCase() === filter);
  const applySunatEstado = (id, estadoRow) => {
    if (!id || !estadoRow) return;
    setDocs((current) => current.map((row) => (
      String(row.id) === String(id)
        ? { ...row, sunat_estado: estadoRow.sunat_estado, sunat_modo: estadoRow.sunat_modo, sunat_fecha: estadoRow.sunat_fecha }
        : row
    )));
    setDetail((current) => {
      if (!current?.documento || String(current.documento.id) !== String(id)) return current;
      return {
        ...current,
        documento: {
          ...current.documento,
          sunat_estado: estadoRow.sunat_estado,
          sunat_modo: estadoRow.sunat_modo,
          sunat_fecha: estadoRow.sunat_fecha,
        },
      };
    });
  };
  const refreshSunatEstado = async (id = selectedId) => {
    if (!id) return null;
    const res = await apiFetch(`/sunat/documentos/${id}/estado`);
    if (okResponse(res)) {
      applySunatEstado(id, res);
      setSunatInfo(res);
      return res;
    }
    return null;
  };
  const loadDetail = async (id) => {
    setSelectedId(String(id));
    setDetail(await apiFetch(`/documentos/${id}`));
    await refreshSunatEstado(id);
  };
  useEffect(() => {
    const watchDocs = selected ? [selected, ...docs] : docs;
    const shouldPoll = watchDocs.some((row) => docNeedsSunatPolling(row));
    if (!shouldPoll) {
      setSunatPolling(false);
      return undefined;
    }
    setSunatPolling(true);
    const timer = window.setInterval(async () => {
      const targets = [...new Set(watchDocs.filter((row) => docNeedsSunatPolling(row)).map((row) => row.id))];
      await Promise.all(targets.map((id) => refreshSunatEstado(id)));
      await load(docDate, docSearch);
    }, 4000);
    return () => window.clearInterval(timer);
  }, [docs, selectedId, selected?.sunat_estado, docDate, docSearch]);
  const setSunat = async (state) => { if (!selected) return; const res = await apiFetch(`/documentos/${selected.id}/sunat`, { method: 'PUT', json: { sunat_estado: state, sunat_modo: state === 'PENDIENTE' ? 'NO_ENVIAR' : 'MANUAL' } }); if (okResponse(res)) { if (soundEnabled) await sound.play(); await load(); await refreshSunatEstado(selected.id); } else alert(res.msg || 'No se pudo actualizar SUNAT.'); };
  const remove = async () => { if (!selected || !confirm(`Eliminar ${selected.tipo} ${docNumber(selected)}?`)) return; const res = await apiFetch(`/documentos/${selected.id}`, { method: 'DELETE' }); if (okResponse(res)) { setSelectedId(''); setDetail(null); await load(); } else alert(res.msg || 'No se pudo eliminar.'); };
  const annulDoc = async () => { if (!selected || !confirm(`Anular ${selected.tipo} ${docNumber(selected)} y restaurar stock?`)) return; const motivo = prompt('Motivo de anulacion', 'Anulado desde documentos') || 'Anulado desde documentos'; const res = await apiFetch(`/documentos/${selected.id}/anular`, { method: 'POST', json: { usuario: user || '', motivo } }); if (okResponse(res)) { if (soundEnabled) await sound.play(); await load(docDate, docSearch); await loadDetail(selected.id); alert(res.msg || 'Documento anulado.'); } else alert(res.msg || 'No se pudo anular.'); };
  const sendSunat = async () => {
    if (!selected) return;
    if (sunatDocEsInterno(selected)) {
      alert('Documento interno de caja. No se envia a SUNAT.');
      return;
    }
    const res = await apiFetch(`/sunat/documentos/${selected.id}/enviar`, { method: 'POST', json: { regenerar: true } });
    if (res.sunat_estado) {
      applySunatEstado(selected.id, {
        sunat_estado: res.sunat_estado,
        sunat_modo: 'API',
        sunat_fecha: new Date().toISOString(),
        respuesta: { http_status: res.http_status, response: res.respuesta },
      });
    }
    await refreshSunatEstado(selected.id);
    await load(docDate, docSearch);
    const err = sunatErrorSnippet(res.respuesta || sunatInfo?.respuesta);
    const estado = res.sunat_estado || sunatInfo?.sunat_estado || 'PENDIENTE';
    alert(err ? `SUNAT ${estado}: ${err}` : (res.msg || `SUNAT: ${estado}`));
  };
  const checkSunat = async () => {
    if (!selected) return;
    const res = await refreshSunatEstado(selected.id);
    await load(docDate, docSearch);
    const err = sunatErrorSnippet(res?.respuesta);
    alert(okResponse(res) ? (err ? `SUNAT ${res.sunat_estado}: ${err}` : `SUNAT: ${res.sunat_estado || 'PENDIENTE'}`) : (res?.msg || 'No se pudo consultar SUNAT.'));
  };
  const openInternalDoc = async () => {
    if (!selected) return;
    try {
      setViewer({ title: 'Cargando documento...', html: '<p style="font-family:Arial;padding:20px">Cargando...</p>' });
      setViewer(await buildDocumentViewer(selected));
    } catch (error) {
      setViewer(null);
      alert(error?.message || 'No se pudo abrir el documento.');
    }
  };
  const loadEditorProducts = async (query = editorProductQuery) => {
    const clean = String(query || '').trim().toLowerCase();
    const rows = asArray(await apiFetch('/productos').catch(() => []));
    setEditorProducts(rows
      .filter((p) => !clean || `${p.nombre || ''} ${p.categoria || ''} ${p.marca || ''} ${p.modelo || ''}`.toLowerCase().includes(clean))
      .slice(0, 60));
  };
  const selectEditorProduct = async (product) => {
    if (!proformaEditor || !product) return;
    const idx = Math.max(0, Math.min(Number(editorTargetLine || 0), proformaEditor.lines.length - 1));
    const seriesOptions = asArray(await apiFetch(`/productos/${product.id}/series`).catch(() => []));
    updateProformaLine(idx, {
      id: product.id,
      producto_id: product.id,
      descripcion: String(product.nombre || '').trim(),
      marca: product.marca || '',
      modelo: product.modelo || '',
      precio: Number(product.precio_venta || product.precio || proformaEditor.lines[idx]?.precio || 0),
      series_texto: '',
      series_options: seriesOptions,
    });
    setEditorProductQuery('');
    setEditorProducts([]);
  };
  const openDocumentEditor = async (targetDoc = selected, existingPayload = null) => {
    if (targetDoc?.nativeEvent || targetDoc?.preventDefault) targetDoc = selected;
    if (!targetDoc) return;
    if (!canEditDocuments) {
      alert('Solo Giomar puede cambiar boletas/facturas ya procesadas.');
      return;
    }
    if (String(targetDoc.estado || '').toUpperCase() === 'ANULADO') {
      alert('No se puede editar un documento anulado.');
      return;
    }
    const payload = existingPayload || (String(targetDoc.id) === String(selected?.id) && detail?.documento ? detail : await apiFetch(`/documentos/${targetDoc.id}`));
    const doc = { ...targetDoc, ...(payload?.documento || {}) };
    const rows = detailLinesFromPayload(payload).map((line) => ({
      id: line.id || '',
      producto_id: line.producto_id || '',
      descripcion: line.descripcion || line.nombre || '',
      cantidad: Number(line.cantidad || 1),
      precio: Number(line.precio_unitario || line.precio || 0),
      total: Number(line.total || (Number(line.cantidad || 1) * Number(line.precio_unitario || line.precio || 0))),
      series_texto: line.series_texto || line.serie || '',
      marca: line.marca || '',
      modelo: line.modelo || '',
      series_options: [],
    }));
    if (!rows.length) {
      alert('El documento no tiene detalle para editar.');
      return;
    }
    setProformaEditor({
      id: doc.id,
      tipo: doc.tipo || targetDoc.tipo || 'DOCUMENTO',
      numero: docNumber(doc),
      cliente_nombre: doc.cliente_nombre || '',
      documento_cliente: doc.documento_cliente || '',
      direccion_cliente: doc.direccion_cliente || '',
      fecha_vencimiento: String(doc.fecha_vencimiento || localDateForApi()).slice(0, 10),
      observacion: doc.observacion || '',
      observacion_cambio: '',
      lines: rows,
    });
    setEditorTargetLine(0);
    setEditorProductQuery('');
    setEditorProducts([]);
  };
  useEffect(() => {
    const pendingId = localStorage.getItem('gg_erp_pending_document_edit');
    if (!pendingId) return;
    localStorage.removeItem('gg_erp_pending_document_edit');
    if (!canEditDocuments) {
      alert('Solo Giomar puede cambiar boletas/facturas ya procesadas.');
      return;
    }
    apiFetch(`/documentos/${pendingId}`).then((payload) => {
      const doc = payload?.documento || { id: pendingId };
      setSelectedId(String(doc.id || pendingId));
      setDetail(payload);
      openDocumentEditor(doc, payload);
    }).catch((error) => {
      alert(error?.message || 'No se pudo abrir el editor del documento.');
    });
  }, [canEditDocuments]);
  const updateProformaLine = (idx, patch) => {
    setProformaEditor((current) => {
      if (!current) return current;
      const lines = current.lines.map((line, i) => {
        if (i !== idx) return line;
        const cleanPatch = { ...patch };
        if (cleanPatch.precio !== undefined) cleanPatch.precio = cleanPriceInput(cleanPatch.precio);
        const next = { ...line, ...cleanPatch };
        next.cantidad = Math.max(1, Number(next.cantidad || 1));
        next.total = next.cantidad * parsePrice(next.precio);
        return next;
      });
      return { ...current, lines };
    });
  };
  const addEditorLine = () => {
    setProformaEditor((current) => {
      if (!current) return current;
      return {
        ...current,
        lines: [
          ...current.lines,
          { id: '', producto_id: '', descripcion: '', cantidad: 1, precio: 0, total: 0, series_texto: '', marca: '', modelo: '', series_options: [] },
        ],
      };
    });
  };
  const removeEditorLine = (idx) => {
    setProformaEditor((current) => {
      if (!current) return current;
      const lines = current.lines.filter((_, i) => i !== idx);
      return { ...current, lines: lines.length ? lines : [{ id: '', producto_id: '', descripcion: '', cantidad: 1, precio: 0, total: 0, series_texto: '', marca: '', modelo: '', series_options: [] }] };
    });
  };
  const saveProformaEditor = async () => {
    if (!proformaEditor?.id) return;
    if (!proformaEditor.lines.some((line) => String(line.descripcion || '').trim())) {
      alert('Agrega al menos un producto al documento.');
      return;
    }
    const observacionCambio = String(proformaEditor.observacion_cambio || '').trim();
    if (!observacionCambio) {
      alert('Debes escribir la observacion del cambio antes de guardar.');
      return;
    }
    const total = proformaEditor.lines.reduce((sum, line) => sum + Number(line.total || 0), 0);
    const res = await apiFetch(`/documentos/${proformaEditor.id}/editar`, {
      method: 'PUT',
      json: {
        cliente_nombre: proformaEditor.cliente_nombre,
        documento_cliente: proformaEditor.documento_cliente,
        direccion_cliente: proformaEditor.direccion_cliente,
        fecha_vencimiento: proformaEditor.fecha_vencimiento || localDateForApi(),
        observacion: proformaEditor.observacion,
        observacion_cambio: observacionCambio,
        subtotal: total,
        igv: 0,
        items: proformaEditor.lines,
        usuario: user || '',
        usuario_emisor: user || '',
      },
    });
    if (okResponse(res)) {
      setProformaEditor(null);
      await load(docDate, docSearch);
      await loadDetail(res.id || selectedId);
      alert(res.msg || 'Documento actualizado.');
    } else {
      alert(res.msg || 'No se pudo actualizar el documento.');
    }
  };
  const saveManualSeries = async () => {
    if (!manualForm.series_texto.trim()) {
      alert('Ingresa las series de la boleta manual.');
      return;
    }
    const res = await apiFetch('/documentos/manual-series', { method: 'POST', json: { ...manualForm, usuario_emisor: user || '' } });
    if (okResponse(res)) {
      if (soundEnabled) await sound.play();
      setManualOpen(false);
      setManualForm({ tipo: 'BOLETA', numero: '', cliente_nombre: '', fecha_emision: localDateForApi(), series_texto: '', observacion: '' });
      await load();
      alert(`Documento registrado: ${res.tipo} ${res.numero}`);
    } else alert(res.msg || 'No se pudo registrar la boleta manual.');
  };
  const convertProforma = async () => {
    if (!selected || String(selected.tipo || '').toUpperCase() !== 'PROFORMA') return;
    const res = await apiFetch(`/documentos/${selected.id}/convertir`, { method: 'POST', json: { ...convertForm, usuario_emisor: user || '' } });
    if (okResponse(res)) {
      if (soundEnabled) await sound.play();
      setConvertOpen(false);
      await load();
      alert(res.msg || 'Proforma convertida.');
    } else alert(res.msg || 'No se pudo convertir la proforma.');
  };
  const lines = detailLinesFromPayload(detail);
  return <div className="grid gap-4 xl:grid-cols-[1fr_430px]"><Card title="Documentos" actions={<><TextInput placeholder="Buscar producto en todas las fechas..." value={docSearch} onChange={(e) => setDocSearch(e.target.value)} onKeyDown={(e) => e.key === 'Enter' && load(docDate, e.currentTarget.value)} className="w-72" /><ActionButton tone="blue" onClick={() => load(docDate, docSearch)}>Buscar</ActionButton><ActionButton tone="neutral" onClick={() => shiftDocDate(-1)}>‹</ActionButton><TextInput type="date" value={docDate} onChange={(e) => { setDocDate(e.target.value); load(e.target.value, ''); }} className="w-36" /><ActionButton tone="neutral" onClick={() => shiftDocDate(1)}>›</ActionButton><SelectInput value={filter} onChange={(e) => setFilter(e.target.value)} className="w-40"><option>TODOS</option>{DOC_TYPES.map((t) => <option key={t}>{t}</option>)}</SelectInput><ActionButton tone="blue" onClick={() => setManualOpen(true)}>Boleta manual</ActionButton><ActionButton tone="neutral" onClick={() => { setDocSearch(''); load(docDate, ''); }}>Limpiar</ActionButton><ActionButton tone="neutral" onClick={() => load(docDate, docSearch)}>Actualizar</ActionButton></>}><div className="max-h-[680px] overflow-auto">{filtered.map((d) => <button key={d.id} onClick={() => loadDetail(d.id)} className={`grid w-full gap-2 border-b border-slate-100 p-3 text-left md:grid-cols-[110px_1fr_100px_110px] ${String(selectedId) === String(d.id) ? 'bg-teal-50' : ''}`}><b>{d.tipo}</b><span><b className="text-slate-700">Fecha: {String(d.fecha_emision || d.fecha || '').slice(0, 10)}</b><br />{docNumber(d)} / {d.cliente_nombre}</span><b>{money(d.total)}</b><span className={`rounded-md px-2 py-1 text-center text-xs font-black ${String(d.estado || '').toUpperCase() === 'ANULADO' ? 'bg-red-100 text-red-700' : String(d.sunat_estado || '').toUpperCase() === 'ACEPTADO' ? 'bg-emerald-100 text-emerald-700' : String(d.sunat_estado || '').toUpperCase() === 'RECHAZADO' ? 'bg-red-100 text-red-700' : 'bg-amber-100 text-amber-700'}`}>{String(d.estado || '').toUpperCase() === 'ANULADO' ? 'ANULADO' : `SUNAT ${d.sunat_estado || 'PENDIENTE'}`}</span></button>)}</div></Card><aside className="grid gap-4"><Card title="Detalle / SUNAT">{selected ? <div className="grid gap-3"><div className="rounded-md bg-slate-50 p-3 text-sm"><b>{selected.tipo} {docNumber(selected)}</b><div>{selected.cliente_nombre}</div><div>{money(selected.total)}</div><div className={`mt-2 rounded-md p-2 text-xs font-black ${sunatStatusTone(selected.sunat_estado)}`}>SUNAT: {selected.sunat_estado || 'PENDIENTE'}{sunatPolling && docNeedsSunatPolling(selected) ? ' (actualizando...)' : ''}</div>{sunatErrorSnippet(sunatInfo?.respuesta) && <div className="mt-2 rounded-md bg-red-50 p-2 text-xs font-bold text-red-700">{sunatErrorSnippet(sunatInfo?.respuesta)}</div>}{String(selected.estado || '').toUpperCase() === 'ANULADO' && <div className="mt-2 rounded-md bg-red-100 p-2 text-xs font-black text-red-700">DOCUMENTO ANULADO</div>}</div><div className="grid grid-cols-2 gap-2">{SUNAT_STATES.map((s) => <ActionButton key={s} tone={s === 'RECHAZADO' ? 'danger' : 'neutral'} onClick={() => setSunat(s)}>{s}</ActionButton>)}</div><div className="grid grid-cols-2 gap-2"><ActionButton tone="blue" onClick={openInternalDoc}>Ver PDF</ActionButton>{String(selected.estado || '').toUpperCase() !== 'ANULADO' && <ActionButton tone="violet" onClick={openDocumentEditor}>Cambiar boleta / productos / series</ActionButton>}{['BOLETA', 'FACTURA'].includes(String(selected.tipo || '').toUpperCase()) && <ActionButton tone="blue" onClick={sendSunat}>Enviar SUNAT</ActionButton>}{['BOLETA', 'FACTURA'].includes(String(selected.tipo || '').toUpperCase()) && <ActionButton tone="neutral" onClick={checkSunat}>Estado SUNAT</ActionButton>}{String(selected.tipo || '').toUpperCase() === 'PROFORMA' && <ActionButton onClick={() => setConvertOpen(true)}>Crear boleta/factura</ActionButton>}<ActionButton tone="danger" onClick={annulDoc}>Anular</ActionButton><ActionButton tone="danger" onClick={remove}>Eliminar</ActionButton></div><div className="max-h-72 overflow-auto">{lines.map((l) => <div key={l.id} className="border-b border-slate-100 py-2 text-xs"><b>{l.descripcion || l.nombre}</b><div>{l.cantidad} x {money(l.precio_unitario || l.precio)} = {money(l.total)}</div>{(l.series_texto || l.serie) && <div className="font-bold text-blue-700">S/N: {l.series_texto || l.serie}</div>}</div>)}</div></div> : <Empty text="Selecciona un documento." />}</Card></aside>{manualOpen && <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/60 p-3"><section className="w-full max-w-xl rounded-lg bg-white shadow-2xl"><header className="flex items-center justify-between border-b border-slate-200 p-3"><h2 className="text-lg font-black">Boleta manual por series</h2><button className="rounded-md bg-slate-100 px-3 py-2 text-xs font-black" onClick={() => setManualOpen(false)}>Cerrar</button></header><div className="grid gap-3 p-3"><div className="grid grid-cols-2 gap-2"><Field label="Tipo"><SelectInput value={manualForm.tipo} onChange={(e) => setManualForm({ ...manualForm, tipo: e.target.value })}><option>BOLETA</option><option>FACTURA</option><option>PASE</option></SelectInput></Field><Field label="Numero"><TextInput value={manualForm.numero} onChange={(e) => setManualForm({ ...manualForm, numero: e.target.value })} /></Field></div><Field label="Cliente / pase"><TextInput value={manualForm.cliente_nombre} onChange={(e) => setManualForm({ ...manualForm, cliente_nombre: e.target.value })} /></Field><Field label="Fecha"><TextInput type="date" value={manualForm.fecha_emision} onChange={(e) => setManualForm({ ...manualForm, fecha_emision: e.target.value })} /></Field><Field label="Series"><TextArea value={manualForm.series_texto} onChange={(e) => setManualForm({ ...manualForm, series_texto: e.target.value })} /></Field><Field label="Observacion"><TextInput value={manualForm.observacion} onChange={(e) => setManualForm({ ...manualForm, observacion: e.target.value })} /></Field></div><footer className="grid grid-cols-2 gap-2 border-t border-slate-200 p-3"><ActionButton tone="neutral" onClick={() => setManualOpen(false)}>Cancelar</ActionButton><ActionButton onClick={saveManualSeries}>Registrar</ActionButton></footer></section></div>}{convertOpen && <Modal title="Crear boleta/factura desde proforma" onClose={() => setConvertOpen(false)}><div className="grid gap-3"><Field label="Tipo"><SelectInput value={convertForm.tipo} onChange={(e) => setConvertForm({ ...convertForm, tipo: e.target.value })}><option>BOLETA</option><option>FACTURA</option></SelectInput></Field><Field label="Estado pago"><SelectInput value={convertForm.estado_pago} onChange={(e) => setConvertForm({ ...convertForm, estado_pago: e.target.value })}>{PAY_STATES.map((s) => <option key={s}>{s}</option>)}</SelectInput></Field><Field label="Metodo"><SelectInput value={convertForm.metodo_pago} onChange={(e) => setConvertForm({ ...convertForm, metodo_pago: e.target.value })}>{PAY_METHODS.map((m) => <option key={m}>{m}</option>)}</SelectInput></Field><Field label="Observacion"><TextInput value={convertForm.observacion} onChange={(e) => setConvertForm({ ...convertForm, observacion: e.target.value })} placeholder="Convertido desde proforma" /></Field><div className="grid grid-cols-2 gap-2"><ActionButton tone="neutral" onClick={() => setConvertOpen(false)}>Cancelar</ActionButton><ActionButton onClick={convertProforma}>Crear documento</ActionButton></div></div></Modal>}{proformaEditor && <Modal title={`Cambiar ${proformaEditor.tipo || 'documento'} ${proformaEditor.numero || `#${proformaEditor.id}`}`} onClose={() => setProformaEditor(null)}><div className="grid gap-3"><Field label="Cliente"><TextInput value={proformaEditor.cliente_nombre} onChange={(e) => setProformaEditor({ ...proformaEditor, cliente_nombre: e.target.value })} /></Field><Field label="Documento cliente"><TextInput value={proformaEditor.documento_cliente} onChange={(e) => setProformaEditor({ ...proformaEditor, documento_cliente: e.target.value })} /></Field><Field label="Direccion"><TextInput value={proformaEditor.direccion_cliente} onChange={(e) => setProformaEditor({ ...proformaEditor, direccion_cliente: e.target.value })} /></Field><Field label="Vencimiento"><TextInput type="date" value={proformaEditor.fecha_vencimiento} onChange={(e) => setProformaEditor({ ...proformaEditor, fecha_vencimiento: e.target.value })} /></Field><div className="rounded-md border border-orange-200 bg-orange-50 p-3"><div className="grid gap-2 md:grid-cols-[160px_1fr_auto]"><Field label="Linea a cambiar"><SelectInput value={editorTargetLine} onChange={(e) => setEditorTargetLine(Number(e.target.value || 0))}>{proformaEditor.lines.map((line, idx) => <option key={idx} value={idx}>Linea {idx + 1}</option>)}</SelectInput></Field><Field label="Buscar producto interno"><TextInput value={editorProductQuery} onChange={(e) => setEditorProductQuery(e.target.value)} onKeyDown={(e) => e.key === 'Enter' && loadEditorProducts(e.currentTarget.value)} placeholder="Escribe nombre, marca o modelo" /></Field><div className="flex items-end gap-2"><ActionButton tone="blue" onClick={() => loadEditorProducts(editorProductQuery)}>Buscar</ActionButton><ActionButton tone="neutral" onClick={addEditorLine}>Agregar linea</ActionButton></div></div>{editorProducts.length > 0 && <div className="mt-2 max-h-44 overflow-auto rounded-md bg-white">{editorProducts.map((p) => <button type="button" key={p.id} onClick={() => selectEditorProduct(p)} className="grid w-full grid-cols-[1fr_auto] gap-2 border-b border-slate-100 p-2 text-left text-xs hover:bg-orange-50"><span><b>{p.nombre}</b><div className="text-slate-500">{p.categoria || 'Sin categoria'} / {p.marca || ''} / Stock {p.stock ?? 0}</div></span><b>{money(p.precio_venta || p.precio || 0)}</b></button>)}</div>}<p className="mt-2 text-xs font-bold text-orange-900">Usa esto cuando el cliente cambia producto o cuando quieres vincular la linea con el producto real interno para stock y series.</p></div><div className="max-h-80 overflow-auto rounded-md border border-slate-100">{proformaEditor.lines.map((line, idx) => <div key={`${line.id || idx}`} className="grid gap-2 border-b border-slate-100 p-2 md:grid-cols-[1fr_80px_110px_105px_auto]"><div><TextInput value={line.descripcion} onChange={(e) => updateProformaLine(idx, { descripcion: e.target.value })} placeholder="Producto / descripcion" /><div className="mt-1 text-[11px] font-black text-slate-500">Interno: {line.producto_id ? `#${line.producto_id}` : 'sin producto vinculado'}</div></div><TextInput inputMode="numeric" value={line.cantidad} onChange={(e) => updateProformaLine(idx, { cantidad: e.target.value })} /><div className="grid grid-cols-[1fr_auto_auto] gap-1"><TextInput inputMode="decimal" value={line.precio} onFocus={(e) => e.target.select()} onChange={(e) => updateProformaLine(idx, { precio: e.target.value })} /><button type="button" className="rounded-md bg-emerald-100 px-2 text-xs font-black text-emerald-800" onClick={() => updateProformaLine(idx, { precio: price99(line.precio) })}>.99</button><button type="button" className="rounded-md bg-slate-100 px-2 text-xs font-black text-slate-700" onClick={() => copyText(parsePrice(line.precio).toFixed(2))}>Copiar</button></div><b className="grid place-items-center rounded-md bg-slate-50 text-sm">{money(line.total)}</b><button type="button" className="rounded-md bg-red-100 px-2 text-xs font-black text-red-700" onClick={() => removeEditorLine(idx)}>Quitar</button><TextArea className="md:col-span-5 min-h-[86px]" placeholder="Series, una por linea. Puedes agregar las series que faltaron." value={line.series_texto} onChange={(e) => updateProformaLine(idx, { series_texto: e.target.value })} /></div>)}</div><Field label="Observacion del documento"><TextArea value={proformaEditor.observacion} onChange={(e) => setProformaEditor({ ...proformaEditor, observacion: e.target.value })} placeholder="Observacion visible del documento (opcional)" /></Field><Field label="Observacion del cambio (obligatorio)"><TextArea className="min-h-24" value={proformaEditor.observacion_cambio || ''} onChange={(e) => setProformaEditor({ ...proformaEditor, observacion_cambio: e.target.value })} placeholder="Ej: Cliente pidio cambiar producto / corregir precio / agregar series faltantes" /></Field><div className="rounded-md border border-violet-200 bg-violet-50 p-2 text-xs font-semibold text-violet-800">Solo Giomar puede editar. Este motivo queda guardado en la nota interna y en Registro.</div><div className="grid grid-cols-2 gap-2"><ActionButton tone="neutral" onClick={() => setProformaEditor(null)}>Cancelar</ActionButton><ActionButton onClick={saveProformaEditor}>Guardar cambios y recalcular stock</ActionButton></div></div></Modal>}<DocumentViewerModal viewer={viewer} onClose={() => setViewer(null)} /></div>;
}

function RadioView({ user, sound, soundEnabled }) {
  const userName = user && typeof user === 'object' ? (user.usuario || '') : String(user || '');
  const [messages, setMessages] = useState([]);
  const [users, setUsers] = useState([]);
  const [targetUser, setTargetUser] = useState('');
  const [recording, setRecording] = useState(false);
  const [status, setStatus] = useState('Listo para hablar');
  const [lastId, setLastId] = useState(0);
  const recorderRef = useRef(null);
  const chunksRef = useRef([]);
  const startRef = useRef(0);
  const stopTimerRef = useRef(null);
  const playedIdsRef = useRef(new Set());

  const loadUsers = async () => {
    const res = await apiFetch('/usuarios/online');
    const rows = asArray(res).filter((u) => String(u.usuario || '').toLowerCase() !== userName.toLowerCase());
    setUsers(rows);
    if (!targetUser && rows.length) setTargetUser(rows[0].usuario || '');
  };

  const load = async () => {
    const res = await apiFetch('/boquitoqui/live', { params: { usuario: userName, since_id: lastId, limit: 25 } });
    const rows = asArray(res);
    if (!rows.length) return;
    setMessages((current) => {
      const seen = new Set(current.map((m) => String(m.id)));
      const next = [...current];
      rows.forEach((row) => {
        if (!seen.has(String(row.id))) next.push(row);
      });
      return next.slice(-40);
    });
    const newest = rows[rows.length - 1];
    setLastId(Number(newest.id || lastId));
    const incomingRows = rows.filter((row) => {
      const id = String(row.id || '');
      return id && String(row.usuario_emisor || '').toLowerCase() !== userName.toLowerCase() && !playedIdsRef.current.has(id);
    });
    incomingRows.forEach((row) => playedIdsRef.current.add(String(row.id || '')));
    if (!IS_NATIVE_APP && incomingRows.length) {
      for (const msg of incomingRows.slice(-3)) await playMessage(msg, true);
    }
  };

  useEffect(() => {
    loadUsers();
    load();
    const audioTimer = window.setInterval(() => {
      load();
    }, 330);
    const usersTimer = window.setInterval(() => {
      loadUsers();
    }, 5000);
    return () => {
      window.clearInterval(audioTimer);
      window.clearInterval(usersTimer);
    };
  }, [lastId, userName]);

  const startRecording = async () => {
    if (recording) return;
    try {
      await sound?.unlock?.();
      if (soundEnabled) await sound?.radioStart?.();
      if (!navigator.mediaDevices?.getUserMedia) {
        setStatus('Este equipo no permite grabar audio desde la app.');
        return;
      }
      const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
      const AudioContext = window.AudioContext || window.webkitAudioContext;
      if (!AudioContext) {
        stream.getTracks().forEach((track) => track.stop());
        setStatus('Este equipo no permite radio en vivo desde la app.');
        return;
      }
      const ctx = new AudioContext({ sampleRate: 16000 });
      const source = ctx.createMediaStreamSource(stream);
      const processor = ctx.createScriptProcessor(2048, 1, 1);
      const sampleRate = ctx.sampleRate;
      const samplesRef = { current: [] };
      const flush = async () => {
        const samples = samplesRef.current;
        if (!samples.length) return;
        samplesRef.current = [];
        const audio_base64 = bufferToBase64(encodeWav(Float32Array.from(samples), sampleRate));
        await sendVoice({
          audio_base64,
          audio_mime: 'audio/wav',
          duracion_ms: Math.max(350, Math.round((samples.length / sampleRate) * 1000)),
        }, true);
      };
      processor.onaudioprocess = (event) => {
        const input = event.inputBuffer.getChannelData(0);
        try {
          event.outputBuffer.getChannelData(0).fill(0);
        } catch {}
        for (let i = 0; i < input.length; i += 1) samplesRef.current.push(input[i]);
      };
      source.connect(processor);
      processor.connect(ctx.destination);
      startRef.current = Date.now();
      const flushTimer = window.setInterval(() => {
        flush().catch(() => {});
      }, 520);
      recorderRef.current = {
        state: 'recording',
        stop: async () => {
          window.clearInterval(flushTimer);
          await flush().catch(() => {});
          try { processor.disconnect(); } catch {}
          try { source.disconnect(); } catch {}
          stream.getTracks().forEach((track) => track.stop());
          await ctx.close().catch(() => {});
          setStatus(`Radio enviado${targetUser ? ` a ${targetUser}` : ' a todos'}.`);
          await load();
        },
      };
      setRecording(true);
      setStatus('Hablando...');
      stopTimerRef.current = window.setTimeout(() => stopRecording(), 20000);
    } catch (error) {
      setStatus(error?.message || 'No se pudo activar el microfono.');
    }
  };

  const stopRecording = async () => {
    if (stopTimerRef.current) window.clearTimeout(stopTimerRef.current);
    stopTimerRef.current = null;
    const recorder = recorderRef.current;
    if (!recorder || recorder.state === 'inactive') return;
    recorderRef.current = null;
    setRecording(false);
    setStatus('Enviando audio...');
    await recorder.stop();
    if (soundEnabled) await sound?.radioStop?.();
  };

  const sendVoice = async (voice, silent = false) => {
    try {
      const res = await apiFetch('/boquitoqui/live', {
        method: 'POST',
        json: {
          usuario_emisor: userName,
          destinatario: targetUser,
          grupo: 'GENERAL',
          audio_mime: voice.audio_mime,
          audio_base64: voice.audio_base64,
          duracion_ms: voice.duracion_ms,
        },
      });
      if (okResponse(res)) {
        if (!silent) setStatus(`Radio enviado${targetUser ? ` a ${targetUser}` : ' a todos'}.`);
        setLastId((current) => Math.max(current, Number(res.id || current)));
        if (!silent && soundEnabled) await sound?.radioStop?.();
        if (!silent) await load();
      } else {
        if (!silent) setStatus(res.msg || 'No se pudo enviar.');
      }
    } catch (error) {
      if (!silent) setStatus(error?.message || 'No se pudo enviar.');
    }
  };

  const playMessage = async (msg, silentAlert = false) => {
    try {
      await playIncomingRadioAudio(msg);
    } catch {
      if (!silentAlert) alert('No se pudo reproducir este audio.');
    }
  };

  return (
    <div className="grid gap-4 xl:grid-cols-[420px_1fr]">
      <Card title="Boquitoqui" actions={<ActionButton tone="neutral" onClick={load}>Actualizar</ActionButton>}>
        <div className="grid gap-4">
          <Field label="Enviar a usuario">
            <SelectInput value={targetUser} onChange={(e) => setTargetUser(e.target.value)}>
              <option value="">Todos los usuarios</option>
              {users.map((u) => (
                <option key={u.usuario} value={u.usuario}>
                  {u.online ? 'EN LINEA' : 'DESCONECTADO'} - {u.usuario}
                </option>
              ))}
            </SelectInput>
          </Field>
          <button
            type="button"
            onClick={recording ? stopRecording : startRecording}
            className={`mx-auto grid h-52 w-52 place-items-center rounded-full text-center text-lg font-black text-white shadow-lg active:scale-95 ${recording ? 'bg-red-600' : 'bg-lime-700'}`}
          >
            {recording ? 'TOCAR\nPARA ENVIAR' : 'TOCAR\nPARA HABLAR'}
          </button>
          <div className="rounded-md bg-slate-50 p-3 text-center text-sm font-black text-slate-700">{status}</div>
          <div className="grid gap-2">
            {users.map((u) => (
              <button key={u.usuario} type="button" onClick={() => setTargetUser(u.usuario)} className={`flex items-center justify-between rounded-md border px-3 py-2 text-left text-xs font-black ${targetUser === u.usuario ? 'border-lime-500 bg-lime-50 text-lime-800' : 'border-slate-100 bg-slate-50 text-slate-600'}`}>
                <span>{u.usuario}</span>
                <span className={u.online ? 'text-emerald-600' : 'text-slate-400'}>{u.online ? 'EN LINEA' : 'DESCONECTADO'}</span>
              </button>
            ))}
          </div>
          <div className="text-xs font-semibold text-slate-500">El radio se envia al usuario seleccionado y suena automaticamente si el receptor tiene la app abierta o activa en segundo plano.</div>
        </div>
      </Card>
      <Card title="Mensajes de voz">
        <div className="max-h-[680px] overflow-auto">
          {messages.map((msg) => (
            <div key={msg.id} className="grid gap-2 border-b border-slate-100 p-3 md:grid-cols-[1fr_120px]">
              <div>
                <b>{msg.usuario_emisor || 'Usuario'}</b>
                <div className="text-xs text-slate-500">{msg.creado_en} / {Math.round(Number(msg.duracion_ms || 0) / 1000)}s</div>
              </div>
              <ActionButton tone="blue" onClick={() => playMessage(msg)}>Escuchar</ActionButton>
            </div>
          ))}
          {!messages.length && <Empty text="Sin mensajes de radio." />}
        </div>
      </Card>
    </div>
  );
}

function UsuariosView({ user }) {
  const [users, setUsers] = useState([]);
  const [branches, setBranches] = useState(BRANCHES);
  const [branchForm, setBranchForm] = useState({ codigo: '', nombre: '' });
  const [permUser, setPermUser] = useState(null);
  const [permDraft, setPermDraft] = useState({});
  const emptyUserForm = { usuario: '', clave: '', rol: 'VENTAS', foto_url: '', fondo_url: '', boquitoqui_enabled: false, color_tema: THEME_COLORS[0], sucursal: currentEmpresa() };
  const [form, setForm] = useState(emptyUserForm);
  const loadBranches = async () => {
    const next = asArray(await apiFetch('/sucursales')).map((b) => ({ value: b.codigo || b.value, label: b.nombre || b.label || b.codigo })).filter((b) => b.value);
    if (next.length) setBranches(next);
  };
  const load = async () => {
    const [userRows] = await Promise.all([apiFetch('/usuarios'), loadBranches().catch(() => {})]);
    setUsers(asArray(userRows));
  };
  useEffect(() => { load(); }, []);
  const save = async () => {
    const res = await apiFetch('/usuarios', { method: 'POST', json: form });
    if (okResponse(res)) {
      setForm(emptyUserForm);
      await load();
    } else alert(res.msg || 'No se pudo guardar.');
  };
  const saveBranch = async () => {
    const res = await apiFetch('/sucursales', { method: 'POST', json: { ...branchForm, usuario: user } });
    if (okResponse(res)) {
      setBranchForm({ codigo: '', nombre: '' });
      await loadBranches();
      alert('Sucursal guardada.');
    } else alert(res.msg || 'No se pudo guardar sucursal.');
  };
  const editPerms = async (u) => {
    setPermUser(u);
    const res = await apiFetch(`/usuarios/${u.id}/permisos`);
    setPermDraft(res.permisos || u.permisos || {});
  };
  const savePerms = async () => {
    if (!permUser) return;
    const res = await apiFetch(`/usuarios/${permUser.id}/permisos`, { method: 'POST', json: { usuario: user, permisos: permDraft } });
    if (okResponse(res)) {
      setPermUser(null);
      await load();
    } else alert(res.msg || 'No se pudieron guardar permisos.');
  };
  const setRole = async (u, rol) => {
    const res = await apiFetch(`/usuarios/${u.id}/rol`, { method: 'PUT', json: { rol } });
    if (okResponse(res)) await load();
    else alert(res.msg || 'No se pudo cambiar rol.');
  };
  const setRadio = async (u, enabled) => {
    const res = await apiFetch(`/usuarios/${u.id}/boquitoqui`, { method: 'PUT', json: { boquitoqui_enabled: enabled } });
    if (okResponse(res)) await load();
    else alert(res.msg || 'No se pudo cambiar boquitoqui.');
  };
  const setUserColor = async (u, color) => {
    const clean = normalizeThemeColor(color);
    const res = await apiFetch(`/usuarios/${u.id}/color`, { method: 'PUT', json: { color_tema: clean } });
    if (okResponse(res)) await load();
    else alert(res.msg || 'No se pudo cambiar color.');
  };
  const pickPhoto = (event) => {
    const file = event.target.files?.[0];
    if (!file) return;
    if (!file.type.startsWith('image/')) {
      alert('Selecciona una imagen PNG, JPG o WebP.');
      return;
    }
    const reader = new FileReader();
    reader.onload = () => setForm((current) => ({ ...current, foto_url: String(reader.result || '') }));
    reader.onerror = () => alert('No se pudo cargar la imagen.');
    reader.readAsDataURL(file);
    event.target.value = '';
  };
  const pickUserBackground = async (event) => {
    const file = event.target.files?.[0];
    event.target.value = '';
    if (!file) return;
    try {
      const dataUrl = await fileToDataUrl(file, 4);
      setForm((current) => ({ ...current, fondo_url: dataUrl }));
    } catch (error) {
      alert(error?.message || 'No se pudo cargar la imagen.');
    }
  };
  const photoPreview = safeImageSrc(form.foto_url);
  const backgroundPreview = safeImageSrc(form.fondo_url);
  return (
    <div className="grid gap-4 xl:grid-cols-[420px_1fr]">
      <Card title="Usuario">
        <div className="grid gap-3">
          <div className="flex items-center gap-3">
            <div className="grid h-20 w-20 place-items-center overflow-hidden rounded-full border border-slate-200 bg-slate-100 text-xl font-black text-slate-500">
              {photoPreview ? <img src={photoPreview} alt="" className="h-full w-full object-cover" /> : 'US'}
            </div>
            <label className="inline-flex cursor-pointer items-center justify-center rounded-md bg-slate-900 px-3 py-2 text-sm font-black text-white">
              Subir foto
              <input className="hidden" type="file" accept="image/*" onChange={pickPhoto} />
            </label>
          </div>
          <div className="grid gap-2">
            <div
              className="min-h-28 overflow-hidden rounded-md border border-slate-200 bg-slate-950 bg-cover bg-right-bottom bg-no-repeat"
              style={backgroundPreview ? { backgroundImage: `linear-gradient(90deg, rgba(15,23,42,.92), rgba(15,23,42,.35)), ${cssImageUrl(backgroundPreview)}` } : undefined}
            >
              <div className="p-3 text-xs font-black text-white">{backgroundPreview ? 'Fondo personalizado' : 'Sin fondo personalizado'}</div>
            </div>
            <div className="grid grid-cols-2 gap-2">
              <label className="inline-flex cursor-pointer items-center justify-center rounded-md bg-cyan-700 px-3 py-2 text-sm font-black text-white">
                Subir fondo
                <input className="hidden" type="file" accept="image/*" onChange={pickUserBackground} />
              </label>
              <ActionButton tone="neutral" onClick={() => setForm({ ...form, fondo_url: '' })}>Quitar fondo</ActionButton>
            </div>
          </div>
          <Field label="Usuario"><TextInput value={form.usuario} onChange={(e) => setForm({ ...form, usuario: e.target.value })} /></Field>
          <Field label="Clave"><TextInput type="password" value={form.clave} onChange={(e) => setForm({ ...form, clave: e.target.value })} /></Field>
          <Field label="Rol"><SelectInput value={form.rol} onChange={(e) => setForm({ ...form, rol: e.target.value })}><option>ADMIN</option><option>VENTAS</option></SelectInput></Field>
          <Field label="Sucursal"><SelectInput value={form.sucursal || currentEmpresa()} onChange={(e) => setForm({ ...form, sucursal: e.target.value })}>{branches.map((b) => <option key={b.value} value={b.value}>{b.label}</option>)}</SelectInput></Field>
          <Field label="Color de interfaz">
            <div className="grid grid-cols-8 gap-2">
              {THEME_COLORS.map((color) => (
                <button key={color} type="button" onClick={() => setForm({ ...form, color_tema: color })} className={`h-9 rounded-md border-2 ${normalizeThemeColor(form.color_tema) === color ? 'border-slate-950' : 'border-white'}`} style={{ backgroundColor: color }} />
              ))}
            </div>
          </Field>
          <label className="flex items-center gap-2 rounded-md bg-lime-50 p-3 text-sm font-black text-lime-800">
            <input type="checkbox" checked={!!form.boquitoqui_enabled} onChange={(e) => setForm({ ...form, boquitoqui_enabled: e.target.checked })} />
            Boquitoqui habilitado
          </label>
          <Field label="Foto URL"><TextInput value={form.foto_url} onChange={(e) => setForm({ ...form, foto_url: e.target.value })} /></Field>
          <Field label="Fondo URL"><TextInput value={form.fondo_url} onChange={(e) => setForm({ ...form, fondo_url: e.target.value })} /></Field>
          <ActionButton onClick={save}>Guardar usuario</ActionButton>
        </div>
        <div className="mt-4 grid gap-2 rounded-md border border-slate-200 bg-slate-50 p-3">
          <div className="text-xs font-black text-slate-500">Crear sucursal</div>
          <TextInput placeholder="Codigo: tienda_sur" value={branchForm.codigo} onChange={(e) => setBranchForm({ ...branchForm, codigo: e.target.value })} />
          <TextInput placeholder="Nombre visible" value={branchForm.nombre} onChange={(e) => setBranchForm({ ...branchForm, nombre: e.target.value })} />
          <ActionButton tone="blue" onClick={saveBranch}>Guardar sucursal</ActionButton>
        </div>
      </Card>
      <Card title="Usuarios">
        <div className="max-h-[680px] overflow-auto">
          {users.map((u) => {
            const userPhoto = safeImageSrc(u.foto_url);
            const userBackground = safeImageSrc(u.fondo_url);
            return (
              <div key={u.id} className="grid gap-2 border-b border-slate-100 p-3 md:grid-cols-[1fr_120px_150px_180px_120px_120px]">
                <button
                  type="button"
                  onClick={() => setForm({ usuario: u.usuario || '', clave: '', rol: u.rol || 'VENTAS', foto_url: u.foto_url || '', fondo_url: u.fondo_url || '', boquitoqui_enabled: !!u.boquitoqui_enabled, color_tema: normalizeThemeColor(u.color_tema), sucursal: u.sucursal || currentEmpresa() })}
                  className="flex min-w-0 items-center gap-3 text-left"
                >
                  <span className="grid h-10 w-10 shrink-0 place-items-center overflow-hidden rounded-full text-xs font-black text-white" style={{ backgroundColor: normalizeThemeColor(u.color_tema) }}>
                    {userPhoto ? <img src={userPhoto} alt="" className="h-full w-full object-cover" /> : String(u.usuario || 'US').slice(0, 2).toUpperCase()}
                  </span>
                  <span className="min-w-0">
                    <b className="block truncate">{u.usuario}</b>
                    <span className="block truncate text-xs text-slate-500">{u.sucursal}{userBackground ? ' / Fondo OK' : ''}</span>
                  </span>
                </button>
                <SelectInput value={u.rol || 'VENTAS'} onChange={(e) => setRole(u, e.target.value)}><option>ADMIN</option><option>VENTAS</option></SelectInput>
                <label className="flex items-center justify-center gap-2 rounded-md bg-slate-50 px-2 py-2 text-xs font-black">
                  <input type="checkbox" checked={!!u.boquitoqui_enabled} onChange={(e) => setRadio(u, e.target.checked)} />
                  Boquitoqui
                </label>
                <div className="grid grid-cols-8 gap-1">
                  {THEME_COLORS.map((color) => (
                    <button key={color} type="button" onClick={() => setUserColor(u, color)} className={`h-8 rounded border ${normalizeThemeColor(u.color_tema) === color ? 'border-slate-950' : 'border-white'}`} style={{ backgroundColor: color }} />
                  ))}
                </div>
                <span className="rounded-md bg-slate-100 px-2 py-2 text-center text-xs font-black">{u.rol}</span>
                <ActionButton tone="violet" onClick={() => editPerms(u)}>Permisos</ActionButton>
              </div>
            );
          })}
        </div>
      </Card>
      {permUser && <Modal title={`Permisos / ${permUser.usuario}`} onClose={() => setPermUser(null)}><div className="grid gap-3"><div className="grid gap-2 sm:grid-cols-2">{modules.map((m) => <label key={m.id} className="flex items-center gap-2 rounded-md bg-slate-50 p-3 text-sm font-black"><input type="checkbox" checked={permDraft[m.id] !== false} onChange={(e) => setPermDraft({ ...permDraft, [m.id]: e.target.checked })} />{m.label}</label>)}</div><div className="grid grid-cols-2 gap-2"><ActionButton tone="neutral" onClick={() => setPermUser(null)}>Cancelar</ActionButton><ActionButton onClick={savePerms}>Guardar permisos</ActionButton></div></div></Modal>}
    </div>
  );
}

async function buscarSerieGarantiaFallback(q) {
  const serieNorm = normalizeSeriesKey(q);
  const series = asArray(await apiFetch('/series', { params: { q } }));
  if (!series.length) return { ok: false, msg: `No se encontro la serie ${q}` };

  const exact = series.find((row) => normalizeSeriesKey(row.serie) === serieNorm) || series[0];
  const productoId = Number(exact.producto_id || 0);
  const products = asArray(await apiFetch('/productos'));
  const productoDb = products.find((p) => Number(p.id) === productoId);
  const producto = {
    id: productoId,
    nombre: productoDb?.nombre || exact.producto_nombre || '',
    marca: productoDb?.marca || exact.marca || '',
    modelo: productoDb?.modelo || exact.modelo || '',
    categoria: productoDb?.categoria || '',
    imagen_url: productoDb?.imagen_url || '',
    stock: Number(productoDb?.stock || 0),
  };

  let boleta = null;
  const docs = asArray(await apiFetch('/documentos', { params: { q } }));
  for (const doc of docs) {
    const full = await apiFetch(`/documentos/${doc.id}`).catch(() => ({}));
    const detalle = asArray(full.detalle || full.data);
    const line = detalle.find((row) => splitSeriesInput(row.series_texto).map((s) => normalizeSeriesKey(s)).includes(serieNorm));
    if (line || String(doc.detalle_busqueda || '').toLowerCase().includes(q.toLowerCase())) {
      boleta = {
        id: doc.id,
        tipo: doc.tipo,
        numero: doc.numero,
        cliente_nombre: doc.cliente_nombre || doc.cliente || '',
        documento_cliente: doc.documento_cliente || '',
        fecha_emision: doc.fecha_emision || doc.fecha || '',
        total: doc.total,
        descripcion: line?.descripcion || line?.nombre || producto.nombre,
        precio_unitario: line?.precio_unitario || line?.precio || 0,
        linea_total: line?.total || 0,
        series_texto: line?.series_texto || '',
      };
      break;
    }
  }

  let seriesDisponibles = [];
  if (productoId) {
    seriesDisponibles = asArray(await apiFetch(`/productos/${productoId}/series`).catch(() => []))
      .filter((row) => ['DISPONIBLE', 'RESERVADO'].includes(String(row.estado || 'DISPONIBLE').toUpperCase()))
      .filter((row) => normalizeSeriesKey(row.serie) !== serieNorm);
  }

  return {
    ok: true,
    success: true,
    serie: exact,
    producto,
    boleta,
    series_disponibles: seriesDisponibles,
  };
}

function GarantiasView({ user }) {
  const [serieSearch, setSerieSearch] = useState('');
  const [scanResult, setScanResult] = useState(null);
  const [newSerie, setNewSerie] = useState('');
  const [falla, setFalla] = useState('');
  const [loading, setLoading] = useState(false);
  const [msg, setMsg] = useState('');
  const [rows, setRows] = useState([]);
  const [lastDocumento, setLastDocumento] = useState(null);
  const [docPreview, setDocPreview] = useState(null);
  const [editorRow, setEditorRow] = useState(null);
  const [seguimientoForm, setSeguimientoForm] = useState({
    tipo_resolucion: 'EN_PROCESO',
    observacion_seguimiento: '',
    monto_devolucion: '',
    proveedor_garantia: '',
    serie_nueva: '',
    estado: '',
    solucion: '',
    generar_nota_credito: true,
  });

  const loadGarantias = async () => setRows(asArray(await apiFetch('/garantias')));
  useEffect(() => { loadGarantias(); }, []);

  const openSeguimientoEditor = (row) => {
    setEditorRow(row);
    setSeguimientoForm({
      tipo_resolucion: row.tipo_resolucion || 'EN_PROCESO',
      observacion_seguimiento: row.observacion_seguimiento || '',
      monto_devolucion: row.monto_devolucion ? String(row.monto_devolucion) : '',
      proveedor_garantia: row.proveedor_garantia || '',
      serie_nueva: '',
      estado: row.estado || '',
      solucion: row.solucion || '',
      generar_nota_credito: true,
    });
  };

  const saveSeguimiento = async () => {
    if (!editorRow?.id) return;
    if (!seguimientoForm.observacion_seguimiento.trim()) {
      alert('Escribe la observacion del seguimiento (obligatoria).');
      return;
    }
    setLoading(true);
    try {
      const res = await apiFetch(`/garantias/${editorRow.id}/seguimiento`, {
        method: 'PUT',
        json: {
          ...seguimientoForm,
          monto_devolucion: Number(seguimientoForm.monto_devolucion || 0),
          usuario: user || '',
        },
      });
      if (okResponse(res)) {
        const prevRow = editorRow;
        setMsg(res.documento_seguimiento?.numero
          ? `Seguimiento guardado. ${res.documento_seguimiento.tipo} ${res.documento_seguimiento.numero}`
          : 'Seguimiento de garantia actualizado.');
        setEditorRow(null);
        await loadGarantias();
        if (res.documento_seguimiento?.id) {
          const full = await apiFetch(`/documentos/${res.documento_seguimiento.id}`).catch(() => ({}));
          setDocPreview({
            documento: full.documento || {},
            detalle: asArray(full.detalle || full.data),
            garantia: res.data || prevRow,
            esNotaCredito: true,
          });
        } else if (prevRow?.documento_cambio_id) {
          await openDocPreview({ ...prevRow, ...(res.data || {}) }, 'cambio');
        }
      } else {
        alert(res.msg || 'No se pudo guardar el seguimiento');
      }
    } catch (e) {
      alert('Error: ' + (e.message || e));
    } finally {
      setLoading(false);
    }
  };

  const resetScan = () => {
    setScanResult(null);
    setNewSerie('');
    setFalla('');
    setMsg('');
  };

  const searchSeries = async () => {
    const q = serieSearch.trim();
    if (!q) return;
    setLoading(true);
    setMsg('');
    setScanResult(null);
    setNewSerie('');
    try {
      let res = await apiFetch('/garantias/buscar-serie', { params: { q } });
      const needsFallback = !okResponse(res) || !res.serie
        || String(res.msg || '').includes('HTTP 405')
        || String(res.msg || '').includes('HTTP 404');
      if (needsFallback) {
        res = await buscarSerieGarantiaFallback(q);
      }
      if (!okResponse(res) || !res.serie) {
        setMsg(res.msg || 'No se encontro la serie pistoleada.');
        return;
      }
      setScanResult(res);
    } catch (e) {
      try {
        const res = await buscarSerieGarantiaFallback(q);
        if (okResponse(res) && res.serie) {
          setScanResult(res);
        } else {
          setMsg(res.msg || 'Error buscando la serie.');
        }
      } catch (err) {
        setMsg('Error buscando la serie: ' + (err.message || err));
      }
    } finally {
      setLoading(false);
    }
  };

  const realizarCambio = async () => {
    if (!scanResult?.serie || !scanResult?.producto) {
      alert('Primero pistolea la serie del producto danado.');
      return;
    }
    const serieCambio = newSerie.trim().toUpperCase();
    if (!serieCambio) {
      alert('Selecciona o escanea la serie del producto de reemplazo.');
      return;
    }
    setLoading(true);
    setMsg('');
    try {
      const boleta = scanResult.boleta || {};
      const producto = scanResult.producto || {};
      const serie = scanResult.serie || {};
      const res = await apiFetch('/garantias', {
        method: 'POST',
        json: {
          cliente: boleta.cliente_nombre || '',
          documento: `${boleta.tipo || ''} ${boleta.numero || ''}`.trim(),
          producto: producto.nombre || serie.producto_nombre || '',
          serie: serie.serie || serieSearch.trim(),
          falla: falla.trim() || 'Cambio por garantia',
          estado: 'ENTREGADO',
          solucion: `Reemplazo automatico: ${producto.nombre || ''} serie ${serieCambio}`,
          usuario: user || '',
          producto_cambio_id: producto.id || null,
          producto_cambio: producto.nombre || '',
          serie_cambio: serieCambio,
          cantidad_cambio: 1,
          aplicar_cambio: true,
        },
      });
      if (okResponse(res)) {
        const doc = res.documento_cambio || null;
        setLastDocumento(doc);
        setMsg(doc?.numero
          ? `Cambio aplicado sin venta. Documento ${doc.tipo || 'GARANTIA'} ${doc.numero}`
          : `Cambio aplicado. Serie entregada: ${serieCambio}`);
        setSerieSearch('');
        resetScan();
        await loadGarantias();
        if (doc?.id) {
          const full = await apiFetch(`/documentos/${doc.id}`).catch(() => ({}));
          if (full?.documento || full?.detalle || full?.data) {
            setDocPreview({ documento: full.documento || {}, detalle: asArray(full.detalle || full.data) });
          }
        }
      } else {
        alert(res.msg || 'No se pudo aplicar el cambio');
      }
    } catch (e) {
      alert('Error al aplicar el cambio: ' + (e.message || e));
    } finally {
      setLoading(false);
    }
  };

  const serie = scanResult?.serie;
  const producto = scanResult?.producto;
  const boleta = scanResult?.boleta;
  const seriesDisponibles = asArray(scanResult?.series_disponibles);

  const openDocPreview = async (row, tipo = 'cambio') => {
    const docId = tipo === 'seguimiento' ? row.documento_seguimiento_id : row.documento_cambio_id;
    if (!docId) return;
    const full = await apiFetch(`/documentos/${docId}`).catch(() => ({}));
    setDocPreview({
      documento: full.documento || {},
      detalle: asArray(full.detalle || full.data),
      garantia: row,
      esNotaCredito: tipo === 'seguimiento',
    });
  };

  const resolucionLabel = (id) => GARANTIA_RESOLUCIONES.find((x) => x.id === id)?.label || id || '-';
  const needsMonto = ['DEVOLUCION_DINERO', 'NOTA_CREDITO'].includes(seguimientoForm.tipo_resolucion);

  return (
    <div className="grid gap-4 xl:grid-cols-[1.2fr_0.8fr]">
      <Card title="Garantia - Pistoleo y Reemplazo Automatico">
        <div className="grid gap-3">
          <div className="grid grid-cols-[1fr_auto] gap-2">
            <TextInput
              placeholder="Pistolea la serie del producto danado..."
              value={serieSearch}
              onChange={(e) => setSerieSearch(e.target.value)}
              onKeyDown={(e) => e.key === 'Enter' && searchSeries()}
              className="h-12 text-lg font-mono font-black uppercase"
              autoFocus
            />
            <ActionButton tone="blue" onClick={searchSeries} disabled={loading}>
              {loading ? 'Buscando...' : 'Buscar'}
            </ActionButton>
          </div>

          {msg && <div className="rounded-md border border-amber-200 bg-amber-50 px-3 py-2 text-sm font-bold text-amber-800">{msg}</div>}

          {serie && producto && (
            <div className="grid gap-3 xl:grid-cols-2">
              <section className="rounded-lg border border-red-200 bg-red-50/60 p-4">
                <div className="mb-2 text-[11px] font-black uppercase text-red-700">Producto en garantia</div>
                <div className="flex gap-3">
                  <div className="h-20 w-20 shrink-0 overflow-hidden rounded-md bg-white">
                    {safeImageSrc(producto.imagen_url)
                      ? <img src={safeImageSrc(producto.imagen_url)} alt="" className="h-full w-full object-cover" />
                      : <div className="grid h-full w-full place-items-center text-xs font-black text-slate-400">IMG</div>}
                  </div>
                  <div className="min-w-0">
                    <div className="line-clamp-2 text-sm font-black uppercase text-slate-950">{producto.nombre}</div>
                    <div className="mt-1 text-xs font-semibold uppercase text-slate-600">
                      {producto.categoria || 'SIN CATEGORIA'}{producto.marca ? ` - ${producto.marca}` : ''}{producto.modelo ? ` ${producto.modelo}` : ''}
                    </div>
                    <div className="mt-2 font-mono text-base font-black text-red-800">{serie.serie}</div>
                    <div className="mt-1 flex flex-wrap gap-2 text-[11px] font-black uppercase">
                      <span className={`rounded px-2 py-0.5 ${String(serie.estado || '').toUpperCase() === 'VENDIDO' ? 'bg-red-200 text-red-800' : 'bg-amber-200 text-amber-800'}`}>
                        {serie.estado || 'SIN ESTADO'}
                      </span>
                      <span className="rounded bg-white px-2 py-0.5 text-slate-700">{serie.almacen || 'TIENDA'}</span>
                    </div>
                  </div>
                </div>
              </section>

              <section className={`rounded-lg border p-4 ${boleta ? 'border-emerald-200 bg-emerald-50/60' : 'border-slate-200 bg-slate-50'}`}>
                <div className={`mb-2 text-[11px] font-black uppercase ${boleta ? 'text-emerald-700' : 'text-slate-500'}`}>Boleta / documento de venta</div>
                {boleta ? (
                  <div className="grid gap-1">
                    <div className="text-lg font-black uppercase text-slate-950">{boleta.tipo || 'DOC'} {boleta.numero || ''}</div>
                    <div className="text-sm font-black text-slate-800">{boleta.cliente_nombre || 'Sin cliente'}</div>
                    {boleta.documento_cliente && <div className="text-xs font-bold text-slate-600">DNI/RUC: {boleta.documento_cliente}</div>}
                    <div className="text-xs font-semibold text-slate-500">Fecha: {boleta.fecha_emision || '-'}</div>
                    <div className="mt-2 grid grid-cols-2 gap-2 text-sm">
                      <div className="rounded bg-white/80 p-2">
                        <div className="text-[10px] font-bold uppercase text-slate-500">P. unitario</div>
                        <div className="font-black">{money(boleta.precio_unitario)}</div>
                      </div>
                      <div className="rounded bg-white/80 p-2">
                        <div className="text-[10px] font-bold uppercase text-slate-500">Total doc.</div>
                        <div className="font-black">{money(boleta.total)}</div>
                      </div>
                    </div>
                    {boleta.descripcion && <div className="mt-1 text-xs font-semibold uppercase text-slate-600">{boleta.descripcion}</div>}
                  </div>
                ) : (
                  <div className="text-sm font-semibold text-slate-500">No se encontro boleta asociada a esta serie. Puedes continuar el cambio igual.</div>
                )}
              </section>
            </div>
          )}

          {serie && producto && (
            <section className="rounded-lg border border-blue-200 bg-blue-50/40 p-4">
              <div className="mb-3 text-sm font-black uppercase text-blue-800">Reemplazo automatico</div>
              <div className="flex gap-3 border-b border-blue-100 pb-3">
                <div className="h-16 w-16 shrink-0 overflow-hidden rounded-md bg-white">
                  {safeImageSrc(producto.imagen_url)
                    ? <img src={safeImageSrc(producto.imagen_url)} alt="" className="h-full w-full object-cover" />
                    : <div className="grid h-full w-full place-items-center text-xs font-black text-slate-400">IMG</div>}
                </div>
                <div>
                  <div className="text-sm font-black uppercase text-slate-950">{producto.nombre}</div>
                  <div className="text-xs font-bold uppercase text-slate-500">Mismo producto interno - selecciona serie disponible</div>
                  <div className="mt-1 text-xs font-black text-emerald-700">Stock: {producto.stock || 0} | Series disponibles: {seriesDisponibles.length}</div>
                </div>
              </div>

              <div className="mt-3">
                <div className="mb-2 text-[11px] font-bold uppercase text-slate-500">Series disponibles para el cambio</div>
                {seriesDisponibles.length === 0 ? (
                  <Empty text="No hay series disponibles para este producto. Revisa inventario o almacen." />
                ) : (
                  <div className="grid max-h-48 gap-2 overflow-auto sm:grid-cols-2 lg:grid-cols-3">
                    {seriesDisponibles.map((s) => {
                      const selected = newSerie.toUpperCase() === String(s.serie || '').toUpperCase();
                      const est = String(s.estado || 'DISPONIBLE').toUpperCase();
                      return (
                        <button
                          key={s.id || s.serie}
                          type="button"
                          onClick={() => setNewSerie(String(s.serie || '').toUpperCase())}
                          className={`rounded-md border p-2 text-left transition ${selected ? 'border-blue-500 bg-blue-100 ring-2 ring-blue-400' : 'border-slate-200 bg-white hover:bg-slate-50'}`}
                        >
                          <div className="font-mono text-sm font-black text-slate-950">{s.serie}</div>
                          <div className={`mt-1 text-[10px] font-black uppercase ${est === 'DISPONIBLE' ? 'text-emerald-700' : 'text-amber-700'}`}>
                            {est} / {s.almacen || 'TIENDA'}
                          </div>
                        </button>
                      );
                    })}
                  </div>
                )}
              </div>

              <div className="mt-3 grid gap-3 md:grid-cols-2">
                <Field label="O pistolea la serie de reemplazo">
                  <TextInput
                    value={newSerie}
                    onChange={(e) => setNewSerie(e.target.value.toUpperCase())}
                    placeholder="Escanea serie del producto nuevo"
                    className="font-mono font-black uppercase"
                  />
                </Field>
                <Field label="Falla / observacion (opcional)">
                  <TextInput value={falla} onChange={(e) => setFalla(e.target.value)} placeholder="Ej: No enciende, pantalla rota..." />
                </Field>
              </div>

              <div className="mt-3 flex flex-wrap gap-2">
                <ActionButton onClick={realizarCambio} disabled={!newSerie || loading}>
                  {loading ? 'Procesando...' : 'Aplicar reemplazo automatico'}
                </ActionButton>
                <ActionButton tone="neutral" onClick={resetScan} disabled={loading}>Limpiar</ActionButton>
              </div>
            </section>
          )}

          {lastDocumento?.numero && (
            <section className="rounded-lg border border-teal-200 bg-teal-50/70 p-4">
              <div className="text-[11px] font-black uppercase text-teal-800">Ultimo documento generado</div>
              <div className="mt-1 text-lg font-black text-slate-950">{lastDocumento.tipo || 'GARANTIA'} {lastDocumento.numero}</div>
              <div className="mt-1 text-xs font-semibold text-slate-600">Solo control de series. Sin precio ni caja.</div>
            </section>
          )}
        </div>
      </Card>

      <div className="grid gap-4">
        <Card title="Cambios de garantia registrados">
          <div className="max-h-[720px] overflow-auto">
            {rows.map((row) => (
              <div key={row.id} className="mb-2 rounded-lg border border-slate-200 bg-white p-3 text-sm">
                <div className="flex flex-wrap items-center justify-between gap-2">
                  <b className="font-black uppercase text-slate-900">{row.cliente || 'Sin cliente'}</b>
                  <span className={`rounded px-2 py-0.5 text-[10px] font-black uppercase ${row.cambio_aplicado ? 'bg-teal-100 text-teal-800' : 'bg-amber-100 text-amber-800'}`}>
                    {row.estado || 'RECIBIDO'}
                  </span>
                </div>
                <div className="mt-1 text-xs text-slate-500">{row.fecha}</div>
                <div className="mt-2 text-xs font-bold uppercase text-slate-700">{row.producto}</div>
                <div className="mt-2 grid gap-1 text-xs">
                  <div className="rounded bg-red-50 px-2 py-1 font-mono font-black text-red-800">INGRESO: {row.serie || '-'}</div>
                  {row.serie_cambio && <div className="rounded bg-emerald-50 px-2 py-1 font-mono font-black text-emerald-800">CAMBIO: {row.serie_cambio}</div>}
                </div>
                {row.documento && <div className="mt-1 text-[11px] font-semibold text-slate-500">Boleta origen: {row.documento}</div>}
                {row.tipo_resolucion && (
                  <div className="mt-2 rounded bg-violet-50 px-2 py-1 text-[11px] font-black uppercase text-violet-800">
                    {resolucionLabel(row.tipo_resolucion)}
                    {Number(row.monto_devolucion || 0) > 0 ? ` · ${money(row.monto_devolucion)}` : ''}
                  </div>
                )}
                {row.observacion_seguimiento && (
                  <div className="mt-1 text-[11px] text-slate-600">{row.observacion_seguimiento}</div>
                )}
                <div className="mt-2 flex flex-wrap gap-2">
                  <ActionButton tone="blue" className="!px-2 !py-1 text-[11px]" onClick={() => openSeguimientoEditor(row)}>
                    Editar seguimiento
                  </ActionButton>
                  {row.documento_cambio_numero && (
                    <button type="button" onClick={() => openDocPreview(row, 'cambio')} className="text-[11px] font-black uppercase text-blue-700 hover:underline">
                      Doc. {row.documento_cambio_numero}
                    </button>
                  )}
                  {row.documento_seguimiento_numero && (
                    <button type="button" onClick={() => openDocPreview(row, 'seguimiento')} className="text-[11px] font-black uppercase text-violet-700 hover:underline">
                      NC {row.documento_seguimiento_numero}
                    </button>
                  )}
                </div>
              </div>
            ))}
            {!rows.length && <Empty text="Aun no hay cambios de garantia registrados." />}
          </div>
        </Card>

        {editorRow && (
          <Modal title={`Editar seguimiento · Garantia #${editorRow.id}`} onClose={() => setEditorRow(null)}>
            <div className="grid gap-3">
              <div className="rounded bg-slate-50 p-2 text-xs">
                <div className="font-black uppercase">{editorRow.cliente}</div>
                <div className="font-mono text-red-700">INGRESO: {editorRow.serie}</div>
                {editorRow.serie_cambio && <div className="font-mono text-emerald-700">CAMBIO: {editorRow.serie_cambio}</div>}
              </div>
              <Field label="Tipo de resolucion">
                <SelectInput value={seguimientoForm.tipo_resolucion} onChange={(e) => setSeguimientoForm({ ...seguimientoForm, tipo_resolucion: e.target.value })}>
                  {GARANTIA_RESOLUCIONES.map((opt) => <option key={opt.id} value={opt.id}>{opt.label}</option>)}
                </SelectInput>
              </Field>
              <Field label="Observacion del seguimiento (obligatoria)">
                <TextArea
                  value={seguimientoForm.observacion_seguimiento}
                  onChange={(e) => setSeguimientoForm({ ...seguimientoForm, observacion_seguimiento: e.target.value })}
                  placeholder="Ej: Proveedor acepto cambio, cliente recibio devolucion, producto reparado..."
                />
              </Field>
              {needsMonto && (
                <Field label="Monto devolucion / nota de credito">
                  <TextInput
                    value={seguimientoForm.monto_devolucion}
                    onChange={(e) => setSeguimientoForm({ ...seguimientoForm, monto_devolucion: e.target.value })}
                    inputMode="decimal"
                    placeholder="0.00"
                  />
                </Field>
              )}
              {needsMonto && (
                <label className="flex items-center gap-2 text-sm font-bold text-slate-700">
                  <input
                    type="checkbox"
                    checked={seguimientoForm.generar_nota_credito}
                    onChange={(e) => setSeguimientoForm({ ...seguimientoForm, generar_nota_credito: e.target.checked })}
                  />
                  Generar documento NOTA DE CREDITO (NC001)
                </label>
              )}
              <Field label="Proveedor (si aplica)">
                <TextInput value={seguimientoForm.proveedor_garantia} onChange={(e) => setSeguimientoForm({ ...seguimientoForm, proveedor_garantia: e.target.value })} />
              </Field>
              <Field label="Nueva serie (si hubo otro cambio de producto)">
                <TextInput value={seguimientoForm.serie_nueva} onChange={(e) => setSeguimientoForm({ ...seguimientoForm, serie_nueva: e.target.value.toUpperCase() })} className="font-mono uppercase" />
              </Field>
              <div className="grid grid-cols-2 gap-2">
                <ActionButton tone="neutral" onClick={() => setEditorRow(null)}>Cancelar</ActionButton>
                <ActionButton onClick={saveSeguimiento} disabled={loading}>{loading ? 'Guardando...' : 'Guardar seguimiento'}</ActionButton>
              </div>
            </div>
          </Modal>
        )}

        {docPreview && (
          <Card title={`Documento ${docPreview.documento?.tipo || 'GARANTIA'} ${docPreview.documento?.numero || ''}`}>
            <div className="grid gap-2 text-sm">
              <div><b>Cliente:</b> {docPreview.documento?.cliente_nombre || docPreview.garantia?.cliente || '-'}</div>
              <div><b>Fecha:</b> {String(docPreview.documento?.fecha_emision || '').slice(0, 16)}</div>
              {docPreview.garantia?.tipo_resolucion && (
                <div className="rounded bg-violet-50 px-3 py-2 text-xs font-black uppercase text-violet-800">
                  Seguimiento: {resolucionLabel(docPreview.garantia.tipo_resolucion)}
                </div>
              )}
              <div className="rounded bg-slate-100 px-3 py-2 text-xs font-semibold text-slate-600">
                {docPreview.esNotaCredito
                  ? 'Nota de credito por garantia. Registra devolucion o ajuste sin venta de stock.'
                  : 'Documento interno de garantia. Control de series sin cobro de venta.'}
              </div>
              {docPreview.detalle.map((line, idx) => (
                <div key={idx} className="rounded border border-slate-200 p-2">
                  <div className="text-xs font-black uppercase text-slate-800">{line.descripcion}</div>
                  <div className="mt-1 font-mono text-sm font-black text-blue-800">S/N: {line.series_texto || '-'}</div>
                  <div className="text-[11px] text-slate-500">Precio: {money(line.precio_unitario || line.precio || 0)} (sin venta)</div>
                </div>
              ))}
              <ActionButton tone="neutral" onClick={() => setDocPreview(null)}>Cerrar vista</ActionButton>
            </div>
          </Card>
        )}
      </div>
    </div>
  );
}

function AuditoriaView() {
  const [rows, setRows] = useState([]);
  const [q, setQ] = useState('');
  const load = async () => setRows(asArray(await apiFetch('/auditoria', { params: { q } })));
  useEffect(() => { load(); }, []);
  return (
    <Card title="Registro de acciones (más detallado)" actions={<><TextInput placeholder="Buscar..." value={q} onChange={(e) => setQ(e.target.value)} className="w-56" /><ActionButton tone="neutral" onClick={load}>Actualizar</ActionButton></>}>
      <div className="max-h-[740px] overflow-auto">
        {rows.map((r, idx) => {
          const isIngreso = (r.accion || '').includes('INGRESO MERCADERIA');
          return (
            <div key={`${r.fecha}-${idx}`} className={`mb-2 rounded-lg border p-3 text-sm ${isIngreso ? 'border-emerald-300 bg-emerald-50' : 'border-slate-200 bg-white'}`}>
              <div className="flex flex-wrap items-center gap-2 text-xs mb-1">
                <span className="font-mono bg-slate-100 px-1 rounded">{r.fecha}</span>
                <b className="text-blue-700">{r.usuario || 'SISTEMA'}</b>
                {r.rol && <span className="text-slate-500">({r.rol})</span>}
                {r.empresa && <span className="text-slate-400">| {r.empresa}</span>}
                {isIngreso && <span className="ml-2 px-2 py-0.5 bg-emerald-200 text-emerald-800 text-[10px] rounded font-bold">INGRESO MERCADERÍA</span>}
              </div>
              <div className="font-bold text-emerald-700 mb-1">{r.accion}</div>
              <div className="text-slate-700 whitespace-pre-wrap break-words">{r.detalle}</div>
            </div>
          );
        })}
        {rows.length === 0 && <Empty text="No hay registros. Las entradas de series/mercaderia ahora se loguean con más detalle (quien, series, fecha)." />}
      </div>
      <div className="text-[10px] text-slate-500 mt-2">Ahora los ingresos de mercadería con series se registran con: usuario, series completas, fecha/hora, almacén, estado.</div>
    </Card>
  );
}

function WebView() {
  const [cfg, setCfg] = useState({ wc_store_url: '', wc_consumer_key: '', wc_consumer_secret: '', woo_auto_sync: false });
  const [products, setProducts] = useState([]);
  const [cvQuery, setCvQuery] = useState('');
  const [cvProducts, setCvProducts] = useState([]);
  const [cvMsg, setCvMsg] = useState('');
  const [msg, setMsg] = useState('');
  const load = async () => { const res = await apiFetch('/web/config'); setCfg({ ...cfg, ...(res.data || {}) }); };
  useEffect(() => { load(); }, []);
  const save = async () => { const res = await apiFetch('/web/config', { method: 'POST', json: cfg }); setMsg(okResponse(res) ? 'Configuración guardada.' : (res.msg || 'Error.')); };
  const test = async () => { const res = await apiFetch('/web/woocommerce/test'); setMsg(res.msg || (res.ok ? 'Conexión correcta.' : 'Sin conexiÃ³n.')); };
  const list = async () => setProducts(asArray(await apiFetch('/web/woocommerce/products')));
  const sync = async () => { const res = await apiFetch('/web/woocommerce/sync-products', { method: 'POST', json: { only_with_stock: false } }); setMsg(res.msg || JSON.stringify(res)); };
  const syncPrices = async () => { const res = await apiFetch('/web/woocommerce/sync-prices', { method: 'POST', json: { only_with_sku: true } }); setMsg(res.msg || JSON.stringify(res)); };
  const cvList = async () => {
    setCvMsg('Buscando CompuVision...');
    const res = await apiFetch('/web/compuvision/products', { params: { q: cvQuery, limit: 40 }, timeoutMs: 45000 });
    setCvProducts(asArray(res));
    setCvMsg(okResponse(res) ? `Productos encontrados: ${asArray(res).length}` : (res.msg || 'No se pudo leer CompuVision.'));
  };
  const cvById = async () => {
    const id = String(cvQuery || '').match(/\d+/)?.[0];
    if (!id) {
      alert('Ingresa el ID del producto CompuVision.');
      return;
    }
    setCvMsg('Consultando producto...');
    const res = await apiFetch(`/web/compuvision/product/${id}`, { timeoutMs: 30000 });
    const item = res.data || res.producto || null;
    setCvProducts(item ? [item] : []);
    setCvMsg(okResponse(res) ? 'Producto cargado.' : (res.msg || 'No se encontro el producto.'));
  };
  const cvImport = async (item) => {
    const id = item.external_id || item.prod_id || item.id;
    if (!id) return;
    const res = await apiFetch(`/web/compuvision/import/${id}`, { method: 'POST', json: {}, timeoutMs: 45000 });
    setCvMsg(res.msg || (okResponse(res) ? 'Producto importado.' : 'No se pudo importar.'));
  };
  return (
    <div className="grid gap-4 xl:grid-cols-[420px_1fr]">
      <div className="grid gap-4">
        <Card title="WooCommerce">
          <div className="grid gap-3">
            <Field label="URL tienda">
              <TextInput value={cfg.wc_store_url || ''} onChange={(e) => setCfg({ ...cfg, wc_store_url: e.target.value })} />
            </Field>
            <Field label="Consumer key">
              <TextInput value={cfg.wc_consumer_key || ''} onChange={(e) => setCfg({ ...cfg, wc_consumer_key: e.target.value })} />
            </Field>
            <Field label="Consumer secret">
              <TextInput value={cfg.wc_consumer_secret || ''} onChange={(e) => setCfg({ ...cfg, wc_consumer_secret: e.target.value })} />
            </Field>
            <label className="flex items-center gap-2 text-sm font-bold">
              <input type="checkbox" checked={!!cfg.woo_auto_sync} onChange={(e) => setCfg({ ...cfg, woo_auto_sync: e.target.checked })} />
              Autosincronizacion de precios por SKU
            </label>
            <div className="grid grid-cols-2 gap-2">
              <ActionButton onClick={save}>Guardar</ActionButton>
              <ActionButton tone="blue" onClick={test}>Probar</ActionButton>
            </div>
            <ActionButton tone="violet" onClick={syncPrices}>Sincronizar solo precios por SKU</ActionButton>
            <ActionButton tone="neutral" onClick={sync}>Sincronizar productos completos</ActionButton>
            <div className="rounded-md bg-amber-50 p-2 text-xs font-bold text-amber-800">
              Precio por SKU solo modifica el precio en WooCommerce. No cambia nombre, stock, descripcion ni imagen.
            </div>
            {msg && <div className="rounded-md bg-slate-50 p-3 text-sm font-bold">{msg}</div>}
          </div>
        </Card>

        <Card title="CompuVision Peru">
          <div className="grid gap-3">
            <Field label="Buscar por texto o ID">
              <TextInput value={cvQuery} onChange={(e) => setCvQuery(e.target.value)} placeholder="Ejemplo: 4873, RAM, CASE, ASUS" />
            </Field>
            <div className="grid grid-cols-2 gap-2">
              <ActionButton tone="blue" onClick={cvList}>Listar catalogo</ActionButton>
              <ActionButton tone="violet" onClick={cvById}>Buscar ID</ActionButton>
            </div>
            <div className="rounded-md bg-cyan-50 p-2 text-xs font-bold text-cyan-900">
              Lee productos publicos de compuvisionperu.pe/CYM y los importa al ERP con precio en soles usando su TC publicado.
            </div>
            {cvMsg && <div className="rounded-md bg-slate-50 p-3 text-sm font-bold">{cvMsg}</div>}
          </div>
        </Card>
      </div>

      <div className="grid gap-4">
        <Card title="Productos web" actions={<ActionButton tone="neutral" onClick={list}>Listar web</ActionButton>}>
          <div className="grid gap-2 md:grid-cols-2">
            {products.map((p) => (
              <div key={p.id} className="rounded-md border border-slate-200 p-3">
                <b>{p.name}</b>
                <div className="text-xs text-slate-500">{p.sku} / {p.status} / {money(p.price || p.regular_price)}</div>
              </div>
            ))}
          </div>
        </Card>

        <Card title="Catalogo CompuVision">
          <div className="grid gap-2 md:grid-cols-2">
            {cvProducts.map((p) => (
              <div key={p.external_id || p.codigo_proveedor || p.nombre} className="grid gap-2 rounded-md border border-slate-200 p-3">
                <div className="grid grid-cols-[58px_1fr] gap-3">
                  <div className="h-14 w-14 overflow-hidden rounded-md border border-slate-200 bg-slate-100">
                    {safeImageSrc(p.imagen_url)
                      ? <img src={safeImageSrc(p.imagen_url)} alt="" className="h-full w-full object-cover" />
                      : <div className="grid h-full w-full place-items-center text-[10px] font-black text-slate-400">SIN IMG</div>}
                  </div>
                  <div className="min-w-0">
                    <b className="line-clamp-2 text-sm">{p.nombre}</b>
                    <div className="text-xs text-slate-500">{p.categoria || 'SIN CAT'} / {p.marca || ''}</div>
                    <div className="text-xs font-black text-slate-700">ID {p.external_id} / {p.codigo || p.codigo_proveedor}</div>
                  </div>
                </div>
                <div className="grid grid-cols-3 gap-2 text-xs font-black">
                  <span className="rounded-md bg-slate-100 p-2">USD {Number(p.precio_usd || 0).toFixed(2)}</span>
                  <span className="rounded-md bg-emerald-50 p-2 text-emerald-800">{money(p.precio_soles || 0)}</span>
                  <span className="rounded-md bg-amber-50 p-2 text-amber-800">Stock {p.stock || 0}</span>
                </div>
                <ActionButton onClick={() => cvImport(p)}>Importar al ERP</ActionButton>
              </div>
            ))}
            {!cvProducts.length && <Empty text="Busca o lista productos CompuVision." />}
          </div>
        </Card>
      </div>
    </div>
  );
}

function sunatStatusTone(estado) {
  const value = String(estado || 'PENDIENTE').toUpperCase();
  if (value === 'ACEPTADO') return 'bg-emerald-100 text-emerald-800';
  if (value === 'RECHAZADO') return 'bg-red-100 text-red-800';
  if (value === 'PROCESO') return 'bg-blue-100 text-blue-800';
  if (value === 'INTERNO') return 'bg-slate-200 text-slate-700';
  return 'bg-amber-100 text-amber-800';
}

function sunatDocEsInterno(doc) {
  const estado = String(doc?.sunat_estado || '').toUpperCase();
  const modo = String(doc?.sunat_modo || '').toUpperCase();
  return estado === 'INTERNO' || modo === 'NO_ENVIAR' || modo === 'INTERNO';
}

function sunatErrorSnippet(payload) {
  if (!payload) return '';
  if (typeof payload === 'string') {
    const match = payload.match(/<faultstring>([^<]+)<\/faultstring>/i);
    return match?.[1]?.trim() || payload.slice(0, 180);
  }
  if (typeof payload !== 'object') return '';
  if (payload.msg) return String(payload.msg);
  const raw = String(payload.response || payload.raw || '');
  const match = raw.match(/<faultstring>([^<]+)<\/faultstring>/i);
  if (match?.[1]) return match[1].trim();
  return raw ? raw.slice(0, 180) : '';
}

const SUNAT_FINAL_STATES = new Set(['ACEPTADO', 'RECHAZADO']);

function docNeedsSunatPolling(doc) {
  const tipo = String(doc?.tipo || '').toUpperCase();
  const estado = String(doc?.sunat_estado || 'PENDIENTE').toUpperCase();
  return ['BOLETA', 'FACTURA'].includes(tipo) && estado === 'PROCESO' && !sunatDocEsInterno(doc);
}

function SunatView() {
  const empresa = currentEmpresa();
  const [cfg, setCfg] = useState({});
  const [docs, setDocs] = useState([]);
  const [docDetails, setDocDetails] = useState({});
  const [msg, setMsg] = useState('');
  const [loading, setLoading] = useState(false);
  const [docsLoading, setDocsLoading] = useState(false);
  const update = (key, value) => setCfg((current) => ({ ...current, [key]: value }));
  const loadDocs = async () => {
    setDocsLoading(true);
    try {
      const rows = asArray(await apiFetch('/documentos')).filter((doc) => ['BOLETA', 'FACTURA'].includes(String(doc.tipo || '').toUpperCase()));
      setDocs(rows.slice(0, 30));
      const details = {};
      await Promise.all(rows.slice(0, 12).map(async (doc) => {
        try {
          const res = await apiFetch(`/sunat/documentos/${doc.id}/estado`);
          if (okResponse(res)) details[String(doc.id)] = res;
        } catch {
          // ignore per-document errors
        }
      }));
      setDocDetails(details);
    } finally {
      setDocsLoading(false);
    }
  };
  const load = async () => {
    setLoading(true);
    try {
      const res = await apiFetch('/sunat/config');
      setCfg(res.data || {});
      setMsg('');
      await loadDocs();
    } finally {
      setLoading(false);
    }
  };
  useEffect(() => { load(); }, [empresa]);
  useEffect(() => {
    if (!docs.some((row) => docNeedsSunatPolling(row))) return undefined;
    const timer = window.setInterval(() => loadDocs(), 4000);
    return () => window.clearInterval(timer);
  }, [docs.map((row) => `${row.id}:${row.sunat_estado}`).join('|')]);
  const save = async () => {
    setMsg('Guardando configuracion SUNAT...');
    const res = await apiFetch('/sunat/config', { method: 'POST', json: cfg });
    if (okResponse(res)) {
      setCfg(res.data || {});
      setMsg('Configuracion SUNAT guardada.');
    } else {
      setMsg(res.msg || 'No se pudo guardar SUNAT.');
    }
  };
  const markHistoricosInternos = async () => {
    if (!window.confirm('Marcar TODAS las boletas/facturas ya registradas como INTERNAS? Ninguna se enviara a SUNAT.')) return;
    setMsg('Marcando documentos historicos como internos...');
    const res = await apiFetch('/sunat/marcar-historicos-internos', { method: 'POST' });
    setMsg(res.msg || (okResponse(res) ? 'Historicos marcados.' : 'No se pudo marcar.'));
    await loadDocs();
  };
  const sendDoc = async (doc) => {
    if (!doc?.id) return;
    if (sunatDocEsInterno(doc)) {
      alert('Documento interno de caja. No se envia a SUNAT.');
      return;
    }
    const res = await apiFetch(`/sunat/documentos/${doc.id}/enviar`, { method: 'POST', json: { regenerar: true } });
    await loadDocs();
    const err = sunatErrorSnippet(res.respuesta || res);
    const estado = res.sunat_estado || 'PENDIENTE';
    alert(err ? `SUNAT ${estado}: ${err}` : (res.msg || `SUNAT: ${estado}`));
  };
  const pickPfx = async (file) => {
    if (!file) return;
    try {
      const base64 = await fileToBase64(file, 8);
      update('certificado_pfx_base64', base64);
      setMsg(`Certificado cargado: ${file.name}. Falta guardar SUNAT.`);
    } catch (error) {
      setMsg(error.message || 'No se pudo cargar el certificado.');
    }
  };
  const branchLabel = empresa === 'pc_fast_store' ? 'PC FAST STORE' : empresa.replace(/_/g, ' ').toUpperCase();
  return (
    <div className="grid gap-4">
      <div className="rounded-lg border border-amber-200 bg-amber-50 px-4 py-3 text-sm font-bold text-amber-950">
        Sucursal activa: <span className="font-black">{branchLabel}</span> ({empresa}). La configuracion y los envios SUNAT son independientes por sucursal.
      </div>
      <div className="grid gap-4 xl:grid-cols-[420px_1fr]">
      <Card title="Estado SUNAT" actions={<><ActionButton tone="neutral" onClick={load}>Recargar</ActionButton><ActionButton onClick={save}>Guardar SUNAT</ActionButton></>}>
        <div className="grid gap-3">
          <div className={`rounded-md p-3 text-sm font-black ${cfg.listo_envio ? 'bg-emerald-100 text-emerald-800' : 'bg-amber-100 text-amber-800'}`}>ENVIO: {cfg.listo_envio ? 'LISTO' : 'FALTA CONFIG'}</div>
          <div className={`rounded-md p-3 text-sm font-black ${cfg.firma_configurada ? 'bg-emerald-100 text-emerald-800' : 'bg-red-100 text-red-800'}`}>FIRMA: {cfg.firma_configurada ? 'CERTIFICADO OK' : 'SIN CERTIFICADO PFX'}</div>
          <div className={`rounded-md p-3 text-sm font-black ${cfg.envio_automatico ? 'bg-blue-100 text-blue-800' : 'bg-slate-100 text-slate-700'}`}>ENVIO AUTOMATICO: {cfg.envio_automatico ? 'ACTIVO' : 'APAGADO'}</div>
          <div className="rounded-md bg-white p-3 text-xs font-bold text-slate-700">
            <div>RUC: {cfg.ruc || '—'}</div>
            <div>Razon social: {cfg.razon_social || '—'}</div>
            <div>Ambiente: {cfg.ambiente || 'BETA'}</div>
            <div>Usuario SOL: {cfg.usuario_sol || '—'}</div>
          </div>
          <div className="rounded-md bg-blue-50 p-3 text-xs font-bold text-blue-800">Solo boletas/facturas ELECTRONICAS (desde fecha de activacion) se envian a SUNAT. Las internas historicas quedan bloqueadas.</div>
          <div className="flex flex-wrap gap-2">
            <ActionButton tone="neutral" onClick={markHistoricosInternos}>Marcar historicos internos</ActionButton>
          </div>
          {loading && <div className="rounded-md bg-slate-50 p-3 text-sm font-bold">Cargando...</div>}
          {msg && <div className="rounded-md bg-slate-50 p-3 text-sm font-bold">{msg}</div>}
        </div>
      </Card>
      <Card title="Credenciales, empresa y certificado">
        <div className="grid gap-4">
          <div className="grid gap-3 md:grid-cols-2">
            <Field label="Ambiente"><SelectInput value={cfg.ambiente || 'BETA'} onChange={(e) => update('ambiente', e.target.value)}><option>BETA</option><option>PRODUCCION</option></SelectInput></Field>
            <Field label="Envio automatico"><SelectInput value={cfg.envio_automatico ? 'SI' : 'NO'} onChange={(e) => update('envio_automatico', e.target.value === 'SI')}><option value="NO">NO</option><option value="SI">SI</option></SelectInput></Field>
            <Field label="Fecha activacion SUNAT (solo desde aqui son legales)"><TextInput type="datetime-local" value={String(cfg.fecha_activacion_sunat || '').replace(' ', 'T').slice(0, 16)} onChange={(e) => update('fecha_activacion_sunat', e.target.value ? `${e.target.value.replace('T', ' ')}:00` : '')} /></Field>
            <Field label="RUC emisor"><TextInput value={cfg.ruc || ''} onChange={(e) => update('ruc', e.target.value)} inputMode="numeric" /></Field>
            <Field label="Razon social"><TextInput value={cfg.razon_social || ''} onChange={(e) => update('razon_social', e.target.value)} /></Field>
            <Field label="Nombre comercial"><TextInput value={cfg.nombre_comercial || ''} onChange={(e) => update('nombre_comercial', e.target.value)} /></Field>
            <Field label="Ubigeo"><TextInput value={cfg.ubigeo || '150101'} onChange={(e) => update('ubigeo', e.target.value)} /></Field>
            <Field label="Direccion fiscal"><TextInput value={cfg.direccion || ''} onChange={(e) => update('direccion', e.target.value)} /></Field>
            <Field label="Distrito"><TextInput value={cfg.distrito || 'LIMA'} onChange={(e) => update('distrito', e.target.value)} /></Field>
            <Field label="Provincia"><TextInput value={cfg.provincia || 'LIMA'} onChange={(e) => update('provincia', e.target.value)} /></Field>
            <Field label="Departamento"><TextInput value={cfg.departamento || 'LIMA'} onChange={(e) => update('departamento', e.target.value)} /></Field>
            <Field label="Usuario SOL"><TextInput value={cfg.usuario_sol || ''} onChange={(e) => update('usuario_sol', e.target.value)} placeholder="Usuario secundario SUNAT" /></Field>
            <Field label="Clave SOL"><TextInput type="password" value={cfg.clave_sol === 'CONFIGURADO' ? '' : (cfg.clave_sol || '')} onChange={(e) => update('clave_sol', e.target.value)} placeholder={cfg.clave_sol === 'CONFIGURADO' ? 'Clave guardada' : 'Clave SOL'} /></Field>
            <Field label="Clave certificado PFX"><TextInput type="password" value={cfg.certificado_password === 'CONFIGURADO' ? '' : (cfg.certificado_password || '')} onChange={(e) => update('certificado_password', e.target.value)} placeholder={cfg.certificado_password === 'CONFIGURADO' ? 'Clave de certificado guardada' : 'Clave del PFX'} /></Field>
            <Field label="Endpoint SUNAT"><TextInput value={cfg.endpoint_url || ''} onChange={(e) => update('endpoint_url', e.target.value)} placeholder="Automatico por ambiente si queda vacio" /></Field>
          </div>
          <Field label="Subir certificado digital .PFX / .P12">
            <input type="file" accept=".pfx,.p12,application/x-pkcs12" onChange={(e) => pickPfx(e.target.files?.[0])} className="block w-full rounded-md border border-slate-200 bg-white p-3 text-sm font-bold" />
            <div className="mt-1 text-xs font-bold text-slate-500">{cfg.certificado_pfx_base64 === 'CONFIGURADO' ? 'Certificado ya guardado. Si cargas otro se reemplaza al guardar.' : 'Carga aqui el certificado que te entregue SUNAT.'}</div>
          </Field>
          <div className="flex justify-end"><ActionButton onClick={save}>Guardar SUNAT</ActionButton></div>
        </div>
      </Card>
      </div>
      <Card title="Boletas y facturas SUNAT" actions={<ActionButton tone="neutral" onClick={loadDocs}>Actualizar lista</ActionButton>}>
        {docsLoading && <div className="rounded-md bg-slate-50 p-3 text-sm font-bold">Cargando documentos...</div>}
        {!docsLoading && !docs.length && <Empty text="No hay boletas ni facturas en esta sucursal." />}
        <div className="max-h-[520px] overflow-auto">
          {docs.map((doc) => {
            const detail = docDetails[String(doc.id)];
            const err = sunatErrorSnippet(detail?.respuesta);
            return (
              <div key={doc.id} className="grid gap-2 border-b border-slate-100 p-3 md:grid-cols-[1fr_140px_120px_auto] md:items-center">
                <div>
                  <div className="text-sm font-black">{doc.tipo} {docNumber(doc)}</div>
                  <div className="text-xs text-slate-500">{String(doc.fecha_emision || doc.fecha || '').slice(0, 10)} / {doc.cliente_nombre || 'Cliente'} / {money(doc.total)}</div>
                  {err && <div className="mt-1 text-xs font-bold text-red-700">{err}</div>}
                </div>
                <span className={`rounded-md px-2 py-1 text-center text-xs font-black ${sunatStatusTone(doc.sunat_estado)}`}>{doc.sunat_estado || 'PENDIENTE'}</span>
                <span className="text-xs font-bold text-slate-500">{doc.sunat_modo || 'MANUAL'}</span>
                {sunatDocEsInterno(doc)
                  ? <span className="rounded-md bg-slate-100 px-2 py-1 text-xs font-black text-slate-600">Interno</span>
                  : <ActionButton tone="blue" onClick={() => sendDoc(doc)}>Reenviar</ActionButton>}
              </div>
            );
          })}
        </div>
      </Card>
    </div>
  );
}

function AjustesView() {
  const [cfg, setCfg] = useState({});
  const [sunatCfg, setSunatCfg] = useState({});
  const [msg, setMsg] = useState('');
  const [sunatMsg, setSunatMsg] = useState('');
  const loadSunat = async () => {
    const res = await apiFetch('/sunat/config');
    setSunatCfg(res.data || {});
  };
  const load = async () => {
    const res = await apiFetch('/config/documento');
    setCfg(res.data || {});
    await loadSunat();
  };
  useEffect(() => { load(); }, []);
  const save = async () => { const res = await apiFetch('/config/documento', { method: 'POST', json: cfg }); setMsg(okResponse(res) ? 'Ajustes guardados.' : (res.msg || 'Error.')); };
  const updateSunat = (key, value) => setSunatCfg((current) => ({ ...current, [key]: value }));
  const saveSunat = async () => {
    setSunatMsg('Guardando configuracion SUNAT...');
    const res = await apiFetch('/sunat/config', { method: 'POST', json: sunatCfg });
    if (okResponse(res)) {
      setSunatCfg(res.data || {});
      setSunatMsg('Configuracion SUNAT guardada.');
    } else {
      setSunatMsg(res.msg || 'No se pudo guardar SUNAT.');
    }
  };
  const pickPfx = async (file) => {
    if (!file) return;
    try {
      const base64 = await fileToBase64(file, 8);
      updateSunat('certificado_pfx_base64', base64);
      setSunatMsg(`Certificado cargado: ${file.name}. Falta guardar.`);
    } catch (error) {
      setSunatMsg(error.message || 'No se pudo cargar el certificado.');
    }
  };
  const layout = cfg?.doc_editor?.layout || {};
  const updateLayout = (key, value) => setCfg((current) => ({
    ...current,
    doc_editor: {
      ...(current.doc_editor || {}),
      layout: {
        ...((current.doc_editor || {}).layout || {}),
        [key]: value,
      },
    },
  }));
  const backup = async () => {
    setMsg('Generando respaldo local...');
    const res = await apiFetch('/backup/export');
    if (!okResponse(res)) {
      setMsg(res.msg || 'No se pudo generar respaldo.');
      return;
    }
    const name = 'gg_erp_backup_' + localDateForApi() + '_' + Date.now() + '.json';
    const blob = new Blob([JSON.stringify(res, null, 2)], { type: 'application/json' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = name;
    document.body.appendChild(a);
    a.click();
    a.remove();
    URL.revokeObjectURL(url);
    setMsg('Respaldo descargado: ' + name);
  };
  const pickLogo = (file) => {
    if (!file) return;
    const reader = new FileReader();
    reader.onload = () => setCfg((current) => ({ ...current, logo: String(reader.result || '') }));
    reader.onerror = () => setMsg('No se pudo cargar el logo.');
    reader.readAsDataURL(file);
  };
  return (
    <div className="grid gap-4 xl:grid-cols-[460px_1fr]">
      <Card title="Ajustes de documentos y sucursal" actions={<><ActionButton tone="neutral" onClick={load}>Recargar</ActionButton><ActionButton tone="blue" onClick={backup}>Backup local</ActionButton><ActionButton onClick={save}>Guardar</ActionButton></>}>
        <div className="grid gap-3">
          <Field label="Nombre empresa"><TextInput value={cfg.company_name || cfg.empresa || ''} onChange={(e) => setCfg({ ...cfg, company_name: e.target.value, empresa: e.target.value })} /></Field>
          <Field label="RUC"><TextInput value={cfg.ruc || ''} onChange={(e) => setCfg({ ...cfg, ruc: e.target.value })} /></Field>
          <Field label="Direccion"><TextInput value={cfg.address || cfg.direccion || ''} onChange={(e) => setCfg({ ...cfg, address: e.target.value, direccion: e.target.value })} /></Field>
          <Field label="Plantilla"><TextInput value={cfg.template_name || ''} onChange={(e) => setCfg({ ...cfg, template_name: e.target.value })} /></Field>
          <Field label="Logo del documento">
            <TextInput value={cfg.logo || ''} onChange={(e) => setCfg({ ...cfg, logo: e.target.value })} />
            <input type="file" accept="image/png,image/jpeg,image/webp" onChange={(e) => pickLogo(e.target.files?.[0])} className="mt-2 block w-full rounded-md border border-slate-200 bg-white p-2 text-sm font-bold" />
          </Field>
          {msg && <div className="rounded-md bg-slate-50 p-3 text-sm font-bold">{msg}</div>}
        </div>
      </Card>
      <div className="grid gap-4">
        <Card title="SUNAT automatico" actions={<><ActionButton tone="neutral" onClick={loadSunat}>Recargar</ActionButton><ActionButton onClick={saveSunat}>Guardar SUNAT</ActionButton></>}>
          <div className="grid gap-3">
            <div className="grid gap-2 md:grid-cols-3">
              <div className={`rounded-md p-3 text-xs font-black ${sunatCfg.listo_envio ? 'bg-emerald-100 text-emerald-800' : 'bg-amber-100 text-amber-800'}`}>ENVIO: {sunatCfg.listo_envio ? 'LISTO' : 'FALTA CONFIG'}</div>
              <div className={`rounded-md p-3 text-xs font-black ${sunatCfg.firma_configurada ? 'bg-emerald-100 text-emerald-800' : 'bg-red-100 text-red-800'}`}>FIRMA: {sunatCfg.firma_configurada ? 'CERTIFICADO OK' : 'SIN PFX'}</div>
              <div className={`rounded-md p-3 text-xs font-black ${sunatCfg.envio_automatico ? 'bg-blue-100 text-blue-800' : 'bg-slate-100 text-slate-700'}`}>AUTO: {sunatCfg.envio_automatico ? 'ACTIVO' : 'APAGADO'}</div>
            </div>
            <div className="grid gap-3 md:grid-cols-2">
              <Field label="Ambiente"><SelectInput value={sunatCfg.ambiente || 'BETA'} onChange={(e) => updateSunat('ambiente', e.target.value)}><option>BETA</option><option>PRODUCCION</option></SelectInput></Field>
              <Field label="Envio automatico"><SelectInput value={sunatCfg.envio_automatico ? 'SI' : 'NO'} onChange={(e) => updateSunat('envio_automatico', e.target.value === 'SI')}><option value="NO">NO</option><option value="SI">SI</option></SelectInput></Field>
              <Field label="RUC emisor"><TextInput value={sunatCfg.ruc || ''} onChange={(e) => updateSunat('ruc', e.target.value)} inputMode="numeric" /></Field>
              <Field label="Razon social"><TextInput value={sunatCfg.razon_social || ''} onChange={(e) => updateSunat('razon_social', e.target.value)} /></Field>
              <Field label="Nombre comercial"><TextInput value={sunatCfg.nombre_comercial || ''} onChange={(e) => updateSunat('nombre_comercial', e.target.value)} /></Field>
              <Field label="Ubigeo"><TextInput value={sunatCfg.ubigeo || '150101'} onChange={(e) => updateSunat('ubigeo', e.target.value)} /></Field>
              <Field label="Direccion fiscal"><TextInput value={sunatCfg.direccion || ''} onChange={(e) => updateSunat('direccion', e.target.value)} /></Field>
              <Field label="Distrito"><TextInput value={sunatCfg.distrito || 'LIMA'} onChange={(e) => updateSunat('distrito', e.target.value)} /></Field>
              <Field label="Provincia"><TextInput value={sunatCfg.provincia || 'LIMA'} onChange={(e) => updateSunat('provincia', e.target.value)} /></Field>
              <Field label="Departamento"><TextInput value={sunatCfg.departamento || 'LIMA'} onChange={(e) => updateSunat('departamento', e.target.value)} /></Field>
              <Field label="Usuario SOL"><TextInput value={sunatCfg.usuario_sol || ''} onChange={(e) => updateSunat('usuario_sol', e.target.value)} placeholder="RUC + usuario secundario" /></Field>
              <Field label="Clave SOL"><TextInput type="password" value={sunatCfg.clave_sol === 'CONFIGURADO' ? '' : (sunatCfg.clave_sol || '')} onChange={(e) => updateSunat('clave_sol', e.target.value)} placeholder={sunatCfg.clave_sol === 'CONFIGURADO' ? 'Clave guardada' : 'Clave SOL'} /></Field>
              <Field label="Endpoint SUNAT"><TextInput value={sunatCfg.endpoint_url || ''} onChange={(e) => updateSunat('endpoint_url', e.target.value)} placeholder="Se autocompleta por ambiente si lo dejas vacio" /></Field>
              <Field label="Clave certificado PFX"><TextInput type="password" value={sunatCfg.certificado_password === 'CONFIGURADO' ? '' : (sunatCfg.certificado_password || '')} onChange={(e) => updateSunat('certificado_password', e.target.value)} placeholder={sunatCfg.certificado_password === 'CONFIGURADO' ? 'Clave de certificado guardada' : 'Clave del PFX'} /></Field>
            </div>
            <Field label="Archivo certificado .PFX / .P12">
              <input type="file" accept=".pfx,.p12,application/x-pkcs12" onChange={(e) => pickPfx(e.target.files?.[0])} className="block w-full rounded-md border border-slate-200 bg-white p-2 text-sm font-bold" />
              <div className="mt-1 text-xs font-bold text-slate-500">{sunatCfg.certificado_pfx_base64 === 'CONFIGURADO' ? 'Certificado ya guardado. Carga otro solo si quieres reemplazarlo.' : 'Cuando SUNAT te entregue el certificado, cargalo aqui y guarda.'}</div>
            </Field>
            <div className="rounded-md bg-blue-50 p-3 text-xs font-bold text-blue-800">Recomendado: primero usar BETA. Cuando SUNAT acepte las pruebas, cambiar a PRODUCCION y activar envio automatico.</div>
            {sunatMsg && <div className="rounded-md bg-slate-50 p-3 text-sm font-bold">{sunatMsg}</div>}
          </div>
        </Card>
        <Card title="Numeros de impresion">
          <div className="grid gap-3 md:grid-cols-2">
            <Field label="Max. productos"><TextInput type="number" min="1" max="24" value={layout.max_productos ?? 12} onChange={(e) => updateLayout('max_productos', Number(e.target.value || 12))} /></Field>
            <Field label="Alto fila mm"><TextInput type="number" step="0.1" value={layout.alto_fila_mm ?? 6.5} onChange={(e) => updateLayout('alto_fila_mm', Number(e.target.value || 6.5))} /></Field>
            <Field label="Alto tabla mm"><TextInput type="number" step="1" value={layout.alto_tabla_mm ?? 86} onChange={(e) => updateLayout('alto_tabla_mm', Number(e.target.value || 86))} /></Field>
            <Field label="Letra tabla px"><TextInput type="number" step="0.1" value={layout.letra_tabla_px ?? 7.0} onChange={(e) => updateLayout('letra_tabla_px', Number(e.target.value || 7.0))} /></Field>
            <Field label="Letra descripcion px"><TextInput type="number" step="0.1" value={layout.letra_descripcion_px ?? 7.0} onChange={(e) => updateLayout('letra_descripcion_px', Number(e.target.value || 7.0))} /></Field>
            <Field label="Margen superior mm"><TextInput type="number" step="0.5" value={layout.margen_superior_mm ?? 8} onChange={(e) => updateLayout('margen_superior_mm', Number(e.target.value || 8))} /></Field>
            <Field label="Logo ancho mm"><TextInput type="number" step="0.5" value={layout.logo_ancho_mm ?? 22} onChange={(e) => updateLayout('logo_ancho_mm', Number(e.target.value || 22))} /></Field>
            <Field label="Logo alto mm"><TextInput type="number" step="0.5" value={layout.logo_alto_mm ?? 15} onChange={(e) => updateLayout('logo_alto_mm', Number(e.target.value || 15))} /></Field>
            <Field label="Bajar logo mm"><TextInput type="number" step="0.5" value={layout.logo_bajar_mm ?? 23} onChange={(e) => updateLayout('logo_bajar_mm', Number(e.target.value || 23))} /></Field>
          </div>
          <div className="mt-3 rounded-md bg-blue-50 p-3 text-xs font-bold text-blue-800">Reglas activas: limite de productos, descripcion con salto de linea, texto ajustado por columna y logo controlado por medidas.</div>
        </Card>
        <Card title="Config avanzada JSON">
          <Field label="JSON"><TextArea value={JSON.stringify(cfg, null, 2)} onChange={(e) => { try { setCfg(JSON.parse(e.target.value)); } catch { setMsg('JSON invalido.'); } }} className="min-h-72 font-mono text-xs" /></Field>
        </Card>
      </div>
    </div>
  );
}

export default function App() {
  const [user, setUser] = useState(null);
  const [view, setView] = useState('dashboard');
  const [soundEnabled, setSoundEnabled] = useState(true);
  const [lastDocKey, setLastDocKey] = useState('');
  const [watchReady, setWatchReady] = useState(false);
  const detectedDeviceMode = useDeviceMode();
  const deviceMode = (IS_PC_TESTER || IS_PC_DESKTOP) ? 'dex' : detectedDeviceMode;
  const sound = useCashRegisterSound();
  const { updateInfo, checkForUpdate } = useAndroidAutoUpdate();
  const userName = user && typeof user === 'object' ? (user.usuario || '') : String(user || '');
  const radioEnabled = !!user;
  useGlobalRadioAutoPlay({ userName, radioEnabled: radioEnabled && view !== 'radio' });

  useEffect(() => {
    if (!userName) return undefined;
    let active = true;
    apiFetch('/usuarios/perfil', { params: { usuario: userName } }).then((res) => {
      if (!active || !res?.found) return;
      setUser((current) => {
        if (!current || typeof current !== 'object') return current;
        return { ...current, ...res, ok: true };
      });
    }).catch(() => {});
    return () => { active = false; };
  }, [userName]);

  useEffect(() => {
    if (Capacitor.getPlatform?.() !== 'android') return;
    AndroidRadio.configure({ enabled: !!userName, user: userName }).catch(() => {});
  }, [radioEnabled, userName]);

  useEffect(() => {
    if (!userName) return undefined;
    const beat = () => {
      apiFetch('/usuarios/online', {
        method: 'POST',
        json: {
          usuario: userName,
          vista: view,
          dispositivo: IS_PC_TESTER ? 'PC TESTER' : deviceMode,
        },
      }).catch(() => {});
    };
    beat();
    const timer = window.setInterval(beat, 30000);
    return () => window.clearInterval(timer);
  }, [userName, view, deviceMode]);

  useEffect(() => {
    if (!user) return undefined;
    let active = true;
    const checkDocuments = async () => {
      try {
        const res = await apiFetch('/documentos/ultimo');
        const newest = res?.data || null;
        const key = newest ? `${newest.id}-${docNumber(newest)}-${newest.total}` : '';
        if (!active || !key) return;
        if (!watchReady) {
          setLastDocKey(key);
          setWatchReady(true);
          return;
        }
        if (lastDocKey && key !== lastDocKey) {
          setLastDocKey(key);
          if (soundEnabled) await sound.play();
        }
      } catch {
        // Retry on next tick.
      }
    };
    checkDocuments();
    const timer = window.setInterval(checkDocuments, 15000);
    return () => {
      active = false;
      window.clearInterval(timer);
    };
  }, [user, soundEnabled, lastDocKey, watchReady]);

  useEffect(() => {
    const openCaja = () => {
      const target = localStorage.getItem('gg_erp_pending_view') || 'caja';
      localStorage.removeItem('gg_erp_pending_view');
      setView(target === 'radio' ? 'radio' : 'caja');
    };
    const pending = localStorage.getItem('gg_erp_pending_view');
    if (user && (pending === 'caja' || pending === 'radio')) openCaja();
    const handler = (event) => {
      if (!event?.detail || event.detail === 'caja' || event.detail === 'radio') {
        localStorage.setItem('gg_erp_pending_view', event.detail || 'caja');
        openCaja();
      }
    };
    window.addEventListener('gg-erp-open-view', handler);
    return () => window.removeEventListener('gg-erp-open-view', handler);
  }, [user]);

  if (!user) return <Login onLogin={setUser} sound={sound} deviceMode={deviceMode} />;

  return (
    <AppShell
      user={user}
      view={view}
      setView={setView}
      sound={sound}
      soundEnabled={soundEnabled}
      setSoundEnabled={setSoundEnabled}
      radioEnabled={radioEnabled}
      deviceMode={deviceMode}
      updateInfo={updateInfo}
      onUserUpdate={(patch) => setUser((current) => (current && typeof current === 'object' ? { ...current, ...patch } : current))}
      onCheckUpdate={IS_PC_TESTER ? () => alert('VersiÃ³n tester local. No se publica ni actualiza a todos.') : checkForUpdate}
      onLogout={() => {
        sessionStorage.setItem('gg_erp_skip_auto_login', '1');
        setUser(null);
      }}
    >
      {view === 'dashboard' && <DashboardView setView={setView} />}
      {view === 'ventas' && <VentasView user={userName} sound={sound} soundEnabled={soundEnabled} setView={setView} />}
      {view === 'reservas' && <ReservasView user={userName} setView={setView} />}
      {view === 'caja' && <CajaView sound={sound} soundEnabled={soundEnabled} user={userName} setView={setView} />}
      {view === 'clientes' && <ClientesView />}
      {view === 'productos' && <ProductosView user={userName} />}
      {view === 'inventario' && <InventarioView user={userName} />}
      {view === 'compras' && <ComprasView user={userName} />}
      {view === 'documentos' && <DocumentosView sound={sound} soundEnabled={soundEnabled} user={userName} />}
      {view === 'sunat' && <SunatView />}
      {view === 'usuarios' && <UsuariosView user={userName} />}
      {view === 'radio' && <RadioView user={user} sound={sound} soundEnabled={soundEnabled} />}
      {view === 'servicios' && <ServiciosTecnicosView user={userName} />}
      {view === 'garantias' && <GarantiasView user={userName} />}
      {view === 'auditoria' && <AuditoriaView />}
      {view === 'web' && <WebView />}
      {view === 'ajustes' && <AjustesView />}
    </AppShell>
  );
}





